fix: L12 diagnostic review-finding batch (createdump runtime-aware discovery, Step2DeadlineSeconds validation, timeout-branch crash-flag removal, KNOWN-LIMITATIONS L12 rewording, ci-parity --diag option-value consumption, EnvVarGuardScanner per-symbol #elif masking)

Seven verified findings fixed, none skipped:

1. scripts/diagnose-l12-createdump.ps1 Step 0: createdump discovery now
   resolves the .NET runtime used by the OfficeIntegration testhost
   (runtimeconfig.json to Microsoft.NETCore.App.RuntimeVersion) and selects
   the createdump.exe under that runtime shared framework directory, with
   a version-matched recursive fallback. The same resolved $createdump path
   is used and logged by both Step 0 and Step 2.

2. Step 1 pre-flight: $Step2DeadlineSeconds validation added alongside the
   existing $Step1TimeoutSeconds/$Step1DeadlineSeconds checks. Zero or
   negative values exit 2 through the same validation path before Step 2
   can produce a misleading timeout-killed result.

3. Timeout branch: $step1Crashed = $true removed. A Step 1 deadline hit
   reports outcome=timeout only, so the summary never conflates timeout
   with crash.

4. docs/KNOWN-LIMITATIONS.md L12 entry: the debug-privilege / antivirus-EDR
   diagnostic is now explicitly stated as unimplemented -- the parent-process
   dump smoke test cannot validate that hypothesis because createdump dumps
   its own parent process, not an arbitrary remote target.

5. scripts/ci-parity.Tests.ps1: Get-DotnetTestProjectTokenCount now consumes
   the value following --diag as an option argument (via a $skipNext flag)
   instead of counting it as a positional target. New positive control
   tripwire accepts dotnet test --diag with a .sln-named diagnostic file
   and no target expects @(0).

6. scripts/diagnose-l12-createdump.Tests.ps1: the createdump assertion
   tightened to Start-Process -FilePath $createdump.FullName so
   declarations or log messages alone cannot satisfy the check.

7. tests/GanttCreator.Architecture.Tests/EnvVarGuardScanner.cs: the
   referenced-symbol parse now iterates each symbol individually (plus
   DEBUG) instead of defining all symbols in one parse, so every #elif
   branch becomes active in at least one parse and its literal/comment
   spans are masked.

Gates: Pester 31/31 PASS (11 diagnose-l12 + 20 ci-parity); PSSA clean on
all three modified PowerShell scripts; GanttCreator.Architecture.Tests
55/55 PASS (build 0 warnings/0 errors); git diff --check clean.

Checklist: A, B, C, G, I