# Status

> Short factual handoff for human reviewers and for restarting a Cline session.
> Update after every commit. Do not paste chat transcripts.
> Enforced: `scripts/check-status.ps1` runs in `verify-quick.ps1` and fails
> when any commit hash referenced here does not resolve, when a backticked
> repo path claimed here does not exist, or when a roadmap ID here is absent
> from `docs/03-ROADMAP.md`.

- **R3.4/R3.5 prerequisite contracts (current branch; docs only)** — Recorded ADR-0012 (Core-derived effective `StackIndex`), ADR-0013 (`SceneOwnerId` chart-level ownership), and ADR-0014 (`ChartTitle="Gantt Chart"`, ShowTitle-only band visibility, schema-v2 `PeriodLabelFormat` compatibility). Updated entity guide, architecture, checklist, R2.8/R3.4/R3.5/R3.9/R5.3/R5.4 plans, roadmap/manifest, and added the downstream R5.6b title-settings work item. No production code or schema values changed in this documentation-only step; targeted tests not run yet. Checklist: G, I.
- **R3.1 — Geometry value objects (commit `b5314c7`)** — Added `SizeD`, `RectD`, `ColourHex`, and `GeometryMath` beside the existing `PointD`, with exact value equality, epsilon geometry predicates, strict RGB/ARGB parsing, guarded non-finite/negative extents, and a single display-precision snap boundary. Added 40 Core tests covering equality, boundaries, invalid-number rejection, colour round-trips, and culture invariance. Observed gates: Core 486/486 PASS; Architecture 78/78 PASS; Release solution build 0 warnings/0 errors; production diagnostics clean; `verify-quick.ps1` PASS after the separate R2.8 gate fix. No Office gate applies. Checklist: A, B, G, I. Next: R3.2, landed as `e244d3c`.
- **R3.2 — Immutable Core scene model (commit `e244d3c`)** — Added the Core `Scene/` vocabulary: exact z-layer and activity-subtype priority contracts; immutable rectangle, line, ordered polygon, text, and group primitives with stable IDs and resolved-style data; `SceneWarning`; typed `GanttScene.TryCreate` refusals and deterministic ordering; and fixed compact strict `SceneSnapshot` JSON round-trip/determinism. Added 19 scene tests covering primitive guards, z-order, groups, refusals, unknown fields, non-finite/negative input, and three shuffled serializations. Observed gates: Core 505/505 PASS; Architecture 78/78 PASS; Release solution build 0 warnings/0 errors; production format PASS; new production/test diagnostics clean; `verify-quick.ps1` PASS 12/12. No Office gate applies. Checklist: B, E, G, I. Next: R3.3.
- **R3.3 — Deterministic TimeScale (commit `e8a77e8`)** — Added the Core `TimeScale` and typed creation outcome. `DateToX` is culture-free and monotonic; inclusive activity duration is `Finish.DayNumber - Start.DayNumber + 1`; activity width is duration-derived with `Start` fixed and `Finish` never incremented; point events map only from their date. Added 20 TimeScale tests covering creation refusals, one-day plots, leap/year boundaries, culture invariance, duration, activity geometry, point events, and monotonic mapping. Observed gates: Core 525/525 PASS; Architecture 78/78 PASS; Release solution build 0 warnings/0 errors; production format PASS; new diagnostics clean; `verify-quick.ps1` PASS 12/12. No Office gate applies. Checklist: A, B, F, G, I. Next: R3.4.


- **Review hardening (current branch)** — Verified and fixed four review findings: `ColourHex` equality/hash now include explicit-alpha representation while `ToString` preserves round-trip form; `SceneSnapshot.Deserialize` now maps document-validation failures to `InvalidDataException` while parser failures remain `JsonException`, and validates style hatch/alignment values; `ParseColour` uses `TryParse`; middle-row Excel insertion uses the adjacent `bodyIndex + 1` position. Added focused Core/Office contract and live-row regression assertions. Observed: Core 531/531 PASS; Office contract 188/188 PASS; `verify-quick.ps1` PASS 11/11 in 174.2s; `git diff --check` PASS; production diagnostics clean. `verify-office.ps1` was started but exceeded the 300-second tool window without a final result, so Office integration is not claimed. `verify.ps1` remains pending because it requires a clean committed tree. Checklist: A, B, C, F, G, I, J.
- **R2.2 review corrections** — Corrected the Initialise-sheet table/protection refusal messages to describe workbook-wide table detection and both worksheet/workbook-structure protection, with focused AddIn assertions. Added the adopted-workbook total sheet-count assertion. Updated `OfficeFixture` to close tracked workbooks without saving and release their RCWs in `finally` before sweeping only workbooks still open. Corrected the LLM checklist numbering and R2.2 Ribbon-icon exclusion wording. Observed: AddIn tests 191/191 PASS; Office integration Debug and Release builds 0 warnings/0 errors; live targeted two-workbook teardown and adopt-path tests PASS; Markdown links PASS; production-only format PASS. Full `verify-office.ps1` was blocked before tests by an existing packed-XLL file lock. `verify-quick.ps1` passed hygiene, skill sync, status, secret scan, locked restore, and production format, then stopped at the Release build because the same packed-XLL output was locked. Checklist: I, J.




- **R2.10 — Safe configuration and identity repair (commit `23de864`)** — Added ADR-0011 and the pure Core integrity finding/plan model with deterministic auto-repair/confirm/refuse classification, plus Office checker/repairer ports and guarded adapters for configuration visibility, plot-anchor, and visible row-identity repair. Added the explicit Repair configuration AddIn command/Ribbon callback, count-only confirmation/summary flow, and the Initialise ConfigSheetExists pointer. Added Core, Office contract, AddIn, architecture, and live Office integration tests. Observed gates: Core 434/434 PASS; Office contract 182/182 PASS; Architecture 78/78 PASS; AddIn 182/182 PASS with `-p:RunExcelDnaPack=false`; complete direct Office integration assembly 31/31 PASS; production format PASS; status/link checks PASS. Human Visual Studio/F5 evidence remains required before R2.10 is marked complete. The normal `verify-office.ps1` wrapper was blocked before tests by the loaded packed-XLL lock, and `verify-quick.ps1`/`verify.ps1` were not run to completion. Checklist: C, D, F, G, I, J, K.

- **R2.8 blank-row correction (commit `75df937`)** — Reproduced the first-add defect in live Excel: the initial table had one physical blank row while `ListRows.Count` was zero, and `ListRows.Add()` created a populated row plus another blank row. `ExcelGanttRowInserter` now reuses the final physical row only in the exact initial empty/blank state; non-empty tables retain the append path, and TypeOptions refusal deletes appended rows or clears reused blanks. Office contract `GanttRowInserterTests`: 9/9 PASS. Live `AddRowIntegrationTests`: 3/3 PASS, including one-row first insert and no trailing blank row after three commands. Pre-commit quick gates passed; full repository gates remain pending. Checklist: C, F, G, I, J.

- **R2.8 active-row insertion follow-up (working tree; pending commit)** — Updated the approved insertion policy: active cells inside `tblGanttData` insert below their row; header, last-row, and outside-table cases use the documented positions/fallback; initial blank-row reuse remains intact. Office contract `GanttRowInserterTests`: 13/13 PASS. Live `AddRowIntegrationTests`: 4/4 PASS, including middle-row insertion and following-row shift. Human F5 visual interaction is confirmed; see [`docs/evidence/r2.8-active-row-f5.md`](evidence/r2.8-active-row-f5.md). Production format, Office/contract/integration builds, `git diff --check`, status checker, markdown links, and diagnostics: PASS. Full repository gates and commit remain pending. Checklist: C, F, G, I, J.


- **Review hardening — committed in `bae844c`** — Added explicit `ParentCycle` validation for Critical Interval self-references and longer parent cycles, including a dependent-child `ParentInvalid` regression; strengthened the Turkish-culture validator test with an accepted `Inside` label; and centralized Office `Value2` matrix normalization for one-based and zero-based payloads. Core: 409/409 PASS; Office contract: 163/163 PASS; Architecture: 78/78 PASS; Release solution build: 0 warnings/0 errors; direct production format check: PASS. `verify-quick.ps1` was attempted twice after the documentation working-tree gate workaround and stopped at the format gate before completion; the direct equivalent `dotnet format GanttCreator.slnx --verify-no-changes --no-restore --exclude tests` then passed. `verify-office.ps1` ran but stopped at the known `OfficeFixtureTests.CreateWorkbook_returns_a_non_null_workbook_and_leaves_no_orphan` assertion (Office 16.0.20326.20158 x64); no retry was made under the bounded retry rule. A subsequent full solution build was blocked by the Office harness leaving `testhost.exe` and the packed XLL locked, not by compilation or analyzer diagnostics. `RollingLog` concurrency remains fixed by the existing `1d95d2a` change and its regression test. A later Office retry passed 22/23; its only failure was the R2.8 live test’s hard-coded body-index arithmetic, corrected to return and verify the actual `ListRow.Index` with increasing indices. Final Office gate: 29/29 PASS on Office 16.0.20326.20158 x64. `verify-quick.ps1`: PASS (11/11) on the current working tree. `verify.ps1`: PASS (14/14, 100% measured line/branch coverage, vulnerability scan clean) on clean committed `bae844c`; the uncommitted `ListRow.Index` follow-up is not yet covered by the clean-tree full gate. Checklist: C, F, G, I, J, K.

- **CI status-artifact portability fix (working tree; not yet committed)** — Removed generated artifact-path claims from STATUS/work-item evidence, added an explicit generated-artifact rejection to `check-status.ps1`, and added positive Pester coverage for generated file and directory tokens. Status checker PASS; Pester 12/12 PASS; full script tests 172/172 PASS; `verify-quick.ps1`: PASS (11/11). Checklist: G, I.

## Recently completed

- **R2.7a guard-adoption closure (commit `d2bb479`)** — `ExcelWorkbookInitialiser` and `ExcelConfigCatalogueWriter` now inject and query the shared `IWorksheetProtectionGuard` before workbook/sheet access and before mutation; target-specific configuration-sheet protection checks remain for the VeryHidden target. `ProtectionGuardFirstTests` pending-adoption set is empty. Office contract: 152/152 PASS; Architecture: 78/78 PASS; Release build: 0 warnings/0 errors; `verify-quick.ps1`: PASS (11/11). The required live Office gate was previously observed PASS at 23/23; the post-closure retry hit only the known OfficeFixture orphan assertion and was not retried again under the bounded retry rule. Checklist: C, F, G, I, J, K. Next: R2.8.

- **R2.7b — Named-style capability schema (commit `49abd4f`; ADR-0009)** — Appended `DefaultLabelPosition`, `AllowedLabelPositions`, and `ColourCapability` to `tblGanttStyles` (14 columns total), projecting built-in values from `EntityTypeDefinition` and validating explicit user-style capabilities. Core: 398/398 PASS; Office contract: 148/148 PASS; Release build: 0 warnings/0 errors; `verify-office.ps1`: PASS on Microsoft 365 x64, including live table materialisation, reader, save/reopen, and user-style preservation; `verify-quick.ps1`: PASS (11/11). Checklist: C, D, F, G, I, J, K. Next: R2.8.

- **R2.5a — Validation normalization and parent validity (commit `f21a908`)** — Added state-aware validation codes `CellContainsExcelError`, `CellValueUnsupported`, and `ParentInvalid`; relevant Excel errors and unsupported values block once, irrelevant populated/error cells warn once and normalize to null, and Critical Interval parents require a valid mapped span event. Added a future `Core/Scene` architecture guard. Core: 391/391 PASS; Architecture: 78/78 PASS; Office contract: 139/139 PASS; Release build: 0 warnings/0 errors; `verify-quick.ps1`: PASS (11/11). Checklist: C, F, G, I. R2.6a is also landed in commit `bf9727c`.


- **R2.7a risk closure — guard-first enforcement, live refusal evidence, L15/L16 (commit `4ffc3ad`)** — Closes the risks recorded against `4c6c648` with enforced behaviour rather than notes. New `tests/GanttCreator.Architecture.Tests/ProtectionGuardFirstTests.cs` (12 tests: 4 rules + 8 positive controls) classifies every `src/GanttCreator.Office/*.cs` file, fails on an unregistered data-mutating adapter, requires a protection read before the first data mutation in each registered adapter and inside its entry method, and requires `docs/KNOWN-LIMITATIONS.md` L16 to name every adapter that has not adopted the guard port — so R2.8 cannot add a mutating adapter without the guard. Observed RED against the real sources: a `.Value2` write prepended to `Initialise` produced "mutates workbook content before its first protection read" (file restored byte-identical, SHA-256 `C7CADF…35A`), and a temporary extra Office source file (RedProbe.cs) containing `ListRows.Add()` produced "mutates workbook content but is not registered as a mutating adapter" (probe deleted). `tests/GanttCreator.Office.IntegrationTests/InitialiseSheetIntegrationTests.cs` gains two live refusal tests (real `Worksheet.Protect()` → guard `SheetProtected`; Initialise → typed `TargetProtected`, both zero-mutation, 5/5 PASS live), which also exercises the `_Worksheet` seam against real COM instead of Moq only. `ExcelWorksheetProtectionGuard.cs` loses its `IDE0046` suppression (ternary form, no `NoWarn` change); `WorksheetProtectionGuardTests.cs` uses the concrete local type (CA1859). Ledger: L15 (no-undo consequence of ADR-0008 D2/D3), L16 (guard-port adoption debt, test-enforced). Docs: `docs/work-items/R2.7a-destructive-command-policy.md` implementation record (live gate with verdict history, verified `Five_cycles` mechanism and fixes, observed-RED evidence, recorded gaps incl. the note-writing `ExcelGanttValidationReporter` gap left for its own decision), `docs/work-items/R2.8-add-row-commands.md` guard-port mandate + registry step + stop condition, `docs/REPO-MAP.md`. Gates: build Release `-warnaserror` 0 warnings; `dotnet format --verify-no-changes` exit 0; non-Office suite 751 PASS (Core 347, Office.Contract 127, Arch 77, AddIn 156, Raster 41, Office.Integration non-tagged 3); Pester 168/168; actionlint PASS; PSSA exit 0; `check-md-links` 134 files; `check-status` OK; pre-commit PASS twice. Live: `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 600` **PASS — 23/23** (final run 2026-09-23 22:26:49 → 22:32, O365HomePremRetail 16.0.20326.20158 x64, archived trx the local archived Office TRX (generated, ignored, and not committed)) after three fixes: `1d95d2a` (bidirectional share-latch — all three `RollingLog` writer sites and the log reader now open `FileShare.ReadWrite`; verified cause of the earlier 22/23 `Five_cycles` failure, replacing the disproven "pending delete" theory), `7df1761` (per-run trx archiving so each attempt stays attributable), `64bff34` (XLL five-cycle orphan poll 15 s → 120 s after the 22:07 archived attempt showed all five owned processes exiting cleanly but slower). Correction: the original claim in this entry that `verify-office` reported PASS on 2026-09-23 was **false** — the 18:39 run ended `FAIL: OfficeIntegration tests exited 1`; corrected here and in the work item, which carries the full attempt history. Prerequisite commit `4f300cc` committed the previously untracked `docs/GanttCreator_R2_Implementation_Findings.md` brief unchanged (it blocked every commit through the untracked-docs gate). Next: R2.8 wires the guard port into the first mutating adapter; the confirmed destructive live run stays at R5.8 per ADR-0008 D6. Checklist: G, H, I, J, K.

- **R2.7a — destructive-command policy + protected-sheet guard (commit `4c6c648`)** — Core `src/GanttCreator.Core/DestructiveCommandPolicy.cs` (ADR-0008 D3/D2: three `CommandClass` values all classify `DestructiveButConfirmed`; count-only confirmation-text builder with the exact no-undo line `This change cannot be undone.`, invariant culture, null/input guards) with 22 Core tests in `tests/GanttCreator.Core.Tests/DestructiveCommandPolicyTests.cs` (truth table, exact-string pins, tr-TR invariance, guard positive tests); Office `src/GanttCreator.Office/IWorksheetProtectionGuard.cs` + `ProtectionGuardOutcome.cs` (four typed outcomes: `NotProtected`/`SheetProtected`/`WorkbookStructureProtected`/`NoActiveWorkbook`) + `ExcelWorksheetProtectionGuard.cs` (read-only, one-proxy-per-local, structure-check-before-sheet, `_Worksheet` sheet seam so Moq proxies satisfy it) with contract tests in `tests/GanttCreator.Office.ContractTests/WorksheetProtectionGuardTests.cs` (four-corner positive tests, both seam-wiring tests with both flags set explicitly, structure-first short-circuit + zero-mutation assertion, COMException-mapping decision recorded); `docs/REPO-MAP.md` Office row. Gates: Office 127/127 PASS, Core 347/347 PASS, Arch 65/65 PASS, AddIn 156/156 PASS; `git diff --check` clean; pre-commit PASS (skill-tree, status, links). `verify-quick.ps1` blocked by the dirty-tree skill-sync gate until this commit; `verify-office.ps1` (live `TargetProtected` refusal re-exercise) still **Required** — then believed blocked by orphan `EXCEL.EXE` strays holding the rolling-log handle (that remediation note is superseded: the verified cause was the `RollingLog` share latch, fixed in `1d95d2a`/`64bff34`, final run 23/23 PASS — see the entry above); live confirmed-destructive deferred to R5.8 per D6. Checklist: G, I, J, K.

- **Implementation-guide authoring — PROGRAMME COMPLETE: all 80 guides (Tier-A batch 2 + Tier-B batches 3–9), final commit `a45203b`** — The approved two-tier plan is fully executed: every remaining roadmap row (R2.7 through R10.8) now has an individual implementation guide in `docs/work-items/`, indexed in `docs/work-items/IMPLEMENTATION-PLAN.md` (80/80 `Authored`, zero pending, unique IDs, all files verified on disk). Tier A (19 guides, prescriptive against source at `8d53d39`): the Phase 2 remainder (`R2.7` config catalogues incl. the exact 23+21+6 token enumeration and ADR-0007 stop-point; `R2.7a` destructive-command policy with ADR-0008 and the protection guard; `R2.8` three add-row commands; `R2.9` TypeOptions dropdown + the R2.5-U4 style-registry gates; `R2.10` ADR-0009 safe-repair boundary closing L5) and all 14 Phase 3 scene-engine guides (value objects through the equivalence thin slice, mutation harness behind D-G3, and the L4 re-dating obligation). Tier B (61 guides, binding contract, upgraded to Tier A at each phase exit): all of Phase 4 (live renderer), Phase 5 (authoring workflow incl. the R5.9 scope-check stop-point), Phase 6 (**R6.7 spike first**, then composition/clipboard), Phase 7 (PowerPoint transfer — new dependency decision **D-G6**), Phase 8 (300-DPI PNG incl. **R8.2a** font ADR / D-G4), Phase 9 (resilience/accessibility/migration incl. the offline audit) and Phase 10 (packaging/release candidate). Every phase-exit row carries the manifest's just-in-time guide-upgrade obligation; the decision register now holds **D-G1…D-G6** awaiting human input. Per-batch slug corrections: Phases 4–10's first-draft filenames were pre-revision-4 guesses and every affected section was retitled/renamed against the actual roadmap rows. Gates: python link-check equivalent — 131 markdown files, all relative links resolve; `git diff --check` clean every commit; **CI green on every push** on this branch (`ci` runs 3m26s–4m38s; one `check-status` violation was caught by CI, diagnosed, fixed in `6628a60`, and the local pre-commit check now encodes the exact `scripts/check-status.ps1` rule); `pwsh ./scripts/verify-quick.ps1` **Not run locally** (authoring sandbox has no pwsh/dotnet) — CI is the authoritative gate. **No production code was changed by this programme.** Next: the guides are ready for implementation — the first action is the human's decisions on D-G1/D-G2 (needed before R2.7/R2.7a implementation). Checklist: I.


- **Implementation-guide authoring — batch 8 COMPLETE: Phase-9 Tier-B guides (commit `5d237f2`)** — All eight resilience/accessibility/migration guides; the manifest's Phase-9 section was retitled and its slugs corrected (the earlier guesses were pre-revision-4 rows that no longer exist). Highlights: R9.1 cancellation/deadline checkpoints for long non-COM renders (named checkpoints, deadline diagnostics, no partial output, host responsiveness — STA reality respected). R9.2 residual: structured diagnostics in real flows with operation-ID correlation and redaction verified against **real-shaped** records (the R0.8 unit-level gap), rotation verified not re-specified. R9.3 consumes R2.10 end to end with data-intact assertions on every path and no policy re-specification. R9.4 shape-ownership repair (idempotence pin, untouchable unowned sentinels, missing marks report-only, deterministic duplicate survivor). R9.5 accessibility extends R5.9 to the error surfaces + keyboard/high-contrast/screen-reader reviews (the R5.9/R9.5 boundary recorded as a stop-point; host capability limits documented, never faked). R9.6 the documentation-first legacy mapping (manual recreation, no implied CPM semantics, synthetic-only example, mechanically validated). R9.7 the consolidated crash-recovery matrix across every export stage (committed stage×failure cell table; host-specific repeats per supported build; gaps route back to their owning row). R9.8 the offline audit (a standing network-API reference guard with a positive control, package network-behaviour inspection, the disconnected-machine acceptance run) plus the Phase-9 exit assembly and the Phase-10 guide upgrade. Gates: python link-check equivalent — 123 files, all resolve; manifest Authored rows (72) exist; 8 rows pending (Phase 10); STATUS passes the exact check-status rule; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates. Next: Batch 9 — Phase 10 (8 Tier-B guides) — the final authoring batch. Checklist: I.

- **Implementation-guide authoring — batch 7 COMPLETE: Phase-8 Tier-B guides (commit `a311c94`)** — All nine 300-DPI PNG guides; the manifest's Phase-8 section was retitled and its slugs corrected (the earlier guesses described pre-revision-4 rows that no longer exist). Highlights: R8.1 is a residual row (verify the landed `ExportSize`, add the preview-feed pin, link acceptance to the existing tests — no second parser). R8.2 the SkiaSharp primitive renderer in Raster with exact-pixel region tests, deterministic re-render, and the native-asset packaging flag passed to R9.5. **R8.2a** (ADR, D-G4) settles installed-vs-bundled-vs-fallback with licence evidence, a font-presence probe reporting the exact resolved font, and the never-silent-substitution policy; it precedes R8.3 per the roadmap note. R8.3 deterministic text (pinned font, scene-resolved bounds, no re-measure) plus hatch patterns. R8.4 representative renders at approved widths with human-approved metadata-pinned goldens — closing L4's replacement gate. R8.5 byte-level `pHYs`/`IHDR`/chunk/CRC pins (11811 px/m, metre unit; encoder behaviour probed first). R8.6 the atomic temp-then-replace writer with a post-write validator (no partial file; distinct failures; the unwritable-path matrix row). R8.7 width/unit controls with the preview delegated to `ExportSize` (equality pin). R8.8 the Export PNG command with an independent re-read of dims/density/crop/aspect and an edge-pixel no-whitespace proof, plus the Phase-8 exit assembly (all-unit demonstration, matrix subset, Phase-9 guide upgrade). Gates: python link-check equivalent — 115 files, all resolve; manifest Authored rows (64) exist; 16 rows pending (Phases 9–10); STATUS passes the exact check-status rule; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates. Next: Batch 8 — Phase 9 (8 Tier-B guides). Checklist: I.

- **Implementation-guide authoring — batch 6 COMPLETE: Phase-7 Tier-B guides (commit `1f18cd2`)** — All seven direct-PowerPoint-transfer guides; the manifest's Phase-7 section was retitled and its slugs corrected (the earlier guesses were PNG-shaped and belonged to Phase 8). Highlights: R7.1 adapter ports with explicit proxy-ownership rules **and a new decision-register entry D-G6** (PowerPoint interop package/version pin — a new production dependency requiring human approval before first install). R7.2 the attach/start + active-slide typed decision matrix under a user-visible policy (its create-new-presentation stop-point feeds R7.6's button contract). R7.3 `PasteSpecial(ppPasteShape)` with `ShapeRange` validation — null/empty/zero-bound rejected, non-throwing is not success. R7.4 pure Core fit math applied once with ratio preserved and a recorded margin policy (4:3/16:9/custom). R7.5 four distinct typed failures with evidence-filtered bounded retry (no sleeps) and cleanup on every path. R7.6 the Send to PowerPoint command with ownership assertions as tests (no save/close/terminate). R7.7 the 25-transfer soak with counters, timings, zero residue per iteration under the verify-office ownership sweep, plus the Phase-7 exit assembly and Phase-8 guide upgrade. Gates: python link-check equivalent — 106 files, all resolve; manifest Authored rows (55) exist; 25 rows pending (Phases 8–10); STATUS passes the exact check-status rule; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates. Next: Batch 7 — Phase 8 (9 Tier-B guides). Checklist: I.

- **Implementation-guide authoring — batch 5 COMPLETE: Phase-6 Tier-B guides (commit `f0c0f28`)** — All eight editable-composition guides with corrected manifest slugs; correct remaining count after this batch is 32 (Phases 7–10; the previous entry's "41" was off by one). Highlights: **R6.7** is a spike protocol running first — the copy-after-delete/delayed-rendering evidence matrix per supported Excel/PowerPoint build, with the ADR outcome (simple delete vs the short-lived staging-workbook fallback) approved before R6.5/R6.6; it also records the stale "Phase 10 contains a compatibility spike" sentence in the architecture doc as drift (roadmap revision 4 governs sequencing). R6.1 export-selection bounds (chart-only vs table-plus-chart); R6.2 cell-free table composition from the R3.11 primitives with zero-write positives; R6.3 full-scene staging at the deterministic origin with bounds-equality and view discipline; R6.4 grouping with membership, deterministic child order, and an editability proof; R6.5 the birth-registry staging lifecycle with a per-failure-point zero-residue matrix; R6.6 `ShapeRange.Copy` with verified Office drawing formats before success (never a non-throwing call; clipboard-unavailable typed); R6.8 the one-click Copy Editable command with count-only verified-success messaging, owning the Phase-6 exit assembly (manual-paste demonstration, slide-size/clipboard/PowerPoint-failure matrix subset, Phase-7 guide upgrade). Gates: python link-check equivalent — 99 files, all resolve; manifest Authored rows (48) exist; STATUS passes the exact check-status rule encoded from `scripts/check-status.ps1`; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates. Next: Batch 6 — Phase 7 (7 Tier-B guides). Checklist: I.

- **Implementation-guide authoring — batch 4 COMPLETE: Phase-5 Tier-B guides R5.1–R5.11 (commit `6f86525`)** — All eleven authoring-workflow contract guides, with corrected manifest slugs matching the actual rows. Highlights: R5.1 discharges the R2.5 D12/U11 plot-range deferral (automatic/explicit modes + user-facing warnings, Ribbon state follows mode); R5.2 scale/page-width choices with per-choice geometry snapshots (page-width semantics flagged for the Tier-A live probe); R5.3 parent/child commands + stable lane reassignment (IDs never rewritten; splitter parenting is display-level, separate from critical-interval `ParentId`); R5.4 move/expand/collapse (order-key writes; collapse = scene exclusion; row/shape alignment invariant; persisted-collapse stop-point); R5.5 label controls projected from the catalogue (R5.10 hosts their gating — split recorded both sides); R5.6 style/theme dialogs (explicit local defaults, range validation, contrast warning, never silent substitution); R5.7 the delineator workflow extending R2.8 — its delete gate discharges R2.7a D6 as the first live confirmed destructive command; R5.8 scene-level warnings panel complementing (never duplicating) R2.6 with a no-note-writes contract test; R5.9 ribbon completion carrying the roadmap scope-check STOP-point (show/hide columns + `StyleKey`/`LabelPosition` in-cell validation need product-owner decisions — the manifest's flagged drift, confirmed); R5.10 the architecture's per-entity selection/override paragraph implemented literally (truth table, capability gating, blank-inherits, persistence); R5.11 the refresh-only enforcement suite (zero-invocation proof via a counting seam; blocking-Refresh preservation) + Phase-5 exit assembly + Phase-6 guide upgrade. Gates: python link-check equivalent — 91 files, all resolve; manifest Authored rows (40) exist; STATUS repo-path tokens all pass the exact check-status rule (read from `scripts/check-status.ps1` after the earlier fix); `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates. Remaining: Batches 5–9 — Phases 6–10 (41 Tier-B guides). Checklist: I.

- **Implementation-guide authoring — batch 3 COMPLETE: Phase-4 Tier-B guides R4.1–R4.10 (commit `07cb337`)** — All ten live-renderer contract guides. Residual rows handled per the roadmap notes (R4.1 adds only the two write-side ports beside the four landed ones; R4.9 wires Refresh — Validate landed in R2.6). Highlights: R4.2 the single state-scope type (six properties, capture-on-change, failure injection at each point); R4.3 snap-once geometry with name=scene-ID + `GanttCreator.` tag and a no-recalculation contract test; R4.4 text consumes resolved bounds (no re-measure, no side change; font-presence evidence feeds R8.2a/D-G4); R4.5 freeform diamonds + one-pass deterministic z-order (no opportunistic `BringToFront`); R4.6 the complete token→Office-property matrix incl. hatch with live comparison against the entity-guide tables; R4.7 idempotent refresh by stable ID per the architecture's seven steps (update-first, atomic fallback only with benchmark evidence); R4.8 sentinel preservation matrix (unowned shapes/cells/formats/active-sheet, failure paths); R4.10 dual benchmark (1,000-event refresh + 1,000-row validate/report) with host builds, no-regression threshold policy, phase-exit evidence assembly (zoom/scale/1,000/protected subset + Phase-5 guide upgrade). Manifest Phase-4 slugs corrected to match the actual roadmap rows (R4.5 polygons/z-order, R4.6 style mapping, R4.7 idempotence, R4.8 preservation, R4.9 refresh command, R4.10 benchmark). Gates: python link-check equivalent — 80 files, all resolve; manifest Authored rows (29) exist; 52 rows pending (Phases 5–10); `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates (all prior pushes green). Next: Batch 4 — Phase 5 (11 Tier-B guides). Checklist: I.

- **Implementation-guide authoring — batch 2 COMPLETE: all Phase-3 Tier-A guides R3.11–R3.14 (commit `030f0ac`)** — Finishes the 19 Tier-A guides (Phase 2 remainder + Phase 3). NEW: `docs/work-items/R3.11-table-header-primitives.md` (§3/§4 export-ready panel/header primitives from a measured `PanelCellGrid` input — 14 schema display names, no invented `Duration` column per the Phase-3 note, `Description` alignment flagged; §23 date labels homed here as D1 with a stop-point), `docs/work-items/R3.12-scene-validator-benchmark.md` (`SceneValidator` invariants with per-invariant positives; deterministic `ReferenceSceneFixture` + a golden scene-snapshot baseline created when R3.12 is implemented (see that guide); Stopwatch benchmark harness — recorded evidence on the reference machine, no new dependency, no CI timing gate), `docs/work-items/R3.13-mutation-testing.md` (R0.5 deferral — pinned tool behind decision D-G3 (Stryker.NET recommended), `verify.ps1` + `ci.yml` parity step, Pester guard with positives incl. the no-`-EnableExit` L10 lesson, archived baseline, 80% changed-code threshold from the next Core change), `docs/work-items/R3.14-equivalence-thin-slice.md` (bar/external-label/diamond field assertions transcribed from the equivalence table, missing-field positive, spike+ADR gap path, Phase-3 exit evidence assembly, and the Phase-4 Tier-B→Tier-A upgrade obligation). Gates: python link-check equivalent — 70 files, all relative links resolve; manifest Authored rows (19) exist on disk; 62 Tier-B rows pending; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** (no pwsh/dotnet in sandbox) — CI runs the gates on every push (all prior pushes on `cline/vd5dq9wz`: green). Next: Batches 3–9 — the 61 Tier-B contract guides for Phases 4–10. Checklist: I.

- **Implementation-guide authoring — batch 2 continued: R3.5–R3.10 (commit `66192a1`)** — Six entity-layout Tier-A guides. NEW: `docs/work-items/R3.5-plot-frame-bands.md` (§1/§2/§5–§8 builder — ChartBounds union + `ChartOuterPaddingPt`, one `BandSequence` feeding year/period/alternating/grid families, exact z-layers 0/10/20/80/90; entity-section split vs R3.11 recorded; blank-title band policy flagged for product confirmation), `docs/work-items/R3.6-span-bar-label-layout.md` (§12 bars + §22 `LabelPlanner` with the exact candidate/acceptance/priority contract + the architecture's single `ITextMetrics` port with a pinned deterministic fake; clip warnings per the coordinate rules; ellipsis-at-`MaximumExternalLabelWidthPt` with wrap explicitly undefined — stop-point), `docs/work-items/R3.7-milestone-marker-layout.md` (§20/§21 diamonds as four-point `ScenePolygon`s, Start-only centre, edge-date and same-date determinism), `docs/work-items/R3.8-critical-interval-overlay.md` (§16 overlays clipped to plot AND the post-clip visible parent span, one warning per child, disjoint/adjacent/overlapping), `docs/work-items/R3.9-multi-event-stack-lanes.md` (`LaneEventLayout` single feed; §13 planned/actual same-lane rules — never shortened/moved/hidden), `docs/work-items/R3.10-delineator-lines-labels.md` (§24 verbatim — corner labels with Auto order, same-date style dedup, `StackGapPt` label stacking, layer 25/75). Gates: python link-check equivalent — 66 files, all resolve; manifest Authored rows (15) exist on disk; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** — CI runs the gates (previous pushes on this branch: green). Remaining in batch 2: R3.11–R3.14. Checklist: I.

- **Implementation-guide authoring — batch 2 partial: Phase-3 foundations R3.1–R3.4 (commit `a2152f5`)** — First four Tier-A guides of the Core scene engine. NEW: `docs/work-items/R3.1-geometry-value-objects.md` (SizeD/RectD/ColourHex/GeometryMath beside the landed `PointD`; exact equality vs epsilon predicates in two homes; one snap boundary `SnapToDisplayPrecision`; pixel math stays in Raster), `docs/work-items/R3.2-scene-primitives.md` (`GanttCreator.Core.Scene` namespace; ScenePrimitive = rectangle/line/polygon/text/group exactly per the architecture's list; role-derived stable primitive IDs; `ZLayer` with the entity guide's exact 11 layers + layer-40 subtype priority; deterministic `SceneSnapshot` serialisation — the Phase-3 exit artifact), `docs/work-items/R3.3-time-scale.md` (typed creation; `DateOnly.DayNumber` day index; start-of-day `DateToX`; inclusive-finish right edge; picks up the R2.5 D12/U11 groundwork only — R5.1 owns modes/warnings), `docs/work-items/R3.4-lane-stack-geometry.md` (§9 slot/height/centre formulas verbatim; splitter/spacer fixed lanes; same-stack overlap warns and is never moved; shuffle-determinism theories without a new dependency). Two in-guide stop-points flagged for the human: R3.4 D2 lane-ordering key; R3.4 D5 property-testing approach (xUnit theories vs a framework). Gates: python link-check equivalent — 60 files, all relative links resolve; manifest Authored rows (9) exist on disk; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run locally** (no pwsh/dotnet in sandbox) — CI runs the gates on the pushed branch. Remaining in batch 2: R3.5–R3.14. Checklist: I.

- **Implementation-guide authoring — batch 1 complete: all five Phase-2-remainder Tier-A guides (commit `0b1ca6c`)** — Follows the batch-1 partial entry below. NEW Tier-A guides: `docs/work-items/R2.8-add-row-commands.md` (three Ribbon add-row commands with catalogue defaults + generated `GanttRowId` via injectable generator, append-at-body-end insertion (D2), guard-first order per R2.7a, no dialogs (R5.7 boundary) and no render; D1 canonical types = `As-Planned Activity`/`As-Planned Milestone`/`Delineator`, flagged for human confirmation), `docs/work-items/R2.9-type-dropdown-materialisation.md` (`GanttCreator.TypeOptions` defined name over `tblGanttTypes` display-name cells, `xlValidateList` on the `Type` body, R2.5-U4 deferral picked up via pure `GanttStyleRegistry` + `StyleKeyUnknown` code + Custom-Activity capability gates reusing existing codes, catalogue-hash gate before materialise), `docs/work-items/R2.10-config-repair-migration.md` (ADR-0009 safe-repair boundary auto/confirm/refuse closing KNOWN-LIMITATIONS L5; picks up the R2.2 plot-anchor and R2.3 identity-repair deferrals; nine-fixture damage matrix; Repair-configuration command with plan-then-act). Manifest `docs/work-items/IMPLEMENTATION-PLAN.md` index now shows all five rows `Authored (Tier A)`. Gates: python link-check equivalent — 56 files, all relative links resolve; manifest Authored rows exist on disk; `git diff --check` clean; **CI on this branch passed** for the batch-1 partial push (run 35634559244, `ci`, success, 3m26s — build/tests/Pester/link checks green). `pwsh ./scripts/verify-quick.ps1` **Not run locally** (sandbox has no pwsh/dotnet); CI runs the gates on every push. Next: Batch 2 — Phase 3 Tier-A guides (R3.1–R3.14). Checklist: I.

- **Implementation-guide authoring — batch 1 partial: manifest + R2.7/R2.7a Tier-A guides (commit `a3f5161`)** — Product owner approved the two-tier plan (2026-09-21): pre-create all 80 remaining roadmap work-item guides; Tier A (prescriptive: Phase 2 remainder + Phase 3) authored now, Tier B (binding contract: Phases 4–10) authored next, upgraded to Tier A at each phase exit just in time. NEW: `docs/work-items/IMPLEMENTATION-PLAN.md` (80-guide index; implementer contract; phase-exit obligations incl. Tier-B upgrades; decision register D-G1..D-G5; drift notes for KNOWN-LIMITATIONS L4 and the roadmap R5.9 note) + Tier-A guide `docs/work-items/R2.7-config-catalogues.md` (ADR-0007 stop-point D1–D3 incl. proposed `tblGanttSettings`; exact catalogue enumeration transcribed from entity guide revision 3 — 23 metric, 21 colour, 6 typography tokens with defaults/ranges; writer/reader ports mirroring the R2.2/R2.4 seam patterns) + Tier-A guide `docs/work-items/R2.7a-destructive-command-policy.md` (ADR-0008 stop-point: undo Option 1 no-undo-warned recommended vs Option 2 transactional undo; `IWorksheetProtectionGuard` extending R2.2's `TargetProtected`; live-destructive-exercise split with R5.8 recorded as D6). Docs-only; no production code changed. Gates: python link-check equivalent of `scripts/check-md-links.ps1` rules — 53 files scanned, all relative links resolve; manifest Authored rows verified on disk; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` **Not run** (authoring sandbox has no pwsh/dotnet — CI runs the gates on the pushed branch `cline/vd5dq9wz`). Batch 1 remaining: R2.8, R2.9, R2.10 guides. Checklist: I.

- **R2.6 — Row-level error reporting without mutating valid input** (commit `7ec36f3`, fix `ee51743`) — NEW: `src/GanttCreator.Office/IGanttValidationReporter.cs` (port, no COM in signatures), `GanttValidationReportOutcome.cs` (`Ok(notesWritten)/Refused(reason)`), `ExcelGanttValidationReporter.cs` (PIA implementation: discovers `tblGanttData`, builds column map mirroring the reader, clears prior add-in notes then writes one classic cell note per offending cell via `Range.AddComment`/`Comment.Text` with the `Start` arg omitted so an existing note is replaced, not duplicated; ownership tagged by text sentinel `"Gantt Creator validation:"` since `Comment.Author` is read-only; `SpecialCells(xlCellTypeComments)` guarded for empty-range COMException; `internal virtual` seams for `GetSheetAt`/`GetTableAt` (both `ListObjects` and `ListColumns` overloads)/`GetCellRange`/`EnumerateCells` so Moq contract tests use no Excel type names), `ValidationReportComposer.cs` (pure grouping of pre-sorted issues into `(RowNumber, Field, text)` note entries with sentinel prefix + severity-prefixed lines in `Severity → Row → Field → Code` order; `tr-TR` invariant; `NoteSentinelPrefix` internal). `src/GanttCreator.AddIn/ValidateSheetCommand.cs` (read → validate → report pipeline; `RunForExcel()` production entry via `ExcelDnaUtil.Application`; internal `Run(reader,reporter,presenter)` for tests; read refusals translate to one actionable message; successful pass shows one count-only summary — never schedule content; presenter failure degrades to no dialog); `RibbonControlIds.ValidateSheet`, `RibbonState.HasActiveWorkbook` gating, `GanttRibbon` callback through `CommandBoundary`, `Ribbon.xml` Data-group button with `Spelling` imageMso. Work item: `docs/work-items/R2.6-row-level-error-reporting.md`. Tests: `tests/GanttCreator.Office.ContractTests/ValidationReportComposerTests.cs` (pure ordering/grouping; sentinel-first-line; multiple issues on one cell collapse to one note; empty input → empty; `tr-TR`); `tests/GanttCreator.Office.ContractTests/ValidationReporterTests.cs` (fake-worksheet Moq mutation set — exactly the expected cells annotated and only those, valid cells provably untouched; error-order; ownership — user note preserved, stale owned note cleared; refusals no-active-workbook/table-missing mutate nothing; every internal seam/validator exercised); `tests/GanttCreator.AddIn.Tests/ValidateSheetCommandTests.cs` (success presents one summary + calls report once; each read-refusal surfaces one message; null-arg guards `ArgumentNullException`; presenter failure degrades without throwing); `tests/GanttCreator.AddIn.Tests/GanttRibbonTests.cs` + `RibbonStateServiceTests.cs` (button/callback/imageMso/state gating/refresh snapshot pinned); `tests/GanttCreator.AddIn.Tests/AddInAssemblyTests.cs` + `tests/GanttCreator.AddIn.Tests/AddInHostTests.cs` + `tests/GanttCreator.AddIn.Tests/AddInLifecycleTests.cs` (AddIn assembly + host + lifecycle unaffected — recorded for traceability). Architecture gates: `tests/GanttCreator.Architecture.Tests` 65/65 PASS (Core stays Office-free; AddIn references only Core + Office ports, no direct COM type naming). `dotnet format` 0 findings on `src/GanttCreator.Office/`, `src/GanttCreator.AddIn/`, `src/GanttCreator.Core/`; `scripts/probe-pia.ps1` PSScriptAnalyzer-clean (`$null` on left of comparison). `verify-quick.ps1` PASS (exit 0, all steps, 197.1 s). Office gate: **Required, live** — `tests/GanttCreator.Office.IntegrationTests/ValidationReporterIntegrationTests.cs` [Trait("Category","OfficeIntegration")]; live Excel asserts cell `Value2` matrices byte-identical before/after, notes exist on expected cells in composer order, user note preserved, stale owned note cleared; manual F5 record pending in work item Notes. Checklist: C, E(cell analog), F, G, I. Next: R2.7 (per-row fill/line/label overrides on the visible table; style-registry ready-check in R2.9).

- **R2.4 — Neutral row read (no locale display parsing)** (commits `402f305`, `4e4380f`, `4d75ae3`) — NEW: `src/GanttCreator.Core/GanttRowDto.cs` (immutable sealed record; `RowNumber int`; ID/LaneId/ParentId/StyleKey/Description/TypeText/LabelPositionText/FillColourText/StrokeColourText/SortOrder as trimmed `string?` (blank → `null`, Ordinal, no case-fold, no rewrite); `Start/Finish` as `DateOnly?`; `StackIndex` as `int?`; `Visible` as `bool?`); `src/GanttCreator.Core/ExcelCellConverter.cs` (pure `DateOnly`/scalar converter; `Value2` shape-only — never `.Text`; 1900 serial range guards incl. leap-bug pin via comment; `IsFinite` + range before cast; `TryConvertDate`/`TryConvertStackIndex`/`TryConvertVisible`/`ToText`; Office-free); `src/GanttCreator.Office/GanttTableReadOutcome.cs` (`Ok(rows)/Refused(reason)`), `IGanttTableReader.cs` (port, no COM in signatures), `ExcelGanttTableReader.cs` (bulk `DataBodyRange.Value2` read; `Date1904` check first; one proxy per local; `internal virtual` seams for `GetDate1904`/`GetSheetAt`/`GetTableAt`/`GetColumnAt`/`GetBodyValues`; rejects 1904 workbooks — ADR-0006). Work item: `docs/work-items/R2.4-neutral-row-read.md`; ADR: `docs/adr/0006-reject-1904-date-system.md`. Acceptance: Core serial/date/edge/non-finite/culture-invariance tests; Contract tests (Moq PIA: no-op app, no workbook, 1904 refusal, table-missing, required-header-missing, no-body, body-order conversion, header-name mapping not position, blank/error-cell null preservation + no-mutation `VerifySet`); Integration tests (GB `dd/MM/yyyy` vs US `MM/dd/yyyy` display formats produce identical DTOs; 1904 refusal) via `OfficeFixture`, `[Trait("Category","OfficeIntegration")]` excluded from `verify-quick`. Gates: Core 204/204 PASS (140 pre-existing + 64 new across `ExcelCellConverterTests` + `GanttRowDtoTests`); Contract 39/39 PASS (33 existing + 6 new reader); Architecture unchanged (Core has no Office/Excel-DNA/Skia/Clipboard/WinForms/UI dependency — Core boundary preserved). Office gate (Required): `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 600` PASS 2026-09-20 — Office `16.0.20326.20144` (O365HomePremRetail, x64, en-us), 16/16 OfficeIntegration PASS in 5 m 44 s; `verify-quick.ps1` PASS in 149.6 s (all 13 steps, exit 0). Live evidence recorded in the reader-test stdout (trx): Excel 16.0, and after the integration test cleared a one-row table body, two `ListRows.Add()` calls produced `ListRows.Count=3` with body `A2:N4` — the created rows landed at body offsets 1 and 3 with an untracked blank body row between them, so the test locates rows by the offset each returned `ListRow` actually occupies (commit `4e4380f`) instead of assuming adjacency, and asserts every non-written body row is wholly blank; the same `44931` serial displayed as GB `05/01/2023` and US `01/05/2023` while both reads returned identical `GanttRowDto`s (record equality). Recorded harness observation (R1.6 scope, not R2.4): the Office suite can leave `EXCEL.EXE` instances alive after the fixture's own teardown — 5 remained after this gate run while the fixture's owned-PID manifest listed 19 launched PIDs — and an immediately following `verify-quick.ps1` then failed its `build Release -warnaserror` step because ExcelDnaPack could not delete `GanttCreator.AddIn-AddIn64-packed.xll` (\"Perhaps loaded in Excel?\"); re-running after killing the strays passed. `verify-office.ps1` sweeps harness-owned Office processes only on its timeout path. Also recorded: the R1.6 five-cycle packed-XLL log-watch failed once on its 60 s poll bound (run 2: `RegisterXLL` returned true, XLL loaded) and passed at 90 s (run 3), so the poll bound is widened in `4d75ae3` with that evidence and the assertion left intact — a second failure at 90 s on an otherwise green build is a defect. Next: R2.5 (row → entity mapping with all-errors validation).

- **R2.3 — Stable row identity** (commit `1f6cb10`, 2026-09-19) — NEW: `src/GanttCreator.Core/GanttRowId.cs` (immutable value object: `G-` plus 32 lowercase hex digits, `New`/`Parse`/`TryParse` Ordinal on trimmed input, `TryParse` never throws; `Parse` throws `ArgumentNullException`/`FormatException`) plus `tests/GanttCreator.Core.Tests/GanttRowIdTests.cs` (generation, 1,000-ID uniqueness, Excel-text-safety, 100-value round-trip, `TryParse`/`Parse` validator positives, insert/sort/move/delete preservation, `tr-TR` invariance). Work item: `docs/work-items/R2.3-stable-ids.md` (decisions D1–D4; duplicate/missing-ID policy deferred to R2.5, repair to R2.10). Gates: Core 140/140 PASS; Architecture 65/65 PASS; `verify-quick.ps1` PASS in 156.4 s (all 12 steps); `dotnet format --exclude tests` clean; `git diff --check` clean; pre-commit hook PASS. Office gate (Required: sort and insert rows in Excel): **Not run** — manual F5 steps recorded in the work item; blocks marking R2.3 done. Next: R2.4.

- **R2.2 — button icon + F5 evidence** (2026-09-19) — human-operated F5 on the unpacked XLL (`GanttCreator.AddIn-AddIn64.xll`, two sessions) confirmed the adopt path live: add-in loaded with no exception and `EXCEL.EXE` exited 0; `GanttCreator.PlotAnchor` created at the expected location; `tblGanttData` created; target sheet labelled `Gantt Data`. A second initialise on an already-initialised workbook produced a dialog whose heading is the boundary's fixed `CommandErrorDialog` main instruction (`"Gantt Creator could not complete the last action"`); the heading alone therefore does **not** identify the path, because `InitialiseSheetCommand` presents typed refusals through that same dialog and the specific text is its body. Two candidates existed: the expected `ConfigSheetExists` refusal, or the boundary's exception translation (a defect). Evidence for the former: the rolling log carries the two `open` records and **no `CommandError:` record** — `CommandBoundary.WriteErrorRecord` writes on the exception path only, and the refusal path logs nothing by design (D3/D7). This assumes the boundary's log is wired by `AddInHost`; it does not, on its own, establish the dialog body text, which the operator should read off the dialog to close the question. Findings from the same run: the button rendered **no icon** (decision D8 had deferred this), so `Ribbon.xml` now carries `imageMso="TableInsertExcel"` — the Excel Insert-Table command, satisfying **both** verification bases exactly as the working `FileOpen`/`Help` do: it is a row in the Excel idMso table of `[MS-CUSTOMUI]-250218` (row `idMso=TableInsertExcel, control=button, label="Table"`) **and** present in the Office 2010 Icons Gallery (`Office2010IconsGallery.docx`, whose embedded `customUI14.xml` lists 7344 unique `imageMso` IDs). The gallery check is validated against the four documented outcomes that file reproduces rather than assumed — `Information` absent, `OfficeDiagnostics` present, `Help` present, `FileOpen` present — which is how the earlier `Information`/`OfficeDiagnostics` failures are now explained by inspection instead of by F5 trial. Note the id is `TableInsertExcel`, not its UI label `"Table"`, which is itself absent from the gallery. Pinned by `Ribbon_buttons_use_gallery_verified_imageMso_ids` (now asserting all three buttons). Evidence: `docs/work-items/R2.2-initialise-sheet.md` D8. **F5 glyph confirmation: PASS — human operator, 2026-09-19.** The Insert-Table glyph renders on the ribbon. D8 is closed. **F5 refusal-path confirmation: PASS — same session.** A second initialise on an already-initialised workbook shows the `ConfigSheetExists` body ("already contains Gantt Creator configuration … left it unchanged"), so the typed refusal fires and nothing is mutated; the rolling log carries the two `open` records and **no `CommandError:` record**, as D3/D7 predict for the refusal path. Gates: see the commit. Office gate (`verify-office.ps1`): still **Not run**. Next: R2.3.

- **R2.2 — Initialise Sheet** (commits `bb4386a`, `b1ffdd8`, `e4bfc34`, `4fe3203`, 2026-09-18) — the initialiser adopts the active blank worksheet as `tblGanttData`'s home when one is available; otherwise it creates the Gantt Data worksheet. A blank visible `Worksheet` that is the active sheet is renamed and populated in place (adopt path); a chart sheet, any non-empty worksheet, or the absence of an active worksheet causes a fresh worksheet to be created (create path). Typed refusals — NoActiveWorkbook, TableExists, ConfigSheetExists, TargetProtected — mutate nothing and surface one concise message per refusal through the presenter; presenter failure degrades. New: `GanttWorkbookContract` (`ConfigSheetName`, `GanttSheetLabel`, `PlotAnchorDefinedName`), `IWorkbookInitialiser` port + `ExcelWorkbookInitialiser` (read-only checks run before any mutation; the create path rolls back a just-added sheet when a protection re-check after creation would block later writes), `WorkbookInitialiseOutcome`/`WorkbookInitialisePath`/`InitialiseRefusalReason`, `InitialiseSheetCommand` (silent success, one actionable message per refusal, presenter failure degrades), `btnInitialiseSheet` in the Data group gated on the workbook fact. The shipped AddIn gate asserted "exactly two gated controls" against in-flight R2.2 wiring — the count literal is `2 → 3` and travels with the new control in this commit, plus a per-control "exactly one getEnabled" guard so a duplicated attribute cannot slip through. Decisions D1/D3/D4/D7A (Hybrid: stored defined name + derive-and-validate at render; adopt-or-create: blank active worksheet adopted, otherwise a new sheet created), D5 (typed refusals + positive refusal tests) recorded in `docs/work-items/R2.2-initialise-sheet.md`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 144/144 PASS (was 115; +29), `dotnet test tests/GanttCreator.Office.ContractTests` 30/30 PASS (was 17; +13), `dotnet test tests/GanttCreator.Core.Tests` 119/119 PASS, `dotnet test tests/GanttCreator.Architecture.Tests` 56/56 PASS; `pwsh ./scripts/verify-quick.ps1` PASS in 152.8s (all 12 steps); `dotnet format` clean; `git diff --check` clean; pre-commit hook PASS. Checklist: A, B, C, G, I. Office gate: the live-Excel path is **implemented and committed** (`tests/GanttCreator.Office.IntegrationTests/InitialiseSheetIntegrationTests.cs`, `OfficeIntegration` trait; `OfficeFixture.CreateWorkbook` + teardown sweep) — adopt/create/refusal against live Excel, asserting the 14-column table, sheet-scoped `GanttCreator.PlotAnchor` = `'Gantt Data'!$O$1`, one visible + one VeryHidden sheet, and no mutation on refusal. **Not yet executed** — requires the self-hosted runner (`pwsh ./scripts/verify-office.ps1`) plus a Visual Studio F5 session. Limitation: the create path requires an available visible worksheet slot in the active workbook; a workbook whose only worksheets are chart sheets or protected sheets cannot be initialised on the create path and is refused. Next: R2.3.

- **fix: reused-PID flag in `Test-HarnessProcessTreeActive` was scoped to the last matching process, not the candidate** — in `scripts/diagnose-l12-createdump.ps1` the `$isReusedPid` exclusion flag was set inside the `foreach ($process in $matching)` loop but read by the parentage-expansion guard *outside* it, so the guard reflected only the last matching process of the previous candidate. Two failure modes: (a) once any candidate set the flag `$true` it was never cleared, so a later unrelated candidate inherited it and its children were skipped from the traversal even though they belong to an exited owned parent that must still be walked; (b) for a candidate PID with multiple live instances (a reused PID) the guard used whichever process happened to be last, not the candidate as a whole. Introduced a per-candidate `$candidateIsReusedPid`, reset to `$false` at the top of each candidate iteration (after the visited-Pid check, before the matching-process loop); the reused-PID branch sets it to `$true` rather than merely `continue`-ing, and the guard reads that per-candidate flag, so descendants of exited owned parents are traversed regardless of what a previous candidate did. New Pester regression test in `scripts/diagnose-l12-createdump.Tests.ps1` (per-candidate reset position, guard reads per-candidate flag, old loop-scoped `$isReusedPid` no longer the guard's input, reused-PID branch sets the flag). Gates: `Parser::ParseFile` 0 diagnostics on both files; PSSA (Error/Warning) clean on both; `scripts/test-scripts.ps1` 164/164 PASS; `git diff --check` clean. Checklist: G. Uncommitted (working tree on `stage-inspect`); `verify.ps1` not run — it refuses on a dirty tree.
- **R2.1 — workbook schema, entity-type catalogue, and schema version in Core** (commit `c79dc84`, 2026-09-18) — pure Core, no Office, no worksheet, no rendering. New types: `GanttEntityType` (16 members; enum carries code identity, catalogue owns the exact workbook display names), `EntityDateMode` (None/StartFinish/StartOnly), `EntityKind` (SectionHeader/Spacer/Span/Milestone/Delineator), `EntityColourCapability` flags (Fill/Stroke/Hatch/StyleDefined), `GanttLabelPosition` (full union of the guide's positions), `EntityTypeCatalog` (single code-owned catalogue of all 16 user-selectable Types with full contract metadata from the entity guide; `TryParse`/`Format` Ordinal on trimmed input, no case-folding, no guessing), `EntityTypeDefinition` (immutable per-type record), `GanttTableColumns` (`tblGanttData` schema: 13 required columns in contract order + optional `SortOrder`), `GanttSchemaVersion` (`CurrentSchemaVersion = 1`). Decisions D1–D7 recorded in `docs/work-items/R2.1-schema-and-type-catalogue.md`. Gates: `dotnet test tests/GanttCreator.Core.Tests` 118/118 PASS (83 pre-existing + 35 new); `pwsh ./scripts/verify-quick.ps1` PASS in 162.7s (all 12 steps); `dotnet format` clean; `git diff --check` clean; pre-commit hook PASS. Checklist: C, I. Office gate: None per roadmap — not run by design. Next: R2.2 (Initialise Sheet).
- **fix: PSSA `PSAvoidAssignmentToAutomaticVariable` on `ci-parity.Tests.ps1:278`** — the `Resolve-DeclarationEvents` loop used `$event`, a PowerShell automatic variable, as its iteration local (introduced in `8f9a0ac`, the scalarVars declaration-history refactor). Assigning to an automatic variable clobbers it for the rest of the script's event handlers, so the rule fires at Warning severity and `verify-quick.ps1` step 4 (`Invoke-PssaGate`) exits 1. Renamed the local to `$declEvent` at all 6 sites (278, 279, 280, 288, 289, 311); no behaviour change — the variable is private to that function and unreferenced anywhere else in the repo (verified by `Select-String '$event'` across all `*.ps1`/`*.cs`). Deliberately did **not** add the rule to `PSScriptAnalyzerSettings.psd1` `ExcludeRules`: that would disable a real safety rule to hide one misuse, contradicting the file's own "exclusions are deliberate and documented" contract. Gates: PSSA over `scripts/` with the repo settings (`Invoke-PssaGate -Path scripts -Settings scripts/PSScriptAnalyzerSettings.psd1`) → 0 findings; `scripts/test-scripts.ps1` 161/161 PASS (incl. `ci-parity` 33/33); `git diff --check` clean; diff is 6/6 lines. Checklist: G. Full `verify-quick.ps1` not re-run in this turn (the PSSA step is the only one affected and is now independently green).
- **Office gate: human-confirmed Excel open/close ×5 (2026-09-18)** — Human operator reports Excel opens and closes all sessions, tested 5 times against the Debug XLL (`GanttCreator.AddIn-AddIn64.xll`): session `open` record written (`addin-version=0.0.0+local`, `excel-version=16.0`, `process=x64`), Diagnostics dialog opened and closed (`button ID = 1`), `EXCEL.EXE` exited with code 0. VS Debug output shows zero exceptions across the sessions. This is human-observed evidence for the lifecycle path, not an automated gate result.
- **fix: hand-rolled parser gaps in artifact-marker, md-link, and CI-step gates** — `tests/GanttCreator.Architecture.Tests/ArtifactSourceMarkerTests.cs`: the `bin`/`publish` reference scan now runs against Roslyn comment-masked source (comments blanked, string literals kept so the quoted `Path.Combine(..., "bin", ...)` signal still fires), pinned to the `EnvVarGuardScanner` C# 14 language level; two positive tests prove `// clean up the bin folder` and `/* publish output */` no longer force a marker. `scripts/check-md-links.ps1`: new `Get-MarkdownTextWithoutFencedCodeBlock` strips CommonMark ```` ``` ````/`~~~` fenced blocks before the link regex so documentation examples are never validated as links; `scripts/check-md-links.Tests.ps1` proves a fenced docs example pointing at a
nonexistent path exits 0 while the same link outside a fence exits 1. `scripts/ci-parity.Tests.ps1`: `Get-CiStepBlock` is now indent-aware (boundary only at same-or-shallower `- name:` indent, so deeper-indented `run: |` script text is body, never boundary) with positive tests for a `- name:` comment/string inside a script and for a real boundary split. No new dependencies (`Microsoft.CodeAnalysis.CSharp` already pinned, PS fixes stdlib-only). `scripts/check-status.ps1` deliberately unchanged (zero fences, single-line inline spans, negligible risk). Gates: `dotnet test tests/GanttCreator.Architecture.Tests` 62/62 PASS (incl. 8/8 `ArtifactSourceMarkerTests`); `scripts/test-scripts.ps1` 157/157 PASS (incl. `ci-parity` 33/33, `check-md-links` 6/6); `pwsh ./scripts/check-md-links.ps1` OK (43 files); `dotnet format` clean; `git diff --check` clean. Follow-up PSSA findings in the same `verify-quick.ps1` run (`ci-parity.Tests.ps1` BOM, `diagnose-l12-createdump.ps1` BOM + unused `pidStr`; reproduced on the stashed pre-change tree,
so pre-existing, not introduced here) are fixed in the next commit on this branch, after which `pwsh ./scripts/verify-quick.ps1` is PASS in 160.9s (all 12 steps) and `pwsh ./scripts/verify.ps1` is PASS in 164.1s (all 14 steps, coverage 100%/100% on all four measured projects, vuln scan clean). Checklist: G.
- **docs: repo-root command paths, R1.6 teardown-count wording, L12 empty-snapshot guard** — `docs/REPO-MAP.md`: the command guidance now says "From the repository root:" instead of the checkout-specific `C:\repos\gantt-creator` prefix (the `pwsh ./scripts/check-md-links.ps1` command itself was already repo-relative and unchanged). `docs/work-items/REPO-MAP-navigation.md`: all seven evidence commands drop the hard-coded `C:\repos\gantt-creator` prefix in favour of repo-relative `./scripts/...` and `git add`/`git diff --cached` invocations; link-check, staging, validation, and diff checks are preserved. `docs/work-items/R1.6-deterministic-shutdown.md`: the outcome paragraph now names three open items — close-record attribution (`AutoClose` produces no `close` record on the `RegisterXLL` + `Quit()` path), human review of the Office-gate report, and the branch-final `verify.ps1`, push, and CI gates — instead of implying only attribution and review; D1, the Scope bullet for `AddInHostTeardownGuardTests`, and the acceptance item were aligned to the verified guard structure in `src/GanttCreator.AddIn/AddInHost.cs` (five teardown operations, each in its own catch-bearing `try`; `CommandBoundary.Reset()` and `DiagnosticsService.Reset()` guarded independently; the log dispose is guarded but is not one of the four operations the checker lists). `scripts/diagnose-l12-createdump.ps1`: `Test-HarnessProcessTreeActive` treats an empty `Win32_Process` snapshot as active (termination not confirmed) with a one-time warning, instead of confirming termination from a failed probe; normal parentage evaluation (`return $activePids.Count -gt 0`) is unchanged. `scripts/diagnose-l12-createdump.Tests.ps1` pins the new behaviour with a positive control for the old `return $false` shape. Gates: `pwsh ./scripts/verify.ps1` PASS in 170.2s (17/17 steps: Pester 154/154, Release build with `-warnaserror` 0 warnings/0 errors, `dotnet format` clean, coverage threshold, package vulnerability scan); `git diff --check` clean. Checklist: I, G.
- **fix: per-step teardown guards in AutoClose (R1.6 review finding)** — `src/GanttCreator.AddIn/AddInHost.cs` `AutoClose` now guards each teardown operation on its own: `RibbonStateService.Reset()`, the close record, `CommandBoundary.Reset()`, `DiagnosticsService.Reset()`, and the log dispose (which keeps its `finally` clearing `_log`/`_sessionToken`). Previously only the close record had its own guard, so a throw from `RibbonStateService.Reset()` skipped the close record *and* both singleton resets, and a throw from `CommandBoundary.Reset()` skipped `DiagnosticsService.Reset()` — contradicting the method's own "every step is individually guarded" comment, work item `docs/work-items/R1.6-deterministic-shutdown.md` D1, and the existing `ExcelApplicationAdapter.Dispose` pattern. Silent degradation and the never-throw contract are unchanged. New tests: `tests/GanttCreator.AddIn.Tests/AddInHostTests.cs` gains `AutoClose_close_record_failure_does_not_skip_the_singleton_resets` (a throwing close-record write still drops the captured boundary's log, clears `DiagnosticsService`'s path, and disposes the log), and the new `tests/GanttCreator.Architecture.Tests/AddInHostTeardownGuardTests.cs` parses `AutoClose` and requires each of the four teardown operations to sit inside exactly one catch-bearing `try` with no shared guard. Its positive controls include the pre-fix shared-guard shape, observed RED against the `HEAD` revision of `AddInHost.cs` (temporary substitution, byte-identical restore verified by file hash) with `LogClose is enclosed by 2 try statement(s)` and `shared guard for: CommandBoundary.Reset, DiagnosticsService.Reset, RibbonStateService.Reset`, plus a deleted-step control. The three `Reset` calls are non-throwing by contract, so their guards are unreachable defence in depth with no runtime failure to inject; that limitation is recorded in the work item. Gates: `dotnet test tests/GanttCreator.AddIn.Tests -c Release` 120/120 PASS; `dotnet test tests/GanttCreator.Architecture.Tests -c Release` 60/60 PASS; AddIn Release build with `-warnaserror` 0 warnings/0 errors; `dotnet format --verify-no-changes --exclude tests` exit 0; `git diff --check` clean (CRLF advisory only, from the pre-existing working-copy line endings of `tests/GanttCreator.AddIn.Tests/AddInHostTests.cs`). Checklist: G, I. Uncommitted (working tree on `stage-inspect`).

- **fix: review-finding sweep — R1.6 post-D5 wording, abort-pattern CRLF, PID-argument and private-scope fixes (five verified findings, none skipped)** — `docs/work-items/R1.6-deterministic-shutdown.md`: the outcome paragraph now separates the historical Commit 2/D3 narrowed smoke-test scope (registration success, shared open-record growth, owned-process exit only) from the post-D5 outcome — open-record attribution closed, five distinct session tokens recorded (Office gate PASS 2026-09-17) — and names close-record attribution and human review as the only open items; the `AutoClose_drops_every_service_log_reference_before_disposing_the_log` acceptance item now requires capturing `CommandBoundary.Instance` before `AutoClose` and asserting the captured instance's `GetLog()` (never a newly created post-reset singleton); the Office acceptance item records the Commit 2 smoke test as historical and superseded by `Five_cycles_registering_the_packed_XLL_leave_no_orphan_and_show_session_open_record_growth`. `scripts/diagnose-l12-createdump.ps1`: the abort signature pattern gains the optional trailing CR (`^(?:Aborted\.|The process was aborted\.)\r?$`) because .NET multiline `$` matches only before a `\n`, so genuine CRLF output never matched and a real abort fell through to the plain test-failure branches; all five `Get-HarnessProcessTreePids` call sites drop the unsupported `-KnownChildPids` argument (a parameter-binding error under `$ErrorActionPreference = 'Stop'`, reproduced in pwsh), and the pre-kill, post-exit, and cleanup snapshots union the pids accumulated during polling so descendants observed before the kill stay classified. `scripts/diagnose-l12-createdump.Tests.ps1` pins the new exact pattern, adds a CRLF capture case, and adds a call-shape/union tripwire. `scripts/ci-parity.Tests.ps1`: the W14 declaration record stores `IsPrivate` alongside `IsScalar` (the `private:` qualifier is preserved from `VariablePath.UserPath`) and scope-chain resolution skips a private declaration found in an outer scope while still resolving it inside its declaring scope; a new parity fixture pins own-scope (1), private-qualified reference (1), nested-outer (0), script-private (0) and the non-private negative control (1, 1). Gates: Pester `scripts/ci-parity.Tests.ps1` + `scripts/diagnose-l12-createdump.Tests.ps1` 60/60 PASS; `pwsh ./scripts/test-scripts.ps1` 152/152 PASS; PSScriptAnalyzer over `scripts/` 0 findings; `git diff --check` clean. `verify-quick.ps1` not completed: its step 2 (`check-cline-skills.ps1`) aborts on the uncommitted `docs/` change this fix necessarily makes before commit, so later steps were not reached. Checklist: G, I.


- **docs: persistent repository map discoverable from the agent contract** — Added `docs/REPO-MAP.md`: a navigation-only map of the four production projects, their project references, task-to-source/test routing, the current capability boundary (scene/layout, chart rendering, PowerPoint transfer, and PNG encoding are planned, not implemented), and the verification commands. `AGENTS.md` gains a short "Repository navigation" section pointing at the map and requiring an update in the same change as any mapped responsibility, entry point, test location, or command. The map deliberately omits line numbers, test counts, and copied schemas, states that symbol outlines are declaration locators rather than compiler or reference evidence, and requires truncated outlines to be narrowed and re-checked against source. The map also documents the limitation that no persistent symbol cache or automatic freshness checker is installed (see `docs/REPO-MAP.md` line 29). Plan, acceptance criteria, and a deferred, separately scoped cached-index proposal live in `docs/work-items/REPO-MAP-navigation.md`. Gates: `scripts/check-md-links.ps1` PASS; `scripts/check-md-links.Tests.ps1` Pester PASS; `scripts/verify-quick.ps1` PASS; clean-tree `pwsh ./scripts/verify.ps1` PASS. Documentation only: no production, test, schema, dependency, or roadmap change. Checklist: I.
- **test: scope-qualified W14 declarations and per-load-cycle AddInHost teardown logs (review-finding sweep)** — Five verified findings fixed, none skipped. `scripts/ci-parity.Tests.ps1`: `Split-ScopeQualifiedName` strips only `script:`/`global:`/`local:`/`private:` qualifiers and marks every other qualified variable path (`$env:Solution`) `IsScopeQualifier = $false`, so provider-qualified paths are unverified targets — never matched against local declarations in the command resolver and never registered by `Add-ScalarVar`; two new parity fixtures pin the provider-qualified (`0`) and global-scope (`1, 0, 0`) behaviours. `Get-EnclosingScopeName` and `Get-EnclosingScopeChain` now map `script:` to `__ScriptScope__` and `global:` to `__GlobalScope__` (previously both collapsed to `__ScriptScope__`). Removed the dead `Add-VariableDeclaration`/`Get-DeclarationScopeName` pair and the stray `if ($variable)` branch in the `TypeConstraint` loop that referenced never-initialized `$variable`/`$assignment`/`$isScalarString` and undefined `$variableDeclarations`. `tests/GanttCreator.AddIn.Tests/AddInHostTests.cs`: `AutoOpen_after_AutoClose_reopens_deterministically` supplies a distinct `CapturingLog` per load cycle (first log holds one open/close pair and is disposed after the first `AutoClose`; second log active during the second session and disposed by its `AutoClose`), and `AutoClose_drops_every_service_log_reference_before_disposing_the_log` captures `CommandBoundary.Instance` before `AutoClose` and asserts on the captured boundary (accessing the singleton after the session reset fabricated a fresh, trivially log-free instance). Gates: `Invoke-Pester scripts/ci-parity.Tests.ps1` 30/30 PASS; `dotnet test tests/GanttCreator.AddIn.Tests` 128/128 PASS; `git diff --check` clean; `launchSettings.json` line-ending noise restored, not committed. `verify.ps1` not run (task-scoped targeted gates only). Checklist: G, I.

- **feat: correlatable lifecycle records and attributable five-cycle gate (R1.6, Option A)** — D5 approved logging-contract change: `src/GanttCreator.AddIn/AddInIdentity.cs` gains `SessionToken`; `AddInLifecycle.LogOpen` writes `open ... session=<token>` and `LogClose(token)` writes `close session=<token>`; `AddInHost.GenerateSessionToken` emits a redaction-safe timestamp-prefixed token (alphabet excludes `a`–`f` so Core `Redactor` cannot mask it; GUID/long-hex/PID rejected for correlation reasons), captured at `AutoOpen` and released at `AutoClose` (detach-first order and never-throw contract unchanged); `DiagnosticsService.GatherIdentifiers` passes `"unknown"` (diagnostics record shape unchanged). Tests: `AddInLifecycleTests` open/close shapes + `unknown` normalization; `AddInHostTests` token redaction-survival, distinctness, and open→close token carry-through; OfficeFixtureTests gains `PackedOpenSessions` matcher with sessionless-record/other-XLL/close-record/empty-token negative controls, and the five-cycle gate now seeds the seen-session set from the log before cycle 1 so no foreign pre-existing record can satisfy any cycle — each cycle must yield a NEW distinct session token. Close-callback attribution remains open: `AutoClose` produces no record on the automation path even after clean `Quit()` and full exit (D6 finding; contract pinned by `AutoClose_carries_the_open_session_token_into_the_close_record`); process exit is not `AutoClose` proof. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 128/128 PASS; AddIn Release build 0/0; `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 900` PASS 9/9 (5 m 35 s, Excel 16.0.20326.20144 x64): 13 pre-existing sessions rejected, five new distinct sessions one per cycle, no orphan, no L12 abort. Human Office review, branch-final `verify.ps1`, push, and CI remain pending. Work item: `docs/work-items/R1.6-deterministic-shutdown.md` (D5/D6 + resolved pending section). Checklist: G, I, J.
- **test: five-cycle packed-XLL registration smoke test (R1.6 partial, superseded by the session-token commit above)** — `tests/GanttCreator.Office.IntegrationTests/OfficeFixture.cs` adds `RegisterXll`; shared-log growth was a smoke signal only and has been replaced by per-session attribution. Production code was unchanged in that commit. Gates: `pwsh ./scripts/verify-quick.ps1` PASS (12/12 steps). Checklist: G, I, J.
- **fix: reconcile diagnose-l12-createdump tests with the merged script lineage (commit `17328b2`)** — Merge `923dff7` combined two divergent lineages of the `diagnose-l12` pair inconsistently, leaving `verify-quick.ps1` red on HEAD (`diagnose-l12-createdump.Tests.ps1` 3/28 FAIL) before any R1.6 change: the script kept its deadline-polled `Wait-HarnessProcessTreeExit` form while two test fragments expected a one-shot `Test-HarnessProcessTreeActive` assignment shape, and the root-only positive-control stub assigned `$step1TreeExited` while asserting it never did. Repair aligns the two foreign test fragments to the stronger behaviour the same test's passing assertions already pin (form-agnostic `Wait-HarnessProcessTreeExit` assertion; stub captures only the root exit result) and reorders two behaviour-neutral script initializations so the pinned pre-`try` shape matches again. No test deleted. Gates: `Invoke-Pester scripts/diagnose-l12-createdump.Tests.ps1` 28/28 PASS; `pwsh ./scripts/verify-quick.ps1` PASS in 145.8 s (12/12 steps); `git diff --check` clean. Checklist: G.
- **feat: deterministic AutoClose teardown with detach-first cleanup (R1.6)** — `src/GanttCreator.AddIn/AddInHost.cs` tears down in one deterministic order: `RibbonStateService.Reset()` detaches the owned COM event connection first, then the close record is written, then `CommandBoundary`/`DiagnosticsService` drop their log references, and the log is disposed last — removing the re-entrancy window where a workbook-state event could fire into services after the log was disposed. The Excel application adapter is now injectable (optional `Func<IExcelApplicationAdapter>` constructor parameter; production parameterless constructor unchanged) so the teardown order is contract-testable outside Excel. `src/GanttCreator.AddIn/CommandBoundary.cs` gains an internal `GetLog()` test-only accessor mirroring `RibbonStateService.GetRibbon()`. Five new contract tests in `tests/GanttCreator.AddIn.Tests/AddInHostTests.cs`: detach-before-close-record sequence, dead subscription after close, no disposed-log retention in any session singleton, deterministic reopen, and real log-file handle release. Work item: `docs/work-items/R1.6-deterministic-shutdown.md`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 115/115 PASS (Release, was 110); `dotnet test tests/GanttCreator.Office.ContractTests` 17/17 PASS; `dotnet test tests/GanttCreator.Architecture.Tests` 56/56 PASS; AddIn Release build 0 warnings/0 errors. Office gate: Not run at implementation time (requires F5/self-hosted runner; the five-cycle `RegisterXLL` fixture is the planned second commit). Checklist: G, I, J.
- **fix: anchored abort-pattern lines and complete-tree wait before stream access (`scripts/diagnose-l12-createdump.ps1`, commit `3a282a0`)** — Step 1 abort classification now uses the line-anchored pattern `^(?:Aborted\.|The process was aborted\.)$` matched with `(?im)`, so only complete lines equal to `Aborted.` or `The process was aborted.` classify as abort signatures; other lines such as `Operation was aborted.` are rejected. Regression tests verify both accepted signatures plus suffix/embedded rejections. Step 1 snapshots known descendants (`Get-HarnessProcessTreePids`) and polls (`Wait-HarnessProcessTreeExit`) until the complete process tree has exited before reading redirected streams or deleting temp files; the `finally` cleanup uses the same complete-tree check. Gates: `pwsh ./scripts/test-scripts.ps1` 148/148 PASS; `git diff --check` clean. Checklist: G.
- **docs: both open and Diagnostics records carry InformationalVersion unchanged (`src/GanttCreator.AddIn/AddInIdentity.cs`, commit `e6822c2`)** — Doc-only correction: removed the stale claim that the `Diagnostics:` record uses a metadata-stripped semantic version. Verified `AddInHost` passes `Core.VersionInfo.InformationalVersion` and `DiagnosticsService.WriteDiagnosticRecord` reuses `identifiers.AddInVersion` unchanged. Gates: `dotnet build`/`dotnet test tests/GanttCreator.AddIn.Tests` 110/110 PASS; `git diff --check` clean. Checklist: G, I.
- **docs: Diagnostics follows active-workbook state, Open Log tested via LogAvailable (`docs/work-items/R1.5-ribbon-state-service.md`, commit `c0a8d04`)** — Office acceptance text no longer treats workbook opening as verification of `btnOpenLog`; `btnOpenLog` is tested independently via `LogAvailable`. Also includes the `scripts/ci-parity.Tests.ps1` scope-resolution fix from the same review batch: variable resolution records scalar and non-scalar declarations per scope, honors explicit scope modifiers, and stops at the nearest matching declaration so local unqualified assignments shadow outer variables. Gates: `pwsh ./scripts/test-scripts.ps1` 148/148 PASS; `git diff --check` clean. Checklist: G, I.
- **fix: review findings for ribbon/version docs, W14 scope resolution, and L12 shutdown/abort classification (uncommitted)** — `docs/work-items/R1.5-ribbon-state-service.md` now tests Diagnostics against active-workbook state and `btnOpenLog` independently against `LogAvailable`; `src/GanttCreator.AddIn/AddInIdentity.cs` documents the unchanged informational version (including metadata) in both records. `scripts/ci-parity.Tests.ps1` records scalar and non-scalar declarations per lexical scope, honors explicit scope qualifiers, and stops at the nearest declaration; its fixture proves a local collection shadows a script `[string]$Solution` while `$script:Solution` still counts. `scripts/diagnose-l12-createdump.ps1` captures known descendants, polls the complete owned tree before stream access/deletion and in `finally`, and restricts abort detection to complete `Aborted.` / `The process was aborted.` lines; `scripts/diagnose-l12-createdump.Tests.ps1` pins the accepted and rejected signatures. Gates: targeted Pester 53/53 PASS; three changed PowerShell files parse clean and PSScriptAnalyzer 1.25.0 reports 0 findings; AddIn Release build 0 warnings/0 errors; `git diff --check` clean. Full `scripts/test-scripts.ps1`: not green on this Linux sandbox because unrelated Windows/POSIX harness cases fail. Checklist: G, I.
- **fix: W14 scalar-variable scope-chain walk + cast-assignment LHS guard (`scripts/ci-parity.Tests.ps1`)** — Two still-valid defects in the `Get-DotnetTestProjectTokenCount` tripwire. (1) The `ConvertExpressionAst` branch recorded the operand variable of *any* `[string]` cast, including a bare cast used as a value expression (e.g. `[string]$x` in a command argument); only a cast that is the **Left** of an `AssignmentStatementAst` proves a scalar assignment. Guard added: `if ($parent.Parent -is [AssignmentStatementAst])` before recording the operand. (2) The variable lookup checked only the immediate function scope, so a scalar `[string]$Solution` declared in script scope (or a parent function) was invisible to a command inside a nested function — a legitimate single-target invocation was miscounted as zero. Fix: new `Get-EnclosingScopeChain` returns the ordered chain from innermost function out to `__ScriptScope__`; the lookup walks that chain and counts on the first match, preserving same-named-variable independence (each scope's scalar set is keyed separately). Gates: `scripts/ci-parity.Tests.ps1` 25/25 PASS (Pester 6.1.0); `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (151.1s); `git diff --check` clean. Checklist: G, I.
- **fix: diagnose-l12 Step 1 deadline kill awaits the owned tree and aborts on non-exit (`scripts/diagnose-l12-createdump.ps1`)** — The `WaitForExit(5000)` call after the Step 1 deadline kill previously discarded its bool (`$null = $step1Proc.WaitForExit(5000)`), so a tree that failed to exit within 5s fell through to reading and deleting the redirected streams while the child still held them open (partial reads, silently-failed deletes, leaked temp files). The bool is now captured into `$step1TreeExited`; a `False` result logs a FAIL and exits 3 before any stream access, so the streams are touched only after confirmed process termination. The error/cleanup `finally` block keeps its own discard form, which is why the new Pester test scopes the negative assertion to the deadline branch. Two new Pester tests with a positive control: capture-and-abort ordering (+ failing stub that discards the bool). Gates: `scripts/diagnose-l12-createdump.Tests.ps1` 27/27 PASS (Pester 6.1.0); PSSA 0 findings on both modified files; both files parse clean; `git diff --check` clean. Checklist: G.
- **fix: W14 scalar-variable tracking keyed by name + lexical scope (`scripts/ci-parity.Tests.ps1`)** — The `Get-DotnetTestProjectTokenCount` helper keyed scalar `[string]` declarations by variable name alone, so a `[string]$Solution` declared in one function vouched for a same-named collection `$Solution` in a sibling function or at script scope; the tripwire would have counted a collection as one positional target and let a `dotnet test $Projects`-style defect pass. Fix: `scalarVars` is now `Dictionary<string, HashSet<string>>` keyed by scope name; new helpers `Add-ScalarVar` and `Get-EnclosingScopeName` record each declaration against the nearest enclosing `FunctionDefinitionAst` (or `__ScriptScope__` when there is none), and every command variable is resolved against its own enclosing scope before scalar validation. Added a parity-test fixture `same-named variables in different lexical scopes remain independent` asserting counts `(1, 0, 0)` for a function-scoped `[string]$Solution` invocation, a sibling function's same-named collection, and a script-scope same-named collection. Verified the old name-only logic produced `(1, 1, 1)` on the fixture (genuine regression guard). Gates: `scripts/ci-parity.Tests.ps1` 25/25 PASS (Pester 6.1.0); `git diff --check` clean. Checklist: G, I.
- **fix: diagnostics Close button ID value equality; `btnDiagnostics` icon switched to gallery+Excel-verified `Help` (commit `6296e7b`)** — F5 evidence 2026-09-15 proved `TaskDialogApi.ShowWithHyperlink` logged `button ID = 0` on Close clicks: `TaskDialogButton.Close` is a static getter returning a new equal instance per access (verified on installed WindowsDesktop 10.0.12: `ReferenceEquals` false, `==` true), so the `ReferenceEquals` comparison could never match. `src/GanttCreator.AddIn/DiagnosticsService.TaskDialog.cs`: new testable `MapDialogResult` using `==`, with XML remarks citing the evidence. Icon: `imageMso="Information"` is absent from the Office 2010 Icons Gallery (7344 IDs) and `imageMso="OfficeDiagnostics"` is absent from the Excel 2007 idMso table in `[MS-CUSTOMUI]-250218` (`.microsoft/` reference, git-ignored) — both render as no icon with no error per the spec ("application-defined and SHOULD be ignored if not understood"). `src/GanttCreator.AddIn/RibbonResources/Ribbon.xml` (+ `RibbonResources.Designer.cs` comment): `btnDiagnostics` now `imageMso="Help"` (present in the Excel idMso table). `src/GanttCreator.AddIn/AddInIdentity.cs`: doc-only clarification pinning the `open`=informational / `Diagnostics:`=semantic version split. `.gitignore`: ignore `.microsoft/` local reference material. Tests in the same change: `MapDialogResult` Close→1 and custom/null→0 facts in `tests/GanttCreator.AddIn.Tests/DiagnosticsServiceTests.cs`; `Ribbon_buttons_use_gallery_verified_imageMso_ids` in `tests/GanttCreator.AddIn.Tests/GanttRibbonTests.cs`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 119/119 PASS; AddIn Release build 0 warnings/0 errors; `dotnet format` on the AddIn project clean; `git diff --check` clean; Office gate (F5, 2× Close clicks → `button ID = 1`, zero exceptions): **PASS 2026-09-16, confirmed by human operator**; icon renders (Help glyph confirmed visible by human operator 2026-09-16). Checklist: G, I.
- **fix: L12 diagnostic review-finding batch 4 (Step 1 timeout kill+wait/stream preservation, any-age abort classification) + W14 counter single-parse and scalar-verified variable targets (`stage-inspect`)** — Four findings from a commit `0da9f59` review verified against the current tree (both files byte-identical to `0da9f59`), all fixed, none skipped. (1) `scripts/ci-parity.Tests.ps1`: `Get-DotnetTestProjectTokenCount` now parses the complete script text once via `Parser::ParseInput` and passes that whole-file AST to `Find-DotnetTestCommands`; the per-line `Join-LogicalLines` pass is removed, so here-string prose is a string literal (never a `CommandAst`) instead of being counted as a phantom `dotnet test` invocation, and comments/line continuations are handled by the AST. (2) Same file: target counting rejects any variable the script does not prove is a scalar string — a variable counts only when the same parsed AST shows a `[string]` type constraint on a `ParameterAst` or on a `ConvertExpressionAst` assignment LHS (operand via `Child`); splats and sub-expressions are never counted, while literal project and solution file targets are unchanged. (3) `scripts/diagnose-l12-createdump.ps1` Step 1: on deadline the owned testhost tree is killed with `taskkill /T /F` and then `WaitForExit(5000)` BEFORE the redirected streams are read and the temp files deleted; both streams are read and preserved into `$step1Output` before the deletes, and the timeout failure report now includes the captured output excerpt. (4) Same script: a new classification branch after the invalid-args check marks nonzero exits with an abort-signature match as `testhost-abort` at any age (not only under one second), and the summary/log wording no longer limits crash classification to sub-second exits. Eight new Pester tests with positive controls: here-string prose ignored, collection variable (`$Projects`) rejected, `[string]`-assigned variable accepted, splat pinning control, timeout kill+wait/read/delete ordering (+ failing stub), any-age abort classification (+ failing stub). Gates: `pwsh ./scripts/test-scripts.ps1` 137/137 PASS (up from 129); PSSA 0 Error/Warning findings on all three modified files; all three files parse clean; `git diff --check` clean. `coderabbit review --agent`: Not run — CLI not installed on this machine. Checklist: G.

- **feat: R1.5 ribbon state service (commit `9bcffff`)** — `docs/work-items/R1.5-ribbon-state-service.md`: one `RibbonStateService` (`src/GanttCreator.AddIn/RibbonStateService.cs`, immutable snapshot in `src/GanttCreator.AddIn/RibbonState.cs`) owns the dynamic getters (`GanttRibbon.GetEnabled` routes through it; `btnDiagnostics` gated on `HasActiveWorkbook`, new `btnOpenLog` gated on `LogAvailable`) and the `IRibbonUI` invalidate mechanism (`GanttRibbon.OnLoad` publishes the handle; workbook-state events from the new narrow port `src/GanttCreator.Office/IExcelApplicationAdapter.cs` refresh it; every command run through the ribbon's boundary ends with a refresh). `AddInHost` arms the service in `AutoOpen` and resets it in `AutoClose`; the one-record lifecycle contract is unchanged. DEBUG-only force-state hook `src/GanttCreator.AddIn/DebugForceRibbonState.cs` (`GANTTCREATOR_FORCE_RIBBON_STATE`), pinned by a targeted positive control in `tests/GanttCreator.Architecture.Tests/DebugHookGuardTests.cs`. Gates: `dotnet test tests/GanttCreator.Office.ContractTests` 17/17 PASS; `dotnet test tests/GanttCreator.AddIn.Tests` 107/107 PASS; `dotnet test tests/GanttCreator.Architecture.Tests` 56/56 PASS; `git diff --check` clean. Office gate (VS F5): **Not run** — requires desktop Excel x64; evidence to be recorded by the human operator in the work item Notes. Checklist: G, H, I, J, K (partial — Office gate pending).
- **fix: RibbonResources strongly-typed accessor is internal** — `src/GanttCreator.AddIn/GanttCreator.AddIn.csproj`: resource `Generator` changed `PublicResxFileCodeGenerator` → `ResxFileCodeGenerator`, and `src/GanttCreator.AddIn/RibbonResources.Designer.cs` was regenerated via `StronglyTypedResourceBuilder` (`internalClass: true`), so the class and members are now `internal`. Gates: AddIn build 0 warnings/0 errors; `dotnet test tests/GanttCreator.AddIn.Tests` 86/86 PASS; `git diff --check` clean.
- **fix: diagnose-l12 Step 1 passes repo-rooted solution path** — `scripts/diagnose-l12-createdump.ps1`: `$solutionPath` is now resolved against `$repoRoot` before `$step1Args` is built and the resolved path is passed to `dotnet test`; the later duplicate resolution was removed with the `Test-Path` guard and `Start-Process` invocation unchanged. Gates: PSSA 0 findings; `git diff --check` clean. Checklist: G.
- **fix: EnvVarGuardScanner masking coverage for #elif branches and non-DEBUG conjunctions (uncommitted, working tree on `stage-inspect`)** — `tests/GanttCreator.Architecture.Tests/EnvVarGuardScanner.cs` `MaskCommentsAndStrings` now parses the source once per referenced symbol in three new modes while preserving the existing DEBUG-inclusive per-symbol parses: (1) DEBUG-excluded per-symbol parses, toggling each non-DEBUG symbol alone, which activates `#elif` branches whose `#if` is DEBUG (e.g. `#if DEBUG ... #elif FEATURE`); (2) an all-symbols parse with every non-DEBUG symbol defined at once, which activates conjunctions of non-DEBUG symbols such as `#if A && B` or `#elif A && B` under a false preceding `#if` — no single-symbol parse can satisfy these. DEBUG is left undefined in both so `!DEBUG`-gated branches stay reachable, and DEBUG is filtered out of the DEBUG-excluded/all-symbols symbol sets because defining DEBUG without DEBUG is contradictory. The explanatory comment was rewritten to describe the full expanded parse coverage. Gates: `dotnet build` Release 0 warnings/0 errors; `dotnet test tests/GanttCreator.Architecture.Tests` 55/55 PASS; `git diff --check` clean; the file is clean under `dotnet format` (the remaining format findings are pre-existing in other `tests/` files, tolerated per `tests/Directory.Build.props`). Checklist: G, I. Uncommitted (working tree on `stage-inspect`).
- **fix: L12 diagnostic review-finding batch 3 (Step 1 owned-process teardown, runtime-aware createdump resolution, dump-written exit/size contract)** — Three verified findings fixed, none skipped. (1) `scripts/diagnose-l12-createdump.ps1` Step 1: the dotnet-test launch, poll, and classification are now wrapped in `try/finally` around `$step1Proc` (nulled before launch); the finally block terminates the owned testhost process tree with `taskkill /PID $step1Proc.Id /T /F` on every path — normal exit, the existing deadline branch, and any terminating error or interruption after launch. (2) Step 0: the requested runtime version is now read from the standard `runtimeOptions.framework` (single) or `runtimeOptions.frameworks` (multi-framework list) schema instead of the non-standard `Microsoft.NETCore.App.RuntimeVersion` property, and the installed runtime is roll-forward-resolved (latest patch of the requested minor band, else latest patch of the lowest higher minor band within the same major) before locating `createdump.exe`. Framework parsing or version resolution failure now skips Step 2 with a log line rather than degrading to the arbitrary recursive `createdump.exe` selection the step exists to avoid. (3) Step 2: `dump-written` is now reported only when the createdump exit code is 0 AND the dump file exists with size > 0; a failed createdump that leaves a zero-byte or partial artifact, and an exit-0 run that leaves only such an artifact, are now classified distinctly (`other-failure`/`access-denied` note the partial artifact; `no-dump-exit-zero` distinguishes absent vs zero-byte). Gates: `scripts/test-scripts.ps1` 129/129 PASS (13 in the diagnose harness, up from 10); PSSA 0 findings on both modified scripts; a synthetic roll-forward spike covering same-band/latest-patch, lowest-higher-minor-band roll, multi-framework selection, self-contained, unparseable, and no-installed-runtime cases all PASS; `git diff --check` clean (the pre-existing CRLF warning is unchanged from the clean tree). Checklist: G. Uncommitted (working tree on `stage-inspect`).
- **fix: L12 diagnostic review-finding batch (createdump runtime-aware discovery, Step2DeadlineSeconds validation, timeout-branch crash-flag removal, KNOWN-LIMITATIONS L12 rewording, ci-parity --diag option-value consumption, EnvVarGuardScanner per-symbol #elif masking)** — Seven verified findings fixed, none skipped. (1) `scripts/diagnose-l12-createdump.ps1` Step 0: createdump discovery now resolves the .NET runtime used by the OfficeIntegration testhost (`GanttCreator.Office.IntegrationTests.runtimeconfig.json` → `Microsoft.NETCore.App.RuntimeVersion`) and selects the createdump.exe under that runtime's shared framework directory, with a version-matched recursive fallback; the same resolved `$createdump` path is used and logged by both Step 0 and Step 2. (2) Step 1 pre-flight: `$Step2DeadlineSeconds` validation added alongside the existing `$Step1TimeoutSeconds`/`$Step1DeadlineSeconds` checks — zero or negative values exit 2 through the same validation path before Step 2 can produce a misleading timeout-killed result. (3) Timeout branch: `$step1Crashed = $true` removed; a Step 1 deadline hit reports `outcome=timeout` only, so the summary never conflates timeout with crash. (4) `docs/KNOWN-LIMITATIONS.md` L12 entry: the debug-privilege / antivirus-EDR diagnostic is now explicitly stated as **unimplemented** — the parent-process dump smoke test cannot validate that hypothesis because createdump dumps its own parent process, not an arbitrary remote target. (5) `scripts/ci-parity.Tests.ps1`: `Get-DotnetTestProjectTokenCount` now consumes the value following `--diag` as an option argument (via a `$skipNext` flag) instead of counting it as a positional target; new positive control `tripwire accepts dotnet test --diag with a .sln-named diagnostic file and no target` expects `@(0)`. (6) `scripts/diagnose-l12-createdump.Tests.ps1`: the `createdump` assertion tightened to `Start-Process -FilePath $createdump.FullName` so declarations or log messages alone cannot satisfy the check. (7) `tests/GanttCreator.Architecture.Tests/EnvVarGuardScanner.cs`: the referenced-symbol parse now iterates each symbol individually (plus DEBUG) instead of defining all symbols in one parse, so every `#elif` branch becomes active in at least one parse and its literal/comment spans are masked. Gates: Pester 31/31 PASS (11 diagnose-l12 + 20 ci-parity); PSSA clean on all three modified PowerShell scripts; `GanttCreator.Architecture.Tests` 55/55 PASS (build 0 warnings/0 errors); `git diff --check` clean.
- **refactor: shared `ScannerLanguageVersion` constant in `EnvVarGuardScanner`** — The three `(LanguageVersion)1400` casts in `MaskCommentsAndStrings` (the two initial parses and the third "all symbols" parse) are replaced by a single `private const LanguageVersion ScannerLanguageVersion = (LanguageVersion)1400;` declared alongside `DebugPreprocessorSymbols`, with the rationale moved to the constant. Value unchanged (still C# 14 / 1400); the named `LanguageVersion.CSharp14` member cannot be used against the installed Roslyn 4.14.0 compile asset (verified: build error CS0117), so the numeric cast is retained. `const` also satisfies CA1802. Gates: build 0 warnings/0 errors; `GanttCreator.Architecture.Tests` 55/55 PASS.
- **fix: L12 diagnostic review-finding batch (ci-parity per-invocation target validation, bare-timeout suffix pattern, Step 1 pre-flight + abort classification, Step 2 smoke-test rewording + stale-dump guard)** — Four verified findings fixed, none skipped. (1) `scripts/ci-parity.Tests.ps1`: `Get-DotnetTestProjectTokenCount` now returns one target-token count per `dotnet test` invocation (an array) instead of an aggregate total; the W14 guard iterates those counts and reports the offending invocation index. New positive control `tripwire rejects a zero-target invocation alongside a valid one` (expects `@(0, 1)`). (2) `scripts/diagnose-l12-createdump.Tests.ps1`: the bare-timeout pattern at line 39 now allows the numeric timeout to carry a duration suffix (`60s`) or be followed immediately by whitespace, end-of-input, or a comma; a new positive control covers the `60s` case. (3) `scripts/diagnose-l12-createdump.ps1`: Step 1 no longer treats every fast `dotnet test` exit as a testhost crash — pre-flight validates the solution path, positive timeout/deadline, and built `GanttCreator.Office.IntegrationTests` artifacts (exit 2 on each); the process is started with redirected stdout/stderr; crash classification captures output and exit status and only sets `$step1Crashed` on a testhost-abort signature, with invalid-argument output routed to a distinct `invalid-args` outcome. (4) Step 2 reclassified as a parent-process dump smoke test (synopsis, header comment, inline comment, and summary all say so; EDR-blocking validation directed to the runtime-triggered path targeting a different process such as `$helperProc`), and the stale dump is removed before each run. New tests `Step 2 never reuses a stale dump from a previous run` and `Step 1 validates inputs and build artifacts before launching`. Gates: Pester 30/30 PASS; PSSA clean on all three modified scripts; `git diff --check` clean; `verify-quick.ps1` not run — it refuses on a dirty tree.
- **fix: review-finding batch (Roslyn 4.14 pin + scanner language pin, ci-parity bare `dotnet test` guard, L12 createdump diagnostic Step 2 rewrite, STATUS open-questions hygiene)** — Six verified findings fixed, none skipped. (1) `Directory.Packages.props` pins `Microsoft.CodeAnalysis.CSharp` 4.12.0 → 4.14.0 and `tests/GanttCreator.Architecture.Tests/packages.lock.json` is regenerated via `dotnet restore` (resolved 4.14.0, content hash updated); `EnvVarGuardScanner` now passes an explicit C# 14 `LanguageVersion` (numeric `1400`) at every `CSharpParseOptions` construction feeding `CollectMaskSpans`. The named member cannot be used: the installed 4.14.0 package's lib/net9.0 compile asset has no `LanguageVersion.CSharp14`, while its lib/net8.0 asset defines `CSharp14 = 1400` (both verified by reflection on the installed package); the numeric value is accepted by the parser and equals the named member where the member exists. (2) `scripts/ci-parity.Tests.ps1`: the W14 validation guard and `Get-DotnetTestProjectTokenCount` now accept end-of-line (not only whitespace) after `dotnet test`, so a bare `dotnet test` invocation enters validation and is counted (still flagged by the exactly-one-target contract); a bare-line case was added to the zero-target tripwire as the positive control. (3)+(4) `scripts/diagnose-l12-createdump.Tests.ps1`: both bare-timeout pattern declarations now allow the numeric timeout to be followed immediately by whitespace or end-of-input (the old pattern required two extra characters, so the positive-control stub `--blame-hang-timeout 600` before a newline could never match and the negative control was silently dead), and the paired-flag assertions now match the script's comma-separated PowerShell argument array — separate `'--blame-hang-dump-type'` and `'none'` arguments, and `'--blame-hang'` required as a standalone argument so longer related flags cannot satisfy the check. (5) `scripts/diagnose-l12-createdump.ps1`: the Step 1 crash-age classification now measures the process lifetime from its own exit/start timestamps (`ExitTime - StartTime`) instead of the watchdog stopwatch, which included up to the 1 s polling quantum and could misclassify rapid crashes; Step 2 rewritten — each redirect parameter gets one valid path variable (the old `-RedirectStandardOutput $env:TEMP Step2-out.tmp` passed two tokens and aborted `Start-Process` binding before createdump ever ran), the invocation no longer uses `-Wait` but polls the returned process against a new `-Step2DeadlineSeconds 120` deadline, terminates the owned createdump process on expiry and continues to the summary, the dump path is passed via createdump's supported `-f`/`--name` option (no `-o` option exists), the PID argument is dropped because the installed createdump 10.0.12 rejects it (live-verified: "The pid argument is no longer supported", exit -1; per dotnet/runtime createdumpmain.cpp — see src/coreclr/debug/createdump/ in the dotnet/runtime repository — createdump writes a dump of its parent process and a target PID cannot be specified), and the helper teardown moved into a `finally` so terminating failures from the dump probe still execute it before propagating. The new invocation shape was smoke-tested live (`createdump -f <path> -n` with redirected output → exit 0, dump written, empty stderr). (6) `docs/STATUS.md`: the historical R1.2 evidence-asymmetry note moved out of "Open questions for human" (now consistent with its `- _none yet_` state) into the status history at the end of this section, content preserved. Gates: `dotnet test tests/GanttCreator.Architecture.Tests` 55/55 PASS; `scripts/test-scripts.ps1` Pester 118/118 PASS (ci-parity 18/18, diagnose-l12 8/8); PSSA gate clean on all modified scripts; `git diff --check` clean; `pwsh ./scripts/verify-quick.ps1` PASS (see next commit's record). 
- **fix: review-finding sweep (gitignore diagnostic-tooling rules, boundary log wording, scanner directive-comment masking)** — Three verified findings fixed, one skipped. (1) `.gitignore` no longer ignores the L12 createdump diagnostic tool sources (`scripts/diagnose-l12-createdump.ps1`, `scripts/diagnose-l12-createdump.Tests.ps1`) so both are trackable; the diagnostic-tooling section now ignores only the local L12 diagnostic directory (generated, ignored, and not committed) (also covered by the pre-existing generated-artifact ignore rule; `git check-ignore` verified both scripts unignored and the output directory still ignored). (2) `CommandErrorTranslator` user footer reworded to conditional wording ("Technical details, when available, are recorded in the diagnostics log (see the Diagnostics button).") — the log may be absent or the record write may fail, so the message no longer asserts a diagnostics record always exists. (3) `EnvVarGuardScanner.CollectMaskSpans` now enumerates `root.DescendantTokens(descendIntoTrivia: true)` so a trailing comment on an `#if`/`#elif` directive line (structured trivia) is masked before `DirectiveRegex`/`CollectReferencedPreprocessorSymbols` process it; without it such a comment produced a spurious "malformed preprocessor expression" finding plus cascading false positives. New regression test `Scanner_ignores_call_mention_in_trailing_comment_on_if_directive` in `tests/GanttCreator.Architecture.Tests/DebugHookGuardTests.cs`, observed RED before the fix (malformed-expression + cascading false findings) and GREEN after. Skipped finding: the proposal to loop over project paths in `scripts/test-non-office.ps1` is stale — the current single-solution invocation is the deliberate fix in commit `536d20d` for the `b54ccbd` MSB1008 regression, and the W14 tripwire in `scripts/ci-parity.Tests.ps1` asserts exactly one target per `dotnet test` line ("hardcoding project lists is what enabled the W14 drift"). Gates: `dotnet test tests/GanttCreator.Architecture.Tests` 55/55 PASS; `dotnet test tests/GanttCreator.AddIn.Tests` 86/86 PASS; `git diff --check` clean; `verify-quick.ps1` "skill tree in sync" step FAILs on the pre-existing uncommitted `docs/STATUS.md` dirty-tree guard (clears on next commit); `verify.ps1` not run — it refuses on a dirty tree.


- **fix: mask literals and comments inside preprocessor regions inactive in both scanner parses (`EnvVarGuardScanner`)** — Review finding verified against the tree and reproduced: a region guarded by a symbol other than `DEBUG` (e.g. `#if FEATURE`) is inactive in both mask parses (no symbols / DEBUG defined), so Roslyn reports its contents as skipped tokens inside structured trivia; `DescendantTokens()` never enumerates them, leaving comments/string literals inside such regions unmasked — a call mention in such a comment produced a false finding and a directive-lookalike inside such a literal produced a spurious "unterminated #if". `MaskCommentsAndStrings` now extracts every symbol referenced by the source's `#if`/`#elif` conditions (new `CollectReferencedPreprocessorSymbols` + `IdentifierRegex`, excluding `defined`/`true`/`false`) and merges a third `CSharpParseOptions` pass with all of those symbols (plus `DEBUG`) defined, so those regions become active and their literal/comment spans are collected and merged. Two new regression tests in `tests/GanttCreator.Architecture.Tests/DebugHookGuardTests.cs` (comment mention and raw-string directive-lookalike inside a doubly-inactive region); both observed RED before the fix and GREEN after. Known residual limitation (documented, unchanged behaviour): a condition mixing defined and undefined non-DEBUG symbols (e.g. `#if FOO && !BAR`) is still inactive in all three parses; full coverage would require 2^n parses. Gates: `dotnet test tests/GanttCreator.Architecture.Tests` 54/54 PASS; `pwsh ./scripts/verify-quick.ps1` PASS in 152.4 s (all steps); `git diff --check` clean. Uncommitted (working tree on `stage-inspect`); `verify.ps1` not run — it refuses on a dirty tree.


- **fix: restore single-solution `dotnet test` in test-non-office.ps1; add W14 tripwire** — Commit `b54ccbd` replaced the solution-level `dotnet test $Solution` with five `.csproj` paths, but `dotnet test` accepts exactly ONE project/solution argument, so MSBuild failed with MSB1008 ("Only one project can be specified") and the CI "Test (OfficeIntegration excluded)" step went red on every run. `scripts/test-non-office.ps1` is restored to `dotnet test $Solution -c $Configuration --no-build --no-restore --filter 'Category!=OfficeIntegration'` (Office.IntegrationTests excluded by the xUnit `Category` trait; every IntegrationTests fact is tagged). New W14 Pester tripwire in `scripts/ci-parity.Tests.ps1` (`dotnet test entry points pass exactly one project or solution`): every `scripts/*.ps1` `dotnet test` invocation must pass exactly one project/solution target token; positive control fires on the b54ccbd multiple-project form and on a zero-target form; negative control accepts the single-solution form; plus a `-Solution` parameter-preservation assertion (verify-quick parity). Local-only hygiene: scripts/diagnose-l12-createdump.ps1 (gitignored/untracked, so invisible to CI and deliberately not backticked here because check-status verifies backticked paths exist in the checkout) carried a non-ASCII em-dash without BOM that failed the local PSSA gate but not CI's; replaced with ASCII. Gates: `scripts/test-scripts.ps1` 118/118 PASS; `pwsh ./scripts/verify-quick.ps1` PASS in 142.0 s (all steps); `git diff --check` clean. Checklist: G.

- **fix: extend env-var guard scanner to multiline and bare calls** — `EnvVarGuardScanner` now scans the full masked source text, preserves newlines, and maps each match back to its source line; the regex also accepts bare `GetEnvironmentVariable` calls. `IsUnsatisfiableWithDebugFalse` behavior is unchanged. Tests: `dotnet test` Architecture 52/52 PASS; `dotnet build` 0 warnings/0 errors; `git diff --check` clean.

- **fix: harden R1.4 command boundary review findings (neutral wording, in-boundary name resolution, modal-free tests, env restore, wider scanner)** — `CommandBoundary.Translator` generic footer is now neutral (`Please inspect the workbook and try again`); `cancelled` wording only in the `OperationCanceledException` branch. `CommandBoundary` gains a `Run(Func<string>, Action, string fallback)` overload whose resolver runs inside the boundary and degrades throw/blank to `OnDiagnosticsClick`; the string overload delegates to it. `GanttRibbon.OnDiagnosticsClick` resolves via that overload. `CommandBoundaryTests.CreateBoundary` defaults to a no-op presenter so failure-path tests no longer invoke the production modal. `DebugForceFailureTests` captures/restores the prior env value in every `finally`. `EnvVarGuardScanner` detects `GetEnvironmentVariable(s)`/`ExpandEnvironmentVariables` with optional whitespace around the dot via per-line regex. New positive tests: resolver/validation facts, ribbon probe-failure fallback, scanner variant fixtures. Gates: `dotnet format --verify-no-changes --exclude tests` exit 0; Release build 0/0; `test-non-office` Core 83, AddIn 80, Architecture 44, Raster 41, Office.Contract 4 PASS; `git diff --check` clean. Committed as `aa116f8`.
- **feat: add command error boundary with operation IDs and user-safe messages (R1.4)** — One command boundary (`src/GanttCreator.AddIn/CommandBoundary.cs`) for every Ribbon callback. A thrown command produces exactly one rolling-log record (`CommandError: op=GC-<timestamp>-<n> command=<id> stage=ribbon-callback excel-version=<x|unknown> exception=<type> hresult=0x<hex> detail=<redacted>`) and exactly one user-safe dialog (concise category sentence + operation ID + log pointer; never exception text, stack, or paths). The boundary is failure-proof (log-write and dialog failures degrade, never propagate into Excel). Operation IDs are deterministic (injected `TimeProvider` + monotonic counter, invariant culture, UTC). `CommandErrorTranslator` is a pure category map (COM/SEH, permission, I/O, cancellation, generic); `CommandErrorDialog` uses the managed WinForms `TaskDialog` (Error icon) with a MessageBox fallback (ADR-0006 pattern, no P/Invoke). `OnDiagnosticsClick` now routes through the boundary; its local CA1031 catch and the superseded `DiagnosticsService.WriteDiagnosticsError` are removed (one error-writing path). `AddInHost` wires/resets the boundary alongside `DiagnosticsService`. A committed `#if DEBUG` force-failure hook (`DebugForceFailure`, `GANTTCREATOR_FORCE_COMMAND_FAILURE=<commandId>`) is compiled out of Release and pinned by a directive-aware `EnvVarGuardScanner` that masks comments and string/char literals, models the `#if`/`#elif`/`#else`/`#endif` branch stack, and accepts a read only when the conjunction of enclosing conditions is unsatisfiable with `DEBUG` unset (so `DEBUG || FEATURE` is rejected, `DEBUG && FEATURE` accepted); it scans the whole `src/` tree (excluding `bin`/`obj`/generated/Designer sources) so a stray read anywhere is caught. Cancellation wording in `CommandBoundary.Translator` updated. Tests: `CommandBoundaryTests`, `DebugForceFailureTests`, `GanttRibbonTests`, and the rewritten `DebugHookGuardTests` (repo-wide positive control + positive/negative/structure fixtures). Gates: `dotnet build` 0 warnings/0 errors; `dotnet test` AddIn 78/78, Architecture 40/40 PASS; `dotnet format --verify-no-changes --exclude tests` exit 0; `git diff --check` clean.


- **fix: enable Windows targeting on non-Windows restore (NETSDK1100)** — The external CI run that restored the solution on a non-Windows host failed because `net10.0-windows` projects lacked `EnableWindowsTargeting`; `GanttCreator.AddIn` and `GanttCreator.AddIn.Tests` were the first observed failures, and the same restore failure applies to the other five Windows-targeting projects. Added `<EnableWindowsTargeting>true</EnableWindowsTargeting>` to all seven `-windows` projects (`src/GanttCreator.AddIn`, `src/GanttCreator.Office`, `src/GanttCreator.Raster`, `tests/GanttCreator.AddIn.Tests`, `tests/GanttCreator.Office.ContractTests`, `tests/GanttCreator.Office.IntegrationTests`, `tests/GanttCreator.Raster.Tests`) with a NETSDK1100 comment. Added `tests/GanttCreator.Architecture.Tests/WindowsTargetingTests.cs`, a repo-pattern mirror of `NoWarnScopeTests`, that pins the discovered `-windows` project set and asserts each declares `EnableWindowsTargeting` exactly once. Gates: `dotnet build GanttCreator.slnx -c Release` 0 warnings/0 errors; `scripts/test-non-office.ps1` Architecture 20/20, Office.Contract 4/4, Raster 41/41, AddIn 53/53, Core 83/83 PASS; `dotnet format --verify-no-changes --exclude tests` exit 0; `git diff --check` clean. Committed as `abcb58d`.


- **stage-inspect review-fix batch (`0f30d98`)** — four findings verified against
  the tree before fixing. (1) The CRLF-hygiene entry's root cause is corrected:
  `core.autocrlf` affects only paths whose attributes do not specify an eol
  policy (the previously uncovered `*.resx` files); it does not override the
  explicit `text eol=lf` rules — remediation and verification details preserved,
  and the working copy of this file normalized to LF so the zero-`wcr` claim
  holds locally. (2) `docs/work-items/R1.3-diagnostics-command.md` Scope,
  Decision, Risk, and Notes no longer describe comctl32 P/Invoke,
  `GetDesktopWindow`, or `TDN_HYPERLINK` as the active design — they document
  the managed WinForms `System.Windows.Forms.TaskDialog` and its MessageBox
  fallback (the addendum's historical evidence is preserved). (3)
  `DiagnosticsService` no longer strips the anchor href out of the content for
  the MessageBox fallback; it builds dedicated plain text via new
  `BuildFallbackText` that includes `LogFilePath`, so the fallback shows the
  actual log path (regression test updated to assert the path is present).
  (4) `RollingLog` normalizes a relative log directory during construction and
  reuses the stored full path, so `LogFilePath` is always absolute, which
  `DiagnosticsService.BuildContent` requires for its `file://` URI (new
  positive test). Gates: `dotnet test tests/GanttCreator.Core.Tests` 83/83
  PASS; `dotnet test tests/GanttCreator.AddIn.Tests` 53/53 PASS; Architecture
  19/19 PASS; `dotnet format --verify-no-changes --exclude tests` exit 0;
  `git diff --check` clean. Committed as `0f30d98`.
- **fix: CRLF line-ending hygiene — `core.autocrlf=true` affected only paths without an explicit eol policy** — The repo had `core.autocrlf=true` set at the local level while `.gitattributes` mandates `text eol=lf` for `*.cs` and friends. Autocrlf does not override those explicit eol rules; it affected only the paths whose attributes did not specify an eol policy, namely the previously uncovered `*.resx` files, which were checked out with CRLF in the working tree while the index stored LF: `git diff` (which normalises) showed empty, but `git status` flagged those files as modified. Fixed by setting `core.autocrlf=false` and `core.eol=lf` in the local config (`.gitattributes` is now authoritative going forward) and running `git add --renormalize .`, which re-staged `src/GanttCreator.AddIn/RibbonResources.resx` (67 lines, CRLF→LF). That file was the only one affected because `*.resx` was not covered by any attribute rule, so it had been left in raw CRLF; the rule `*.resx text eol=lf` was added to `.gitattributes` so the gap is closed. Verified: `git ls-files --eol` reports zero `wcr` entries across the whole tree, `git diff --check` is clean, `git status` shows only the staged `.gitattributes` change. Gates: `pwsh ./scripts/pre-commit.ps1` → PASS (skill tree in sync, status accuracy 68 hashes/67 paths/13 roadmap IDs, markdown link sanity 38 files). commit `402f305`.
- **fix: verify-office sweep requires positive parentage, not mere absence from the pre-run snapshot** — `Remove-HarnessOwnedOfficeProcesses` in `scripts/verify-office.ps1` previously killed every `EXCEL`/`POWERPNT` PID that appeared during the run but was absent from the before-snapshot, which could kill a user-owned Office process the user started while the tests were running. It now takes a second `OwnedTreePids` array (the children of the owned `dotnet test` launcher, captured via `Get-CimInstance Win32_Process` before `taskkill` runs) and kills an Office process only if it is not in the before-snapshot **and** its `ParentProcessId` is in the owned tree; an Office process whose parent cannot be determined is skipped, never killed. `ShouldProcess` confirmation and `taskkill /PID /T /F` by PID are preserved. `.DESCRIPTION` step 4 updated; two new Pester cases (`sweep requires a positive parentage test`, `sweep refuses to kill when the parent cannot be identified`) added to `scripts/verify-office.Tests.ps1`. Gates: `scripts/verify-office.Tests.ps1` 13/13 PASS; PSSA 0 findings on all modified scripts; `git diff --check` clean; **live `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 600` → 8/8 PASS, exit 0, no orphan, and the pre-existing user-owned EXCEL.EXE (PID 25544, parent `explorer.exe`) was left running — the sweep logged `Skipping Office process PID 25544 (parent 13596 is not in the owned dotnet-test tree)` rather than killing it.** **`L13` in `docs/KNOWN-LIMITATIONS.md` is now closed by the fixture-to-shell PID handoff** (`OfficeFixture` records its owned Excel PID into `$env:GANTTCREATOR_OWNED_PIDS_PATH`, read by the sweep as the primary ownership signal; parentage is only the fallback). Residual parentage-fallback exposure recorded as `L14`. commit `402f305`.

- **R1.2 — Add minimal valid RibbonX resource with a Gantt Creator tab** — `GanttRibbon : ExcelRibbon` added to `src/GanttCreator.AddIn/`; returns a minimal RibbonX document with one Gantt Creator tab and an `onLoad` callback. Excel-DNA auto-registers the class (verified by decompiling the installed 1.9.0 `ExcelDna.Integration` assembly: `IsRibbonType` + `GetExcelAddIns` + `LoadCustomUI`), so no `.dna` change. The RibbonX document is stored in `src/GanttCreator.AddIn/RibbonResources.resx` as a `ResXFileRef` to `src/GanttCreator.AddIn/RibbonResources/Ribbon.xml`, read through the committed `src/GanttCreator.AddIn/RibbonResources.Designer.cs` accessor; `src/GanttCreator.AddIn/GanttCreator.AddIn.csproj` declares `NeutralLanguage` and resource generator metadata, and `docs/02-ARCHITECTURE.md` records the embedded-resource standard. 7 contract tests in `tests/GanttCreator.AddIn.Tests/GanttRibbonTests.cs`. Commits `656ad13`, `3f6a6c5`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 30/30 PASS; full non-office suite 176/176 PASS; Release build 0/0; packed-XLL publish embeds the new assembly; `pwsh ./scripts/verify-quick.ps1` → PASS. Office gate (Visual Studio/Excel, minimal ribbon visible, no ribbon errors): **Successful R1.2 implementation (user-reported verification)** — the implementation was edited in Visual Studio, the minimal ribbon was created and displayed in Excel, and no bugs were found. The exact edited filename and Visual Studio/Excel versions were not supplied.

- **R1.1 — Excel-DNA 1.9 entry point with `AutoOpen`/`AutoClose` logging** — `AddInHost`, `AddInIdentity`, `AddInLifecycle`, `AddInLogFactory`; log location `%LOCALAPPDATA%\GanttCreator\logs` with temp fallback. Contract tests in `tests/GanttCreator.AddIn.Tests/`. Commits `1515b7b`, `9fadae4`, `82e80f5`, and earlier. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 23/23 PASS; Architecture 19/19; `pwsh ./scripts/verify-quick.ps1` → PASS. Office gate (F5 x64 XLL, AutoOpen/AutoClose breakpoints): Not run — requires Visual Studio against desktop Excel.

- **fix: update AddInLogFactory with IsFailed fallback and isolated temp test directories** — `AddInLogFactory.Create()` now retains the RollingLog from `LocalApplicationData`, checks `IsFailed`, disposes it, and creates a temp fallback when initialization fails; preserves direct temp-path behavior when `LocalApplicationData` is empty. `AddInLogFactoryTests` use isolated `Directory.CreateTempSubdirectory()` roots instead of the shared `Path.GetTempPath()/GanttCreator/logs` directory. `docs/work-items/R1.1-exceldna-entry-point.md` updated so `verify.ps1` is described as running after the last commit with a clean working tree. Commit `1515b7b`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 23/23 PASS, Release build green, pre-commit hooks pass.

- **fix: anchor verify.ps1 dirty-tree check to repo root with git -C** — The 'working tree hygiene' step in `scripts/verify.ps1` used `git status --short` which resolved against the caller's CWD; running verify.ps1 from a subdirectory could report a clean tree while the repo root was dirty. Changed to `git -C (Split-Path -Parent $scriptRoot) status --short` to anchor the check to the repository root regardless of CWD. No contract change — the clean-tree requirement is identical. Commit `9fadae4`. Gates: pre-commit hook passes (skill tree in sync, status accuracy, markdown link sanity).

- **test: serialize temp-fallback log tests to avoid concurrent file collisions** — The two TempLogFallbackTests in `tests/GanttCreator.AddIn.Tests/AddInLogFactoryTests.cs` share the same log file under `Path.GetTempPath()`; running them in parallel risked write collisions. Moved them into a dedicated `[Collection("temp-log-fallback")]` so xUnit runs them sequentially. Also added the missing `using Xunit;` import. Commit `82e80f5`. Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 23/23 PASS (109 ms).

- **Two-gate sequence and human-gated branch lifecycle documented** — `docs/05-GIT-QUALITY.md` § "Local gates" no longer says "Before every commit/PR: verify.ps1" (a doc/code contradiction that caused the R1.1 session to burn a full pre-commit `verify.ps1` run on a staged tree): the section now records the two-gate contract — `verify-quick.ps1` is the every-commit gate run after staging (accepts staged changes; validates the staged index), `verify.ps1` is the branch-final gate run once after the last commit (clean tree required, fail-fast step 1). § "Branch and review policy" gains the human-gated lifecycle rule: the agent never pushes and never opens a PR unasked; the agent's branch-final self-certification is a green `verify.ps1` after its last commit; the CodeRabbit review stage runs on the pushed branch; the human creates the PR once satisfied. `docs/work-items/TEMPLATE.md` evidence commands re-annotated ("accepts staged changes" / "branch-final, after the LAST commit — requires a clean tree"); `docs/06-LLM-PROTOCOL.md` pre-flight checklist updated so every agent session starts knowing the sequence. Skill views regenerated via `scripts/sync-cline-skills.ps1`. Gates: `pwsh ./scripts/verify-quick.ps1` → PASS (see commit); branch-final `verify.ps1` → PASS on the clean tree (see commit).

- **verify.ps1 hygiene step moved to first** — The 'working tree hygiene' step in `scripts/verify.ps1` now runs as step 1 (was step 14), so a dirty tree fails in ~0.1 s instead of after the ~2-minute suite (cost paid during the R1.1 session when a pre-commit `verify.ps1` run burned the full suite before failing on the dirty-tree check). No contract change: the clean-tree requirement is identical, only its position — `verify.ps1` remains the branch-final gate run after the last commit, while `verify-quick.ps1` stays the per-commit gate that accepts staged changes. `scripts/step-parity.Tests.ps1` (parses `.DESCRIPTION` labels against `Invoke-Step` names at runtime) passes unchanged once the description enumeration matched the new order. Gates observed: step-parity 11/11 PASS; live `verify.ps1` on a dirty tree → step 1 FAIL in 0.1 s (positive demonstration); `pwsh ./scripts/verify-quick.ps1` → blocked by `check-cline-skills` because `docs/KNOWN-LIMITATIONS.md` and `docs/STATUS.md` are intentionally uncommitted.

- **L12 closed: verify-office watchdog replaces --blame-hang-timeout** — `scripts/verify-office.ps1` no longer passes the blame-hang collector; the `$DeadlineSeconds` deadline is enforced by an external watchdog (`Start-Process -PassThru` + `Stopwatch` poll every 5 s; soft timeout kills the owned `dotnet test` process tree with `taskkill /PID /T /F` and re-runs the harness-owned Office-process sweep as a safety net). New `scripts/verify-office.Tests.ps1` (auto-discovered by `test-scripts.ps1`): no-`--blame-hang-timeout` assertion with positive control, plus isolated harness cases (hanging stub `dotnet` → exit 124 + `TIMEOUT`; prompt stub → exit 0 + PASS). `KNOWN-LIMITATIONS.md` L12 marked `_closed_`; `STATUS.md` Known limitations mirrors. Gates: `scripts/verify-office.ps1` 5/5 PASS (27.8 s, no orphan); `scripts/verify-office.Tests.ps1` 10/10 PASS; PSSA 0 findings on both modified scripts; `verify-quick.ps1` blocked by `check-cline-skills` because `docs/KNOWN-LIMITATIONS.md` and `docs/STATUS.md` are intentionally uncommitted.

- **CA2007 narrowed to affected test code, Office fixture green** — Removed the project-wide `CA2007` entry from `tests/Directory.Build.props` `NoWarn` (now only `CA1707;IDE0011`); the comment records the approved 2026-09-11 exception — test/fixture awaits use `ConfigureAwait(true)`, satisfying both CA2007 and xUnit1030 with no precedence declared — and directs future awaits to file-/member-scoped suppressions with written justification. Scoped the affected code instead: `OfficeFixtureTests.cs` disposal rewritten from `await using` (which CA2007 cannot see a `ConfigureAwait` on) to explicit `try/finally` + `await DisposeAsync().ConfigureAwait(true)`; `OfficeFixture.cs` `PollForProcessExitAsync` await gained `ConfigureAwait(true)`; the two general `catch (Exception)` aggregation points in `DisposeAsync` and the guarded `Quit()` cleanup in `InitializeAsync` carry `#pragma warning disable CA1031` with written justification (first-error aggregation, rethrown via `ExceptionDispatchInfo` / original error preserved). Also removed the never-read `quitAttempted`/`quitSucceeded`/`releaseAttempted` bools (CS0219). STATUS.md history note: the "Office fixture + L3 re-date" entry's "suppressed CA2007 ... conflicting with CA2007's library-code ConfigureAwait(false)" wording is superseded — CA2007 is now enforced in tests, no conflict claimed. Gates: `dotnet build -c Release GanttCreator.slnx` 0 warnings 0 errors; `dotnet build -c Debug` on the Office integration project 0/0; Architecture 19/19; Core + AddIn Release builds 0/0. Office-host proof (2026-09-11, this machine's O365 x64 install, blame collector omitted per L12): `dotnet test -c Release tests/GanttCreator.Office.IntegrationTests` 5/5 PASS (23 s), no orphan — the `try/finally + ConfigureAwait(true)` disposal timing is confirmed equivalent on live Excel, risk retired. `scripts/test-scripts.ps1` / `verify-quick.ps1`: Not run (no `pwsh` on the tool PATH in this session).
- **actionlint pin single-sourced in ci.yml** — The "Workflow lint (actionlint)" step in `.github/workflows/ci.yml` hardcoded `$version = '1.7.7'` and a literal SHA-256 (`7f12f180…`), duplicating `scripts/tool-versions.psd1#actionlint` (W11 drift surface). Now the step imports the psd1 and derives both `$version = $versions.actionlint.Version` and `$expectedHash = $versions.actionlint.Sha256` at runtime, and builds the URL from `$versions.actionlint.DownloadUrl -f $version` instead of a hardcoded string. The unused `$expectedHash = $versions.actionlint.Sha256` was also removed from the PSScriptAnalyzer step (it assigned a value nothing in that scope consumed). The two W11 tripwires that asserted a literal mirror of the hash in ci.yml (`ci-parity.Tests.ps1` "actionlint SHA-256 in ci.yml equals…", `lint-ci.Tests.ps1` "ci.yml mirrors the same actionlint pin") were rewritten to assert the import + property reference instead — the same indirection as the existing PSScriptAnalyzer test — since a literal mirror is no longer present to compare. Both tripwires keep their step-scoped extraction so the import in the PSScriptAnalyzer step cannot satisfy them. Gates: `scripts/test-scripts.ps1` 88/88 PASS (Pester 6.1.0); PSSA 0 findings over all three changed files; `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (329.3 s, incl. the actionlint download+SHA-256 step re-verified at runtime); `git diff --check` clean.
- **check-status path predicate extracted to shared helper** — The repo-path shape predicate (separator + extension-like suffix, no whitespace/glob/URL/dash noise) and the wildcard-metacharacter rejection that followed it were inlined twice: once in the validation loop and again as an inline `Where-Object` filter in the `check-status: OK` summary count. Extracted both into one function, `Get-RepoPathTokenStatus`, returning `IsPath`/`HasWildcard`, so the loop and the verified-path count share a single definition. The count now excludes wildcard-rejected tokens (`IsPath -and -not HasWildcard`), so a rejected token can never be reported as verified. Added two positive Pester cases in `scripts/check-status.Tests.ps1`: a path-shaped token carrying `?`/`[]` exits 1 with the documented message, and a non-path token such as `[Fact]` exits 0 (the wildcard rule must not fire on attribute annotations). Gates: `scripts/test-scripts.ps1` 88/88 PASS (Pester 6.1.0); PSSA 0 findings over both changed files; `pwsh ./scripts/check-status.ps1` → `check-status: OK (63 hashes, 58 paths, 10 roadmap IDs verified)` — identical to the pre-refactor baseline; `git diff --check` clean.

- **pre-commit verify-quick source-text assertion hardened** — Replaced the invocation-pattern regex (`(pwsh|\$\(|&)\s?.*verify-quick\.ps1`) in `scripts/pre-commit.Tests.ps1` with a direct filename match after stripping all comments (XML doc block + line comments). The old regex could miss multi-line invocations (`.*` does not cross newlines), multiple-space invocations (`\s?` allows only one), and non-standard invocation patterns. Added two positive-control tests: one verifying the check catches direct, wrapped, and multi-line invocations; another verifying the filename is allowed only in comment/documentation contexts. Gates: `scripts/pre-commit.Tests.ps1` 16/16 PASS (Pester 6.1.0); PSSA 0 findings; skill-tree drift in sync; `git diff --check` clean.

- **lint-ci single-file splat fix (`d1eb20f`)** — `scripts/lint-ci.ps1` collected workflow files as a scalar `String` when only one `*.yml` exists, so `& actionlint @workflowFiles` splatted characters (`could not read "C"` / `":"`) and failed `verify-quick.ps1` step 5 while direct actionlint on `ci.yml` passed. Fix: coerce to `[object[]]@(...) | Sort-Object -Unique`; added a Pester positive test in `scripts/lint-ci.Tests.ps1`. Gates: `verify-quick.ps1` 12/12 PASS (128.5 s); Pester 84/84; `git diff --check` clean. Checklist: G.
- **Office integration serialization enforced** — Verified the stage-inspect finding: the `docs/04-TEST-STRATEGY.md` harness rule "serialize all tests in one collection" had zero mechanical enforcement — no `xunit.runner.json`, no `[Collection]`/`[CollectionDefinition]`/`DisableParallelization`, no parallelization switch in `scripts/verify-office.ps1` — and the trigger condition the finding predicted had already materialised: `OfficeEnvironmentTests` is a second Office integration class beside `OfficeFixtureTests`, so xUnit 2.9.3's default per-test-class collections run both concurrently. Fix: added `tests/GanttCreator.Office.IntegrationTests/xunit.runner.json` with `parallelizeAssembly`/`parallelizeTestCollections` = `false` and wired `CopyToOutputDirectory=PreserveNewest` in `tests/GanttCreator.Office.IntegrationTests/GanttCreator.Office.IntegrationTests.csproj` (per xunit.net/config-xunit-runner-json). Gates: Release build `-warnaserror` clean (0 warnings); `xunit.runner.json` copied to the test output directory; OfficeIntegration suite 5/5 PASS via plain `dotnet test` on this host's O365 x64 install. Observed pre-existing defect (recorded, not introduced by this change): the `--blame-hang-timeout` collector used by `verify-office.ps1` aborts the testhost ~0.2 s after start via `createdump`/hang dump, and that crash reproduces identically in the pre-fix state — the suite passes only with the blame collector omitted on this machine (see `docs/KNOWN-LIMITATIONS.md` L12).
- **RollingLog constructor guard tests** — Positive tests added in `tests/GanttCreator.Core.Tests/RollingLogTests.cs` for the constructor guards in `src/GanttCreator.Core/Logging/RollingLog.cs`: whitespace-only `logDirectory` → `ArgumentException` with `ParamName` `logDirectory`; `maxFileSizeBytes` 0/−1 and `maxFileCount` 0/−1 → `ArgumentOutOfRangeException` with their respective `ParamName`s. No production change. Gates: RollingLogTests 30/30 PASS (net10.0); `pwsh ./scripts/verify-quick.ps1` → 12/12 PASS (116.0 s, 2026-09-11); `git diff --check` clean.

- **stage-inspect review-fix batch** — Thirteen findings from an external review, each verified against the current tree before fixing. Skill texts: `docs/01-ENVIRONMENT.md` and `docs/06-LLM-PROTOCOL.md` no longer reference a "table below" that the skill view does not contain, nor an absolute no-retry rule — they point at the canonical baseline doc and AGENTS.md's single recorded-retry exception for documented flaky external Office operations (skills regenerated via `scripts/sync-cline-skills.ps1`; Phase-2 byte-compare green). `.github/copilot-instructions.md` now stops-and-reports on instruction conflicts instead of silently defaulting to "the kit" and now mirrors AGENTS.md's Tool routing table. `docs/evidence/script-test-harness-refactor.md` drops its Suggested-follow-up and residual-risk reference for the deleted `check-skill-summary` gate. Scripts: `check-status.ps1` recognises POSIX-style `../` parent traversal via a separator-normalised relative path (new Pester case); `check-cline-skills.ps1` captures and emits the sync's output on failure instead of `Out-Null` (new Pester case); `install-pre-commit.ps1` chmod +x the hook shim on POSIX hosts behind `!$IsWindows`; `test-locked-restore.Tests.ps1` clears inherited `NUGET_PACKAGES` in the replay harness; `lint-ci.Tests.ps1` source-shape assertion is newline-tolerant (`(?s)`); `pre-commit.Tests.ps1` short-circuit comment and install-pre-commit assertions corrected. C#: `AddInAssemblyTests` and `CoreBoundaryTests` resolve the active build configuration from the test output path and require the artifact to exist in the candidate configuration directory, so a Debug `dotnet test` is no longer satisfied by a stale Release DLL. Gates: Pester 82/82, PSSA 0 findings, AddIn.Tests 3/3 Debug + 3/3 Release, Architecture.Tests 17/17 Debug + 17/17 Release, `check-status`/`check-md-links`/`git diff --check` clean.

- **SKILL-TOOLS bridge: Cline skills now hooked to MCP tools** — Every canonical `docs/0N-*.md` gained a mandatory `SKILL-TOOLS` block extracted verbatim into `.cline/skills/*/SKILL.md`, so each skill names the MCP tools that execute its steps. `scripts/sync-cline-skills.ps1` now hard-fails on a mapped doc missing a non-empty block (two new positive Pester cases in `scripts/sync-cline-skills.Tests.ps1`, 7/7); `scripts/check-cline-skills.Tests.ps1` fixtures updated (4/4). Added a lean tool-routing matrix to `AGENTS.md` (+17 lines, always-on), pre-flight tooling checklists to `docs/04-TEST-STRATEGY.md`, `docs/05-GIT-QUALITY.md`, and `docs/06-LLM-PROTOCOL.md`, wired `memra__*` into the evidence-ledger persistence rules in `docs/06-LLM-PROTOCOL.md`, and replaced its generic anti-hallucination lookup steps with a concrete highest-fidelity tool order (LSP → `ilspy_decompile` → context7 / Microsoft Learn → `dotnet_build` → spike). `docs/clinerules/SYNC.md` documents the new block and sync rules. Gates: `scripts/test-scripts.ps1` 79/79, PSSA 0 findings, `git diff --check` clean.

- **R0.8 — Versioning and local rolling-log abstractions (CI GREEN)** — Production code committed (`8206037`, `0fc89e5`): `VersionInfo.cs`, `IRedactor.cs`, `Redactor.cs`, `IRollingLog.cs`, `RollingLog.cs` plus tests. L6 closed via `lint-ci.ps1` + actionlint; L7 closed via `test-scripts.ps1` + Pester. QA hardening rounds added the Phase-2 drift gate, verify parity, pre-commit hook, PSSA gate fix, RollingLog failure paths, script-test harness refactor, and cap-1 rotation branch. **CI gate: green** — run #40 (`adbded8`) concluded `success` at `https://github.com/Gekko47/gantt-creator/actions/runs/34576783029`. L10 and L11 closed on this same green run. R0.8 exit demonstration met: clean Release build and test report without Office.

- **L3, L8, L9 closed + Pester 6+ standard** — L3 (Office fixture): `OfficeFixture` harness exists, `OfficeFixtureTests.Excel_open_close_five_times_no_orphan` passes (5/5). L8 (docstring coverage): documented decision, not a gap. L9 (Pester version): decision (2026-09-11, human) — Pester 6+ is the standard across all docs and CI; `tool-versions.psd1` pins `MinimumMajor = 6`; version drift tolerated as long as both dev/CI are 6+; version-safe counting (`TotalCount`/`PassedCount`/`FailedCount`) is the permanent contract. Updated references in `docs/06-LLM-PROTOCOL.md`, `docs/05-GIT-QUALITY.md`, `.github/workflows/ci.yml` from "Pester 5" to "Pester 6+".

- **Office fixture + L3 re-date (pending commit)** — Added `OfficeFixture.cs` to `tests/GanttCreator.Office.IntegrationTests`: an `IAsyncLifetime` harness that launches a real Excel Application (Visible=false, DisplayAlerts=false, ScreenUpdating=false), captures the new EXCEL.EXE process ID by diffing the process list before/after launch, and on teardown closes all workbooks without saving, calls `Quit()`, releases each COM proxy through `Marshal.ReleaseComObject` (one release per proxy, never chained), forces a GC pass, and polls the owned process ID until exit or a 10 s deadline. `OfficeFixtureTests.cs` proves the lifecycle: launch reports a non-zero PID, single teardown leaves no orphan, and the open/close five-times cycle (R1.6 gate) leaves no orphan. Added `ExcelDna.Interop` package reference to the IntegrationTests project for COM types; suppressed CA2007 in `tests/Directory.Build.props` (xUnit1030 requires `ConfigureAwait(true)` in test methods, conflicting with CA2007's library-code `ConfigureAwait(false)`). L3 in `KNOWN-LIMITATIONS.md` re-dated from R0.5 to R1.6; STATUS.md L3 text reconciled with the canonical limitation doc. Non-Office: Core 75/75, Raster 42/42, Architecture 17/17, AddIn 3/3, Office Contract 4/4. Office Integration (self-hosted, real Excel): 5/5.

- **Retroactive R0.2 and R0.6 work-item files (commits `107ab51`, `e20de24`)** — The roadmap rule requires a work-item file before implementation. R0.2 (solution, projects, architecture test) and R0.6 (verify scripts, coverage settings) were implemented in commit `900f78c` without work-item files. Added `docs/work-items/R0.2-solution-and-project-structure.md` and `docs/work-items/R0.6-verification-scripts-and-coverage.md` retroactively to close the paper-trail gap.

- **test: remove redundant ToPixels_oversized_pixel_width_throws (pending commit)** — The test duplicated the `MaxPixelDimension` guard coverage already held by `ToPixels_width_above_limit_throws`; its comment incorrectly cited the int range rather than the 65,535 limit (verified against `ExportSize.cs:139`, where the guard is `pixelWidth > MaxPixelDimension` and the int cast happens after). Removed; Raster 41/41.

- **Review-finding closure round** — Eight review findings verified and fixed without changing shipped behaviour: `ExportSize` `sceneHeightPt` zero now rejected at the input guard matching the greater-than-zero contract; `verify-helpers.ps1` throw prefix corrected to `Read-VerifyDescriptionStepNumber`; `sync-cline-skills.ps1` empty SKILL-SUMMARY now fails with the positive Pester case; `CoreBoundaryTests` Release fallback guarded by `Directory.Exists`; `RollingLogTests` cleanup tolerates `UnauthorizedAccessException`; `STATUS.md` L11 remediation aligned to `_Maintainer_` matching `KNOWN-LIMITATIONS.md`. Evidence: Core 75/75, Raster 42/42, Architecture 17/17.

- **Duration-claim alignment + offline locked-restore proof (2026-09-10, commits `a477a97`, `d01713c`)** — Two docs/code-drift clusters from an external review. (1) `a477a97`: the verify-gate duration claim corrected in `2c1519e` had survived stale in four other copies (`.vscode/tasks.json` verify-quick and verify-full detail texts, `scripts/install-pre-commit.ps1` docblock, `scripts/pre-commit.ps1` docblock — all claiming sub-minute runtimes), and `install-pre-commit.ps1`'s docblock still listed the deleted skill-summary gate while its own Write-Host already listed the correct three. All copies aligned to the measured reality (recorded verify-quick runs: 97-333 s), `scripts/verify-quick.ps1`'s own "under ~2 minutes" header and the "~2 minutes" claims in `docs/05-GIT-QUALITY.md` / `docs/06-LLM-PROTOCOL.md` included, and the stale R0.8 sub-minute duration record in this file revised. New review-runbook tripwire fails on any sub-minute duration token on a line mentioning a verify gate (review-runbook 7/7). (2) `d01713c`: `scripts/test-locked-restore.ps1` claimed run 2 succeeded "purely from the lock file with no network access" but never denied network — both runs were plain `dotnet restore --locked-mode --no-cache`, which can reach nuget.org unnoticed. It is now a four-run proof: runs 1-2 deterministic from-clean; run 3 negative control (NuGet.config whose only source is `https://127.0.0.1:1`, a closed loopback port, plus `NUGET_PACKAGES` isolated to an empty directory) MUST fail — proving the block denies package acquisition; run 4 offline proof (same blocked source, normal cache) must succeed purely from lock files + local cache. Blocked runs pass `-p:NuGetAudit=false` (NuGetAudit fetches vulnerability data from the source and fails NU1900 Warning As Error under warnings-as-errors even when the lock file alone suffices — observed on SDK 10.0.400; plain-HTTP blocked sources are unusable, NU1302 at config validation). Harness updated: the `dotnet` stub exits non-zero exactly when `NUGET_PACKAGES` is isolated; new assertions cover the four restores, the blocked-configfile argv, and the isolated control. Evidence: real run observed — runs 1-2 PASS, run 3 failed with NU1301 connection refused as required, run 4 PASS offline, script exit 0; Pester 75/75 (`scripts/test-scripts.ps1`); `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (97.3 s, 2026-09-10, pre-`d01713c` state). R0.3 work-item acceptance wording updated to match.

- **Review-evidence hardening round (2026-09-10, commits `4617eaa`, `99df4dd`, `902cc21`, `531223a`, `a459a41`, `0983476`, `2ee5b1a`, `317d517`, `8ee33b5`)** — Thirteen verified review findings fixed across docs, scripts, and Core. (1) `RollingLog` constructor now latches `Directory.CreateDirectory` failures through `MarkFailed` instead of throwing into product code, with the positive regression test `Constructor_latches_failure_when_log_directory_path_points_to_an_existing_file` (Core 75/75). (2) `ExportSize` XML doc for `sceneHeightPt` corrected to "finite and greater than zero". (3) `pre-commit.ps1` dirty-tree guard now blocks on any git failure instead of continuing; `pre-commit.Tests.ps1` BeforeEach inits a clean git repo so stub tests exercise the real checker loop, and AfterEach removes both the BeforeEach directory and test-reassigned replacements (Pester 12/12). (4) `check-cline-skills.ps1` Phase 1 git calls run with `git -C $repoRoot` (CWD-independent; pathspecs unchanged). (5) `lint-ci.ps1` verifies the cached `actionlint.exe` SHA-256 against the new `actionlint.ExeSha256` pin in `scripts/tool-versions.psd1` before every execution — missing/mismatched cache is removed and re-downloaded with full archive+exe verification, and a fresh-download mismatch fails hard; `actionlint-hash.ps1` gained a `-FilePath` parameter set (lint-ci 7/7, ci-parity 13/13, live lint-ci PASS). (6) `test-locked-restore.Tests.ps1` derives the harness sequence from the script's parsed AST instead of a hardcoded copy, exposing and fixing a masked label mismatch (Pester 4/4). (7) `verify-quick.ps1`/`verify.ps1` default `-Solution` against the repository root, explicit paths pass through (step-parity 8/8). (8) `sync-cline-skills.ps1` now FAILS (non-zero) when a mapped doc lacks the SKILL-SUMMARY block — the truncation fallback is removed post-migration — documented in `docs/clinerules/SYNC.md`, with the positive failure-path test and drift-gate fixture updated (Pester 8/8; real sync regenerates 7 skills, no warnings). (9) Docs: `01-ENVIRONMENT.md` states the Visual Studio 2026 Community (18) baseline consistently with ADR 0003/CONTRIBUTING.md, KNOWN-LIMITATIONS L11 owner assigned (`_Maintainer_`), the evidence file's Pester 6.1.0 claim corrected to observed-local-version with the Pester 5+ contract stated, and the R0.8 work item records the current 11-step verify-quick result. PSScriptAnalyzer clean over `scripts/` after ASCII-only comment fix in `pre-commit.Tests.ps1`.

- **RollingLog UTF-8 preamble fix** — `RollingLog` wrote both `StreamWriter`s (fresh create + append reopen) with `System.Text.Encoding.UTF8`, which in .NET 10 is a BOM-emitting `UTF8Encoding` (preamble EF BB BF). `_currentFileSize` is tracked from `Encoding.UTF8.GetByteCount` and never counted that preamble, so tracked size diverged from the real file size (rotation could drift up to 3 bytes past the cap; an append reopen could inject a preamble at EOF). Fix: construct the writers with `new System.Text.UTF8Encoding(encoderShouldEmitUTF8Identifier: false)` (documented UTF-8 without preamble); byte accounting unchanged. Regression `[Fact] Log_file_tracks_utf8_byte_count_exactly_without_preamble` asserts fresh-create size equals the exact UTF-8 line byte count and stays exact after an append reopen (fails by 3 bytes pre-fix, verified by disposable spike).
- **RollingLog failure latch made volatile (pending commit)** — `RollingLog.Failed` was a manifest auto-property read lock-free by `IsFailed` (diagnostics) while every write happened under `_gate`, so a cross-thread reader had no memory-visibility guarantee for the latched flag. Replaced the auto-property with `private volatile bool _failed` and updated all six read/write sites (`IsFailed`, the two `WriteCore` guards, the `MarkFailed` write, and the `RotateIfNeeded` guard), preserving the existing locking discipline (writes under `_gate`, constructor excepted). Evidence: Core tests 73/73 PASS; `pwsh ./scripts/verify-quick.ps1` → 12/12 PASS (228.6 s, 2026-09-10).
- **Review-evidence alignment round (2026-09-09, pending commit)** — Closes a 16-finding review batch; 12 verified valid and fixed, 4 skipped (premise already fixed or false). Fixes: (1) `CONTRIBUTING.md` prerequisites name Visual Studio 2026 (18) Community as the verification IDE (ADR-0003) and the review process now requires the branch pushed + GitHub CI observed green (W-12) before merge — a local `verify.ps1` PASS alone is insufficient. (2) `Directory.Build.props` version-policy comment now points at `docs/work-items/R0.8-versioning-and-rolling-log.md`. (3) `SECURITY.md` replaces "next patch release" with the pre-release policy (fixes land on `stage-inspect`, ship with first stable release), keeping the 5-business-day acknowledgment. (4) `docs/02-ARCHITECTURE.md` requires XML documentation for every public member (CS1591 + warnings-as-errors) with extra detail for non-obvious contracts; internal-comment guidance unchanged. (5) `docs/06-LLM-PROTOCOL.md` worked evidence corrected to the verified 11-warning count recorded in `docs/STATUS.md` / `docs/KNOWN-LIMITATIONS.md` L10 (was 10), with the discrepancy reported rather than reconciled. (6) `docs/KNOWN-LIMITATIONS.md` L11 Owner column now holds `_Maintainer_`; the unrun replacement-gate status moved into the limitation text and L11 stays open until the next `stage-inspect` green CI run. (7) `docs/work-items/R0.8-versioning-and-rolling-log.md` rollback note now states the Core production files under `src/GanttCreator.Core/` are removed and only Office/add-in rendering code is untouched. (8) `scripts/ci-parity.Tests.ps1` inline-dotnet regex accepts the YAML sequence form `- run: dotnet ...` (optional dash before the run label), applied identically to the tripwire and both positive controls, with dashed-form vetted negatives added. (9) `scripts/pre-commit.ps1` resets `$LASTEXITCODE` immediately before each checker invocation so a cmdlet-only checker cannot inherit a stale exit code. (10) `scripts/verify.ps1` coverage-threshold step now parses `coverage.cobertura.xml` and prints measured line/branch rates beside each target, flagging below-target entries as visible non-failing warnings. (11) `ExportSize.MaxTotalPixels` reduced from 2,000,000,000 (~8 GB at 32-bit pixels) to 100,000,000 px (~400 MB full frame + working buffers), with the three boundary tests updated in the same commit. Skipped with reason: SKILL.md/AGENTS.md checklist references (both `docs/08-TEST-CHECKLIST.md` and `docs/REVIEW-CHECKLIST.md` exist and serve distinct per-commit vs per-PR purposes), the pre-commit dirty-tree guard (already uses root-relative `git -C $repoRoot` since e5b5c67), and the AddIn `Configurations` array (already `["Release", "Debug"]` since 8927f2f).
- **AddInAssemblyTests Debug fallback restored (8927f2f)** — `AddInAssemblyTests.Configurations` had been narrowed to `["Release"]` in `f4d79da` while the comment still described a Debug fallback and the exception message still named `{Release|Debug}` — both lies. The silently-broken fallback also pulled `AddIn_assembly_loads_successfully` and `AddIn_targets_windows_TFM` into requiring a Release build even though only `AddIn_packaged_xll_exists` needs the packed XLL (a Release publish artifact). Fix: restore `Configurations` to `["Release", "Debug"]` (Release preferred) matching the `d0c6c8e` design goal, extract `LocateAddInBinDirectory()` so the XLL test resolves the Release publish path directly, and align the comment + exception message with what the code actually does. Evidence: `dotnet test --configuration Debug` → AddIn.Tests 3/3 (XLL test passes because Release publish output exists from prior verify run); `pwsh ./scripts/verify-quick.ps1` → 12/12 PASS. Note: `IDE1006` (private field `_` prefix) fires on `Configurations` but is pre-existing — every `private static readonly` field in the codebase uses PascalCase, so this is a `.editorconfig`-vs-convention mismatch, out of scope here.

- **AGENTS.md trim + skill-tree references.md removal (pending commit)** — Token-footprint audit of `.cline/skills/` and `AGENTS.md` found two redundancies: (1) `AGENTS.md` (loaded every turn) substantially restated content already owned in full by skills `02-architecture`, `04-test-strategy`, `05-git-quality`, and `06-llm-protocol`; (2) the per-skill `references.md` copy was a byte-for-byte duplicate of the matching `docs/0N-*.md`, generated and drift-checked for zero information `SKILL.md`'s own "Full reference: docs/..." footer didn't already provide. Fixes: `AGENTS.md` condensed (sections that elaborate a rule now end with a one-line pointer to the owning skill instead of restating it; the non-negotiable product invariants and the validator-positive-test rule kept verbatim; the orphaned C# conventions moved into a new `docs/02-ARCHITECTURE.md` "C# and architecture conventions" section, the testing principles into `docs/04-TEST-STRATEGY.md` "Test objective", and the control-record discipline into `docs/05-GIT-QUALITY.md` "Control-record discipline" so every AGENTS.md pointer resolves). `scripts/sync-cline-skills.ps1` no longer generates `references.md`; all 7 committed `references.md` files were deleted. Each `docs/0N-*.md` gained a hand-authored `<!-- SKILL-SUMMARY:START/END -->` block (5-15 lines) that the sync script extracts verbatim for `SKILL.md`, replacing the "first 80 non-empty lines" truncation heuristic that had already once silently dropped load-bearing content (the reason `check-skill-summary.ps1` existed). The `SKILL-SUMMARY` block is now **required**: a canonical doc missing it fails the sync with a non-zero exit, consistent with `docs/clinerules/SYNC.md`; the truncation fallback is removed. `scripts/sync-cline-skills.Tests.ps1` updated (references.md-existence assertion removed; new assertions: no `references.md` created, SKILL-SUMMARY block extracted verbatim, footer points at the canonical doc, no "references.md" mention) and its fixture doc gained a SKILL-SUMMARY block; the `$map`-injection regex in both `sync-cline-skills.Tests.ps1` and `check-cline-skills.Tests.ps1` hardened from a lookahead form that swallowed the script's new variable declarations to a line-anchored `(?ms)\$map = @\{.*?^\}` form. `scripts/check-cline-skills.ps1` needed no change — its drift check is a generic regenerate-and-diff, not a hardcoded file-name check. `check-skill-summary.ps1` + `check-skill-summary.Tests.ps1` deleted (their hardcoded phrase checks patched the truncation failure mode the SKILL-SUMMARY block eliminates); the 'skill summary phrases' step removed from `verify-quick.ps1` (now 11 steps) and `verify.ps1` (now 14 steps), and the corresponding pre-commit-hook checker entry removed from `scripts/pre-commit.ps1` (3 gates now: skill-tree drift, status accuracy, markdown links) with `pre-commit.Tests.ps1` updated to the 3-checker contract (including a new negative assertion that the hook must NOT invoke `check-skill-summary.ps1`). `docs/clinerules/SYNC.md` updated to describe the one-file-per-skill shape and the `SKILL-SUMMARY` convention. `.cline/skills/0N-*/SKILL.md` regenerated (7 files, ~1.0-1.4 KB each; summaries 100% hand-authored). Follow-up hardening: the `02-architecture` SKILL-SUMMARY block gained a "do not get wrong" bullet naming the doc's new "C# and architecture conventions" section as binding for every C# change, and the `02-architecture` skill description in `sync-cline-skills.ps1` was extended to trigger on C# conventions conversations — without this, an agent that only loaded the summary would never learn the relocated C# rules existed. Token-budget round: all 7 skill descriptions compressed (326 → 235 words, ~70 tokens/turn off the constant always-on budget) by dropping the "Use this skill when the conversation is about" boilerplate and moving to keyword-forward "Trigger on ..." forms with every trigger keyword retained; and the `04-test-strategy` / `05-git-quality` summaries now deliver their relocated AGENTS.md one-liner rules verbatim in the summary (observable-behaviour + deterministic/parallel-safe; comment-value + control-record discipline) so those skills need no second hop when triggered. Evidence: `pwsh ./scripts/test-scripts.ps1` → 65/65 Pester PASS; `pwsh ./scripts/sync-cline-skills.ps1` → "regenerated 7 skills", no fallback warnings; `pwsh ./scripts/verify-quick.ps1` → 11 steps, all PASS (110.1 s, 2026-09-08; includes the drift gate against the regenerated tree, PSScriptAnalyzer clean after UTF-8 BOMs were added to the three edited non-ASCII scripts, and the full non-Office test suite).

- **Phase D review methodology (2026-09-07)** — W-7 from the QA/QC fix programme. (1) `docs/06-LLM-PROTOCOL.md` gains an "End-to-end code review methodology" section that codifies the six-phase protocol demonstrated by the W-13 / Phase A/B/C run on 2026-09-07 (read the source, probe empirically, identify defect classes, plan, implement+verify+commit, self-application), with the four observed defect classes named (Gate integrity / Build pipeline drift / Docs/code drift / Local/CI view divergence) and the W-8/W-9/W-10/W-11/W-12/W-13 amendments cited as the worked examples. (2) New `scripts/review-runbook.Tests.ps1` (6 Pester tests) is the self-test for the methodology: it asserts the methodology section exists, the four defect classes are named, the W-13 capture-then-judge pattern is in `docs/04-TEST-STRATEGY.md`, the four worked-example tests (`pssa-gate`, `step-parity`, `ArtifactSourceMarker`, `ci-parity`) exist as files, `scripts/tool-versions.psd1` is read by `ci.yml` + `lint-ci.ps1` + `test-scripts.ps1` (the W-11 single source of truth has three consumers), and the W-12 branch policy is in `docs/05-GIT-QUALITY.md`. A future contributor who removes any of these artefacts or the supporting gate test fails this runbook test, which forces a conversation rather than a silent regression. (3) Skill regeneration: `docs/04-TEST-STRATEGY.md` gained "Gate integrity (W-13)" and "Step-parity (W9)" subsections; the skill copy under `.cline/skills/` is regenerated so the next agent gets the same rules. Evidence: `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (post-Phase-C run, 117.5 s); the review-runbook test suite is itself 6/6 Pester and now runs as part of every `test-scripts.ps1` invocation. Two observed green GitHub CI runs of `stage-inspect` are the closing evidence; the next push (which will include this Phase D commit) is the third, the strongest signal that the methodology produced durable gates.
- **Phase C QA/QC amendments (2026-09-07)** — W8 / W9 / W10 / W11 / W12 from the QA/QC fix programme. (1) **W11 single source of truth**: new `scripts/tool-versions.psd1` pins Pester minimum major (5), PSScriptAnalyzer 1.25.0, and the actionlint 1.7.7 SHA-256 + download URL; `lint-ci.ps1` and `test-scripts.ps1` now import it instead of hard-coding the values, and `ci.yml`'s PSScriptAnalyzer install line reads the version from the same source. `ci-parity.Tests.ps1` has three new Pester tests asserting the SHA-256 in `ci.yml` and `lint-ci.ps1` equals the psd1 (closes L9). (2) **W8 full step allow-list**: `ci-parity.Tests.ps1` now also asserts `dotnet format` is a native allow-listed step and no bare `dotnet test` exists outside the verify-script delegation contract; the original 3 tripwires are preserved. (3) **W9 step-parity + no-silent-pass**: new `scripts/verify-helpers.ps1` extracts the verify scripts' `Invoke-Step` names and `.DESCRIPTION` numbering; new `scripts/step-parity.Tests.ps1` asserts they match (description-vs-actual drift becomes a gate failure). `check-md-links.ps1` now fails when the scan finds zero markdown files (a no-silent-pass fix — the original code returned OK on an empty scan, which proves nothing about link health). (4) **W10 artifact-traceability enforcement**: new `tests/GanttCreator.Architecture.Tests/ArtifactSourceMarkerTests.cs` scans every `tests/**/bin` or `publish` reference and requires a `// artifact-source: <step>` marker; positive control test ensures the regex + marker contract stays in sync. (5) **W12 review/process policy**: `docs/REVIEW-CHECKLIST.md` now has CI-parity, artifact-traceability, and reviewer-rule sections (validator-with-positive-test, culture-roundtrip, `TimeProvider` boundary); `docs/05-GIT-QUALITY.md` adds the CI-parity section and a branch-policy rule that phases may not exit on local-only evidence (the W-12 amendment motivated by the PSSA blind-gate surviving three rounds without CI). Evidence: `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (113.7 s, 2026-09-07); Pester 51/51 (44 prior + 2 step-parity + 2 ci-parity version + 3 tool-versions); C# tests Core 65/65, Raster 37/37, Architecture 15/15, AddIn 3/3; zero PSSA warnings. First green GitHub CI run of `stage-inspect` (after the W13 PSSA fix) is the closing evidence for the W12 branch-policy rule.
- **PSScriptAnalyzer blind-gate fix (2026-09-07)** — The first CI push of `stage-inspect` failed at the PSScriptAnalyzer step (exit 11) under PSScriptAnalyzer 1.25.0 with 11 warnings in `scripts/*.Tests.ps1`. Investigation found that `Invoke-ScriptAnalyzer -EnableExit` calls `exit` from inside a function, which does not propagate to the parent process's $LASTEXITCODE when the cmdlet runs inside the Tee/ForEach pipeline in `Invoke-Step` — so the local analyzer step was silently blind: every "PSScriptAnalyzer clean" claim since R0.8 was a false PASS. (1) The W-13 fix: both `verify-quick.ps1` and `verify.ps1` now capture findings and judge in the step's own scope instead of using `-EnableExit`. (2) The W-14 fix: 8 `PSUseDeclaredVarsMoreThanAssignment` warnings resolved by moving `$scriptPath`/`$installScriptPath` from a top-level `BeforeAll` (PSSA can't follow Pester's block-scoping) into a `Describe` `BeforeAll` that uses the `$script:` scope, and updating all inner references. 3 `PSUseShouldProcessForStateChangingFunctions` warnings resolved by renaming the state-mutating test helpers from `New-PreCommitStubChecker`, `New-PreCommitTerminatingErrorStubChecker`, and `New-LockedRestoreHarness` to `Write-*` (renaming out of the state-changing verb class is the documented PSSA escape; the helpers do not actually require `-Confirm` semantics). (3) The W-15 positive test: new `scripts/pssa-gate.Tests.ps1` asserts the step's anti-blind guarantee (no `-EnableExit` in `verify-quick.ps1`) and reproduces a real warning with a synthetic fixture. (4) The W-16 docs: this entry + L10 in `KNOWN-LIMITATIONS.md`. Evidence: `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (114.2 s, 2026-09-07); Pester 41/41 (40 prior + 1 new); zero PSSA warnings.
- **Phase B correctness round (2026-09-07)** — W4/W5/W6 from the QA/QC fix programme. (1) W4 Core: `VersionInfo.ExtractSemanticVersion` now validates semver numeric parts character-by-character against ASCII digits (rejecting signs, whitespace, leading zeros, and empty prerelease; culture-invariant), `SemanticVersion` documented as retaining a prerelease suffix, and the `SemanticVersion` test regex widened accordingly (`VersionInfoTests` 9 new assertions + de-DE culture test). `ExportSize` rejects non-positive widths at parse and a zero request value in `ToPixels`, and enforces a practical `MaxPixelDimension` of 65,535 px on both computed dimensions (`ExportSizeTests` 8 new cases). `RollingLog` injects `TimeProvider` (AGENTS clock-boundary rule), matches rotated files only as exact `{base}.{index}.log` with ASCII-digit indexes so prefix-sharing foreign files (`{base}2.log`, `{base}-backup.log`) are never shifted or deleted, removes the dead `_currentFilePath` field, converts `_failed` to an auto property behind a new public `IsFailed`, and contains disposal errors in `Dispose` (`RollingLogTests` 4 new tests incl. fixed-clock timestamp and de-DE culture test for the format overload). (2) W5: `Redactor`'s Unix path pattern gained a leading lookbehind so prose slashes (`and/or`, `3/4`, `12/31/2026`) are no longer redacted as `[path]`, with a behaviour table pinning both masked and unmasked cases (`RedactorTests` 5 new assertions). (3) W6: `check-md-links.ps1` anchors its roots to the repository root, uses `-LiteralPath` throughout, and now validates dot-prefixed relative links of the form dot-slash-name (a leading dot no longer exempts a link); `lint-ci.ps1` verifies the actionlint SHA-256 exactly as CI does; `CheckStatusScriptTests.cs` text tripwires were replaced by behavioural child-pwsh tests including both traversal-rejection cases; new `scripts/check-md-links.Tests.ps1` (4 tests) and `scripts/lint-ci.Tests.ps1` (2 anti-drift tests). Finding fixed during the round: the first version of the rotation matcher threw on the active `base.log` (prefix/suffix overlap inverted the slice range), latched the logger, and was caught only by the `Rotation_creates_new_file_when_size_exceeded` ≥2-files assertion — the one-sided "≤ cap" assertions passed with a single file. Evidence: Core 65/65, Raster 37/37, Architecture 13/13, Pester 40/40, `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (2026-09-07).
- **Phase A QA fixes (2026-09-07)** — `7b0c2d9`, `071e9f1`, `d0c6c8e` fix three blocking/latent gate defects found in a full line-by-line code review. (1) `7b0c2d9`: the CI Pester step inlined a Pester call using the `-Script` parameter (removed in Pester 5) and would have failed the first CI run; the step now delegates to `scripts/test-scripts.ps1` (verify-parity restored) and `scripts/ci-parity.Tests.ps1` tripwires against inline workflow Pester logic, with a positive control for the step-extraction helper. (2) `071e9f1`: verify-script `Invoke-Step` clears `$LASTEXITCODE` per step (stale-exit false-PASS); `verify.ps1` lost the phantom SBOM step claim and the incorrect coverage-comment wording (docs/code drift, checklist I); `test-scripts.ps1` now uses the Pester 5/6-safe `PassedCount`/`FailedCount`/`TotalCount` — the previous `.TestResult` accounting reported `Total=0` under Pester 6 while 32 tests ran — and fails when no test files are found or a discovered file contains zero tests. (3) `d0c6c8e`: `AddInAssemblyTests`/`CoreBoundaryTests` locate Release or Debug build output so plain `dotnet test` is green on a clean tree, both carry `artifact-source` markers per the build-pipeline artifact contract (first increment of W10 traceability), and the `.vscode` `test` task depends on `publish-addin` with a positive `VscodeTasksTests` assertion. Evidence: `pwsh ./scripts/verify-quick.ps1` 12/12 PASS (97.2 s, 2026-09-07); Pester 32/32; Architecture 16/16; AddIn.Tests 3/3.
- **R0.1** — Governance kit and repo scaffolding. Kit installed at `docs/0N-*.md`; `AGENTS.md`, `docs/DECISIONS.md`, `docs/KNOWN-LIMITATIONS.md`, `docs/REVIEW-CHECKLIST.md`, `docs/work-items/TEMPLATE.md`, the first work item, four ADRs, and `scripts/check-md-links.ps1` committed. Commit `aa0815b`.
- **R0.2 + R0.6 (verify scripts)** — Solution (`GanttCreator.slnx`), four production projects (Core on `net10.0`; Raster, Office, AddIn on `net10.0-windows`), six test projects, central package management with `Directory.Packages.props` (ExcelDna.AddIn 1.9.0, ExcelDna.Integration 1.9.0, ExcelDna.Interop 16.0.0 per ADR-0004), `Directory.Build.props` enforcing nullable + warnings-as-errors + analyzers + deterministic, `global.json` pinning .NET SDK 10.0.400, the architecture test that prevents `GanttCreator.Core` from referencing Office/Excel-DNA/SkiaSharp/clipboard, and the three verify scripts. Commit `900f78c`.
- **ADR-0005** — Document the actual host as Windows 11 25H2 (build 26200). The `HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion` `ProductName` string may report "Windows 10 Pro" on some configurations; the build number is the source of truth. Commit `1645c1e`.
- **Two-view rule discipline** — `scripts/sync-cline-skills.ps1` regenerates `.cline/skills/0N-*/SKILL.md` from `docs/0N-*.md`; `scripts/check-cline-skills.ps1` is the drift gate; both `verify-quick.ps1` and `verify.ps1` run the gate. `docs/clinerules/SYNC.md` documents the discipline. Commit `8650717`. The .clinerules/ tree was removed in the QA/QC hardening round (commit `dc7433d` and later): always-on rules now live in `AGENTS.md` only, and the skills use Cline's progressive on-demand loading.
- **Drift gate fix** — `check-cline-skills.ps1` was checking post-sync state, which silently overwrote any hand-edited rule. It now checks the working tree directly and fails if either view has unstaged or untracked changes (staged changes remain allowed so the pre-commit hook is usable). Commit `d543e60`.
- **R0.3 — Central packages and lock file** — `RestorePackagesWithLockFile=true` enabled in `Directory.Build.props`. Per-project `packages.lock.json` files generated for all projects with dependencies in both `src/` and `tests/`. `verify-quick.ps1` and `verify.ps1` both use `--locked-mode` unconditionally. `scripts/test-locked-restore.ps1` performs a deliberate two-run-from-clean-state test: both runs PASS, proving the lock file is honoured without network access. Commits `229e93c` (property + work item), `559212f` (verify + test scripts), `f9c27cf` (lock files), `ab27ee6` (test project lock files).
- **R0.7 — Windows CI** — `.github/workflows/ci.yml` mirrors `scripts/verify-quick.ps1` step-for-step on `windows-latest`; every CI command passed locally verbatim, including a deliberate-failure annotation gate. Commits `9ef6cc2` (workflow), `649ff14` (format), `b5d0801` (Get-Module alignment fix).
- **R0.8 — Versioning and local rolling-log abstractions** — `VersionInfo.cs` reads `AssemblyInformationalVersion` with deterministic fallback; `RollingLog.cs` implements size-based rotation with file-count cap and injected redaction; `Redactor.cs` masks emails, paths, dates, GUIDs, hex tokens. L6 (workflow lint) closed via `scripts/lint-ci.ps1` + pinned actionlint; L7 (script tests) closed via `scripts/test-scripts.ps1` + Pester for all `scripts/*.ps1`. Commits `8206037` (R0.8 production code + tests), `0fc89e5` (complete R0.8). `pwsh ./scripts/verify-quick.ps1` → PASS (duration record from that run
predates the current 12-step gate and is superseded by the measured
97-333 s range; see commit `2c1519e`); `pwsh ./scripts/test-locked-restore.ps1` → PASS.
- **R0.4 — `.editorconfig` and `.gitattributes`** — C# language conventions, analyzer severity, naming rules, var preferences in `.editorconfig`. Line-ending policy (LF for all source, scripting, config, Markdown) and `packages.lock.json` merge strategy in `.gitattributes`. Normalized all 57 tracked text files from CRLF to LF. Added `CA1707` to `NoWarn` with a per-rule comment. `tests/Directory.Build.props` now imports the root props via `GetPathOfFileAbove`, and sets `EnforceCodeStyleInBuild=false` because the test code uses xUnit conventions (snake_case, braceless if) that production-style rules flag. `CoreBoundaryTests.cs` gained `StringComparison.Ordinal` on `Assert.Contains`/`Assert.DoesNotContain` (CA1307). Commits `c66e683` (editorconfig + gitattributes), `50ffabe` (LF normalize), `5f4ac32` (CA1707), `7d64cd7` (test props import + EnforceCodeStyleInBuild off).
- **R0.5 — Sample tests, OfficeIntegration trait, `.vscode/tasks.json`** — 6 `UnitTest1.cs` placeholders deleted; 6 sample test classes added (one per test project); 2 `[Trait("Category","OfficeIntegration")]` tests in `Office.IntegrationTests` (Excel + PowerPoint COM progid presence). `.vscode/tasks.json` mirrors the verify scripts and includes sequential clean/build plus a shared Release AddIn publish prerequisite for general test tasks. Both verification gates publish the packed XLL before AddIn assembly tests. `NoWarn` tightened from 8 to 1 (only `CA1707` remains, with documented rationale). `GenerateDocumentationFile=true` in production so CS1591 enforces XML doc comments; test projects override to `false` because tests aren't API. Production `ExportSize` type lives in `GanttCreator.Raster` (R8.1 seed); culture-invariant parsing, tuple-switch validation, brace-styled ifs. `dotnet test --filter "Category=OfficeIntegration"` → 2/2 PASS. Full suite at R0.5: 35 tests across 6 projects (53 non-Office + 2 OfficeIntegration = 55 as of the R0.7 work; see the review-hardening entry below). Commits `eba9339` (initial), `12e8329` (analyzer fixes), `6578155` (XML doc enforcement). Clean-tree `pwsh ./scripts/verify-quick.ps1` → PASS in 22.1 s after the publish follow-up.
- **Gate parity round (2026-09-04)** — Closed the CI↔local parity gaps found in the tooling audit. (1) `verify-quick.ps1` gains the format gate (`dotnet format --verify-no-changes --exclude tests`, measured 12.2 s) after restore, mirroring CI order. (2) `ci.yml` gains full-history checkout (`fetch-depth: 0`, required by check-status hash resolution on shallow clones), a doc-gates step running `check-md-links.ps1` + `check-status.ps1` + `check-cline-skills.ps1`, and a PSScriptAnalyzer step. (3) PSScriptAnalyzer 1.25.0 adopted for `scripts/`: baseline 47 warnings → 0 after real fixes (Invoke-Step/Invoke-LockRestore approved verbs; `Remove-ObjDirectory` gained `SupportsShouldProcess`; 2 dead variables removed; UTF-8 BOM added to 2 non-ASCII files) plus two documented exclusions in `scripts/PSScriptAnalyzerSettings.psd1` (PSAvoidUsingWriteHost: console UX by design; PSReviewUnusedParameter: known false positive for params used inside scriptblock arguments). (4) YAML workflow lint and Pester-for-scripts deferred by decision as **L6/L7** in `docs/KNOWN-LIMITATIONS.md`, with a do-not-skip note parked on R0.8 in `docs/03-ROADMAP.md` (skill views resynced).
- **R0.6 verification (2026-09-04)** — Confirmed implemented in code, not just comments: `scripts/verify-quick.ps1` (5 steps) and `scripts/verify.ps1` (8 steps incl. XPlat Code Coverage collection and the per-project threshold check) both PASS on this machine (verify: PASS in 32.3 s); `coverlet.collector` 6.0.4 is referenced by all six test projects. Two documented gaps, both deliberate: (1) threshold *enforcement* is deferred to R3.x per `docs/04-TEST-STRATEGY.md` — the check records targets instead of failing; (2) no dedicated `docs/work-items/R0.6-*.md` exists — R0.6's scripts landed with R0.2's commit `900f78c` and are recorded in this file's R0.2 entry.
- **R0.7 — Windows CI** — `.github/workflows/ci.yml` on `windows-latest`: locked restore → `dotnet format --verify-no-changes --exclude tests` → Release build `-warnaserror` → AddIn publish (packed XLL uploaded as artifact with `if-no-files-found: error`) → non-Office tests. NuGet cache keyed on `**/packages.lock.json` per docs/05-GIT-QUALITY.md. A `workflow_dispatch` input `deliberate_failure` runs a step emitting `::error::` and exiting 1 — the R0.7 annotation gate, testable on demand. Every CI command is verbatim from `verify-quick.ps1`; the format command was executed locally (supports `.slnx`, passes with `--exclude tests`, matching the EnforceCodeStyleInBuild=false test policy). CI runs no Office tests by policy (ADR-0001). Commits `649ff14` (dotnet format fixes making the format gate green) and `9ef6cc2` (workflow + work item). Work item: `docs/work-items/R0.7-windows-ci.md`. CI-gate status: **Not run — requires push to GitHub**; local equivalents of all five CI commands PASS.
- **Review-hardening round (2026-09-04)** — Eight commits responding to external review findings, recorded here because they predate this entry: `2661db4` PointD.ToString pinned to invariant culture; `bd8c39b` VS Code process tasks invoke pwsh explicitly, build-clean split; `d32a0f4` verify-full task detail corrected to non-Office tests; `080e34c` R0.5 work-item test count (1→2) and entry-point description corrected; `83cccde` ExportSize.ToPixels guards (finite/positive/non-negative, int-range) + 6 tests; `c6b77af` VS Code object-schema restored, ParseValue non-finite rejection + 6 tests, PointD culture-invariance test; `47e2bb3` docs/08-TEST-CHECKLIST.md added (11 sections A–K) and skills updated; `4286b0b` checklist automation — VscodeTasksTests (5) and NoWarnScopeTests (2) architecture tests, AGENTS.md step 6 self-certification, clean-task dependency fix.

- **Pre-commit preventive-test round (2026-09-04)** — Implements the four remediations from the post-mortem on the last review round. (1) New check-skill-summary.ps1 (since removed) invoked as a new `verify-quick.ps1` step; asserts canonical phrases ("do not skip", "additionally produces coverage/", "set \`CurrentCulture\`", "ambient") appear in the regenerated `.cline/skills/0N-*/SKILL.md` files, so the sync-budget truncation that lost the R0.8 note and the culture-test contract cannot recur. (2) a C# text-tripwire test file — removed 2026-09-07 in the W6 behavioural migration; superseded by behavioural child-pwsh tests in `scripts/check-status.Tests.ps1`, including the two traversal-rejection cases the text assertions could not exercise — with three `[Fact]`s that assert the `git -C $repoRoot`, `GetFullPath`, and `GetRelativePath` safety guards in `check-status.ps1` are present — tripwire text assertions, since the script itself can't be imported into the C# xUnit harness. (3) `AGENTS.md` (and resynced always-on views) gains the rule "Every new `if (bad) { error }` validator ships with a positive test in the same commit" so future validator+test pairings are enforced by the agent contract, not by reviewer catch. `verify-quick.ps1` now 12 steps; PASS.
- **QA hardening round (2026-09-06)** — Post-R0.8 hardening. (1) Phase 2 of the drift gate (`b0fb26b`): `check-cline-skills.ps1` re-runs sync into a temp dir and byte-compares via `git diff --no-index --quiet`, so drift is caught even when the working tree is clean. (2) verify.ps1/verify-quick parity + pre-commit safety-net hook (`48cdf15`). (3) Pre-commit hook documented in canonical `05-GIT-QUALITY.md` + skills resynced (`dc7433d`). (4) Three-view skill discipline collapsed to two-view (`6aa16a0`): always-on rules live in `AGENTS.md` only. (5) PSScriptAnalyzer gate fix (`7d3c5a5`): `Get-Module -MinimumVersion` not supported, aligned with `test-scripts.ps1`. (6) CI Get-Module alignment (`b5d0801`). Commits `b0fb26b`, `48cdf15`, `dc7433d`, `6aa16a0`, `7d3c5a5`, `b5d0801`.
- **QA hardening round 3 (2026-09-07)** — Closes R0.8. (1) `7d69419` rewrote four script-test files (`check-cline-skills.Tests.ps1`, `check-status.Tests.ps1`, `check-skill-summary.Tests.ps1`, `sync-cline-skills.Tests.ps1`) plus `test-locked-restore.Tests.ps1` from fragile text-level regex assertions to an isolated child-pwsh harness that exercises the script's observable behaviour (exit code + script-written output); added `docs/evidence/script-test-harness-refactor.md`. (2) `5e20712` fixed the pre-commit `$LASTEXITCODE`-reset bug: piping the checker through `ForEach-Object`/`Write-Host` reset the exit code to 0, and a terminating `Write-Error` aborted the parent before the blocking message could print — both bugs combined silently masked `check-cline-skills.ps1`'s terminating-error path. The new try/catch wrapper captures `$LASTEXITCODE` before any other native command runs and converts terminating errors into exit 1 with the exception message. (3) `13b0f56` widened `IRedactor.Redact` to accept and return `string?` with `[NotNullIfNotNull(nameof(input))]`; `RollingLog.WriteCore` coalesces a null redactor result to `string.Empty`. (4) `c13a21a` clarified the drift-gate Phase 1 wording to "unstaged or untracked changes (staged changes are allowed)" and corrected the `install-pre-commit.ps1` `.DESCRIPTION` to match the actual `.githooks/` repository-root install path. Pester 28/28, Core 45/45, format clean, PSScriptAnalyzer clean.
- **R0.8 follow-up (2026-09-07)** — `d4b36fe` fixes a cap-violating branch in `RollingLog.PerformRotation`: when `_maxFileCount == 1` the active `.log` is now deleted instead of moved to `.1.log`, so the directory never holds two files for a cap of one. Behaviour for `maxFileCount >= 2` is unchanged. Paired with `[Fact] File_count_cap_of_one_deletes_active_log_on_rotation` in `RollingLogTests` (positive test, 10 rotations under cap-1, asserts no `.1.log` is ever created and total files `<= 1`). Failure containment is preserved: the new branch executes inside the existing `RotateIfNeeded` try/catch, so any I/O failure disables further writes via `_failed`. Core 46/46, Pester 28/28, `verify-quick.ps1` 12/12 PASS (201.4 s).

- **Review-hardening round (2026-09-08)** — Closes the review-findings batch across four commits. (1) `2c1519e` marked `SECURITY.md` pre-release (no released version exists), corrected the `verify-quick.ps1` duration claim in `docs/05-GIT-QUALITY.md` + skill mirrors to the measured `~2 minutes`, and scoped the `docs/06-LLM-PROTOCOL.md` validator positive-test rule to behavior changes that add validators (doc-only changes use doc/link/drift gates). (2) `dc3c41d` delegated the CI `dotnet build`/`dotnet publish`/`dotnet test` steps to shared entry points `scripts/build-release.ps1`, `scripts/publish-addin.ps1`, `scripts/test-non-office.ps1` (same entry points `verify-quick.ps1`/`verify.ps1` call, W8 local/CI parity); hardened the `ci-parity.Tests.ps1` inline-dotnet tripwire to reject every inline `dotnet` gate command (test/build/publish) with arguments and in multiline `run:` blocks, allowing only the vetted native commands `dotnet format`/`dotnet restore --locked-mode`; rewrote `install-pre-commit.ps1` to use the cross-platform `pwsh` shim and `git -C $repoRoot config core.hooksPath .githooks`; changed `pre-commit.ps1` blocking message from `Write-Error` to `Write-Host` (retaining `exit $checkerExit`). (3) `2555244` made `VersionInfo.ExtractSemanticVersion` a public contract and added prerelease-identifier validation (rejects empty identifiers like `alpha..1`, non-ASCII/non-hyphen chars, and numeric identifiers with leading zeros like `01`); `RollingLog` now exposes `IsFailed` through `IRollingLog`; renamed the rotation helper `GetRotatedFilesNewestFirst` → `GetRotatedFilesOldestFirst` with corrected XML docs (descending rotation index = oldest first) and removed the stale `int.MaxValue` comment. (4) `67a3a11` relocated the `pssa-gate.Tests.ps1` runner to a sibling temp directory (so `Invoke-PssaGate -Path` scans only fixtures) and added a W13-shape fixture asserting FAIL through the `Invoke-Step`/`Tee-Object`/`ForEach-Object` pipeline; extracted the actionlint SHA-256 check into `scripts/actionlint-hash.ps1` and added an offline child-process test to `lint-ci.Tests.ps1` asserting acceptance (matching hash) and rejection (mismatched hash). (5) `efcec33` added a pre-flight guard to `pre-commit.ps1` that refuses to run the checkers while the working tree is dirty under the inspected roots (`docs/`, `.cline/skills/`, `AGENTS.md`, `.github/`), the minimal correct alternative to per-checker `git show` index-snapshot refactoring: it guarantees working tree == index for the validated content so unstaged changes cannot mask issues in the committed content. Core 73/73, Pester 28/28, `ci-parity` 12/12, `pssa-gate` 4/4, `lint-ci` 4/4, `pre-commit` 9/9, format clean, PSScriptAnalyzer clean, `verify-quick.ps1` 12/12 PASS (332.8 s).
- **pre-commit dirty-tree guard repair (e5b5c67)** — The `pre-commit.ps1` dirty-tree guard was dead code: `$scriptRoot` resolves to `scripts/`, so `git -C $scriptRoot status -- docs .cline/skills AGENTS.md .github` looked for `scripts/docs` etc. (none exist) — `$dirty` was always empty and the guard never fired. Fixed by computing `$repoRoot = Split-Path -Parent $scriptRoot` and using `git -C $repoRoot`; verified empirically (unstaged AGENTS.md now correctly blocks with exit 1 + "Commit blocked"). Added two `pre-commit.Tests.ps1` cases in an isolated fixture repo (dirty → non-zero exit + "Commit blocked"; clean → exit 0 + "pre-commit: quick gates PASS"), three cases that would have caught the defects (dirty blocks; clean passes; staged-only passes). Pester 12/12 pre-commit.

- **CI PSSA step delegates to Invoke-PssaGate (pending commit)** — `ci.yml` PSSA step still called `Invoke-ScriptAnalyzer ... -EnableExit` directly instead of routing through `Invoke-PssaGate` (the entry point verify-quick.ps1/verify.ps1 use, hardened in W13). Fixed by dot-sourcing verify-helpers.ps1 and calling Invoke-PssaGate; added a ci-parity.Tests.ps1 `Describe 'PSScriptAnalyzer gate delegates to Invoke-PssaGate (W13)'` block asserting the step matches Invoke-PssaGate and (on code lines only, stripping full-line comments) does not match -EnableExit. Pester 13/13 ci-parity, 4/4 pssa-gate.

- **R1.2 evidence asymmetry (historical, not fixable; moved out of "Open questions for human" so that section stays consistent with its "none yet" state)** — The R1.2 Office-gate entry records "user-reported verification" without the edited filename or VS/Excel versions, unlike the detailed timestamped cross-corroborated entries for R1.3/R1.4. This asymmetry is permanent: the R1.2 regression fix already landed and the code moved on, so the entry cannot be retroactively strengthened. It is recorded here so an end-to-end audit of the evidence trail does not treat the gap as an omission.


## Active work item

- **R1.3 — Add diagnostics command showing add-in/Office/bitness identifiers** —
  `DiagnosticsService` singleton added to `src/GanttCreator.AddIn/` with
  `DiagnosticsService.TaskDialog.cs` (managed WinForms TaskDialog:
  `TaskDialogPage` + `EnableLinks`); `GanttRibbon` extended with `OnDiagnosticsClick`
  callback; Ribbon button added in `src/GanttCreator.AddIn/RibbonResources/Ribbon.xml`
  with `onAction`; Core's `IRollingLog` extended with read-only `LogFilePath` property
  implemented by `RollingLog`; button label in `src/GanttCreator.AddIn/RibbonResources.resx`.
  Contract tests in `tests/GanttCreator.AddIn.Tests/DiagnosticsServiceTests.cs`
  (13 `[Fact]`s covering singleton lifetime, log path, failed-log path, identifier
  gathering, content building, log write, and hyperlink handling). Commits pending.
  Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 43/43 PASS; full non-office
  suite green; Release build 0 warnings; Office gate
  (F5, click Diagnostics, TaskDialog with clickable hyperlink, log written):
  **Not run by the agent** — requires Visual Studio against desktop Excel.

- **R1.3 fix — Diagnostics button did nothing in Office; task-dialog swapped to
  managed WinForms API.** Evidence captured in a VS F5 session (one
  `System.EntryPointNotFoundException` per click, first exception read from the
  `.callback-error.txt` capture file next to the rolling log): the then hand-rolled
  P/Invoke declared `GetDesktopWindow` with `DllImport("comctl32.dll")`, but the
  entry point lives in user32.dll, so every click threw; comctl32 v6 exports
  `TaskDialogIndirect`/`TaskDialog` only by ordinal (121/119), never by name, so the
  same failure awaited after any by-name repair. Fix in
  `src/GanttCreator.AddIn/DiagnosticsService.TaskDialog.cs` (rewritten to the managed
  `System.Windows.Forms.TaskDialog` API: `TaskDialogPage`, `EnableLinks`, `LinkClicked`
  with `TaskDialogLinkClickedEventArgs.LinkHref`), `DiagnosticsService.cs`
  (MessageBox fallback with dedicated plain text carrying the log file path; `&`-escaping
  via `EscapeAccessKeys` because `EnableLinks` treats `&` as an access-key prefix;
  `WriteDiagnosticsError` now failure-proof through the rolling log), `GanttRibbon.cs`
  (one-line catch — the recorder is failure-proof by contract), and
  `<UseWindowsForms>true</UseWindowsForms>` in `GanttCreator.AddIn.csproj` and the test
  project (imports the .NET Desktop SDK — no new NuGet package; empirically needs no
  `TreatAsUsed`/analyzer tweak on `net10.0-windows` with `AnalysisMode=All` +
  warnings-as-errors: Release build 0 warnings). Regression facts for the page wiring,
  link delivery, fallback plain-text (log file path present), `&`-escaping, and positive error-path coverage in
  `tests/GanttCreator.AddIn.Tests/DiagnosticsServiceTests.cs`. Committed as
  `9ca4188` (fix), `77e28f3` (LF normalization for the whitespace gate), and
  `a92a583` (IDE0001/IDE0007 — note: the format gate is bare `dotnet format`,
  which includes style/analyzers that do not fire as build warnings).
  Gates: `dotnet test tests/GanttCreator.AddIn.Tests` 50/50 PASS;
  **`pwsh ./scripts/verify.ps1` → PASS in 154.4 s (all 14 steps)**; Office gate
  (F5, click Diagnostics, TaskDialog appears, hyperlink opens the log file, and no
  `EntryPointNotFoundException` in the VS Debug output): **PASS — confirmed by the
  human operator in VS 2026 F5 against desktop Excel (2026-09-13)**. Corroborating
  evidence from the session rolling log (gantt-creator-addin.log in the
  per-user log directory):
  every pre-fix `Diagnostics:` record (22:31–23:32 on 2026-09-12) has no
  `dialog closed` follow-up line (the throw happened between record and dialog),
  while every post-fix click (from 2026-09-13 06:57:49, build of `a92a583`+) is
  followed by `Diagnostics dialog closed; button ID = ...`, and the VS Debug output
  shows zero exceptions where the pre-fix session showed one
  `EntryPointNotFoundException` per click.

- **R1.3 lifecycle cleanup fix (2026-09-13)** — `AddInHost.AutoClose` now calls
  `DiagnosticsService.Reset()` in its disposal `finally`, so the singleton no
  longer retains the disposed session log before the next `AutoOpen`; the focused
  regression verifies the old singleton's `LogFilePath` is cleared and the
  singleton is replaced. `AddInHostTests` and `DiagnosticsServiceTests` share a
  serial diagnostics collection to isolate the process-wide singleton. Gates:
  `dotnet test tests/GanttCreator.AddIn.Tests` 51/51 PASS;
  **`pwsh ./scripts/verify-quick.ps1` → PASS in 149.7 s (all 12 steps)**;
  `git diff --check` clean. Office gate remains the human-confirmed F5 result
  recorded above.

## Environment (recorded once, then referenced)

- **Host OS**: Windows 11 25H2 (build 26200), x64, Professional. The registry `ProductName` may report "Windows 10 Pro" — trust the build number, not the string. See `docs/adr/0005-windows-host-build-number.md`.
- **.NET SDK**: 10.0.400
- **.NET runtimes**: 10.0.11, 8.0.30 (Microsoft.NETCore.App, Microsoft.AspNetCore.App, Microsoft.WindowsDesktop.App)
- **Visual Studio**: 18.10.0 (Community), installed at `C:\Program Files\Microsoft Visual Studio\18\Community`
- **Office**: Microsoft 365 x64 retail, 16.0.20326.20112, current channel, en-US
- **PowerShell**: 7.4.19
- **Excel-DNA target**: 1.9.0
- **Self-hosted runner policy**: option 1 — a real licensed Microsoft 365 x64 install on this machine; the Office-integration suite is excluded from `verify.ps1` and runs via `scripts/verify-office.ps1` only at phase exit and release.

## Known limitations

- **L2** — `.vs/` (Visual Studio user options) is present locally and is already covered by `.gitignore`.
- **L3** — ~~Office fixture gap~~ **closed**. `OfficeFixture` harness exists; `OfficeFixtureTests.Excel_open_close_five_times_no_orphan` passes with no orphan `EXCEL.EXE` (5/5 Office integration tests).
- **L12** — ~~Blame-timeout limitation~~ **closed**. `verify-office.ps1` no longer passes `--blame-hang-timeout` (which aborted the testhost ~0.2 s after start); the deadline is enforced by an external watchdog that polls the owned `dotnet test` process and, on timeout, kills the whole owned process tree (`taskkill /PID /T /F`) and re-runs the harness-owned Office-process sweep as a safety net. Cleanup is guaranteed only for Office processes positively identified by the fixture manifest or by parentage in the owned `dotnet test` tree; when neither identifies a process, it is skipped and may remain. Covered by `scripts/verify-office.Tests.ps1` (no-flag assertion + hang/timeout and prompt-pass harness cases). Gates: `scripts/verify-office.Tests.ps1` 14/14 PASS; `scripts/verify-office.ps1` 8/8 PASS (27.8 s, no orphan); PSSA 0 findings on both modified scripts.
- **L8** — ~~Docstring coverage gap~~ **closed**. Documented decision (2026-09-04, human): not a project gate; rely on CS1591 + reviewer judgement.

## Open questions for human

- _none yet_
