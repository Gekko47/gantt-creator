# Known limitations

> Short list of accepted, time-bounded limitations. Each entry has an ID, an owner, an expiry, and a replacement release gate. Quarantined tests do not count as passing; quarantined features do not count as shipped.

| ID | Limitation | Owner | Since | Expires | Replacement gate |
| --- | --- | --- | --- | --- | --- |
| L1 | _(removed by ADR-0005: the actual host is Windows 11 25H2 / build 26200)_ | — | — | — | — |
| L2 | Visual Studio 2026 (not 2022) is the verification IDE | _unassigned_ | 2026-09-04 | first R1.1 Office gate | R1.1 F5 evidence with a documented VS 2026 build number |
| L3 | Office fixture was R0.x scope; re-dated to R1.6 after the `OfficeFixture` harness was added to `tests/GanttCreator.Office.IntegrationTests`. Harness exists; `OfficeFixtureTests.Excel_open_close_five_times_no_orphan` passes with no orphan `EXCEL.EXE` | _closed_ | 2026-09-04 | R1.6 exit | `OfficeFixture` exercised by `OfficeFixtureTests.Excel_open_close_five_times_no_orphan` with no orphan `EXCEL.EXE`. Gate met locally (5/5 Office integration tests) |
| L4 | Golden PNG baseline directory does not exist yet | _unassigned_ | 2026-09-04 | R3.12 exit | R3.12 reference scene with at least one approved PNG |
| L5 | The `_GanttCreatorConfig` safe-repair policy is not yet specified | _unassigned_ | 2026-09-04 | R2.10 start | R2.10 ADR defining auto-repair vs confirmation boundary |
| L6 | `ci.yml` has no dedicated workflow lint (actionlint / yamllint); workflow structure is only exercised when CI itself runs | _closed_ | 2026-09-04 | R0.8 | A committed workflow-lint step (e.g. `actionlint` pinned binary or equivalent) passing on `ci.yml` in CI |
| L7 | PowerShell scripts in `scripts/` have no unit tests (Pester); they are exercised only by execution inside the verify gates | _closed_ | 2026-09-04 | R0.8 | Pester (or equivalent) unit tests for `scripts/*.ps1` wired into `verify-quick.ps1` |
| L8 | External diff-scoped docstring coverage reports 23.08% against an 80% threshold; this is **not** a project gate. Decision (2026-09-04, human): do not force-doc non-public or obvious symbols to chase a volume metric; rely on CS1591 + reviewer judgement per `AGENTS.md` | _closed_ | 2026-09-04 | n/a (decision, not a gap) | The repo's own policy — `GenerateDocumentationFile=true` + `CS1591` build error per undocumented public member in `src/` — is the enforced standard. Revisit only if a non-obvious public symbol lands without a summary, or a reviewer's "what does this do?" signals real missing docs |
| L9 | Pester 6+ is the standard. `scripts/tool-versions.psd1` pins `MinimumMajor = 6`; `test-scripts.ps1` and CI install/enforce Pester 6+. Version drift between dev and CI is tolerated as long as both are 6+ (counting uses version-safe `TotalCount`/`PassedCount`/`FailedCount`, never the Pester 4-era `.TestResult` collection). Single-source tooling (W11) shipped: `tool-versions.psd1` is the single source of truth; `lint-ci.ps1`, `test-scripts.ps1`, and `ci.yml` import it; `ci-parity.Tests.ps1` enforces equality | _closed_ | 2026-09-07 | version-parity decision | Decision (2026-09-11, human): Pester 6+ is the standard across all docs and CI. No single exact version pin; version-safe counting is the permanent contract |
| L10 | The PSScriptAnalyzer gate in `verify-quick.ps1`/`verify.ps1` used `-EnableExit`, which does not propagate through the `Invoke-Step` Tee pipeline — every "PSScriptAnalyzer clean" claim since R0.8 was a false PASS. Fixed locally (capture+judge + positive Pester test in `pssa-gate.Tests.ps1`) and in CI (`ci.yml` routes through `Invoke-PssaGate`). Regression guard (`pssa-gate.Tests.ps1` "no `-EnableExit`" assertion + `ci-parity.Tests.ps1` parity assertion) observed on green `stage-inspect` CI run #40 (`adbded8`) | _closed_ | 2026-09-07 | first green CI run of `stage-inspect` | The `pssa-gate.Tests.ps1` "no `-EnableExit`" assertion is the regression guard, observed on green `stage-inspect` CI run #40 (`https://github.com/Gekko47/gantt-creator/actions/runs/34576783029`) |
| L11 | End-to-end code review methodology was previously implicit in chat memory; the next reviewer would have had to reconstruct it from the W-13 / Phase A/B/C commits alone. Phase D (2026-09-07) shipped `docs/06-LLM-PROTOCOL.md` "End-to-end code review methodology" section + `scripts/review-runbook.Tests.ps1` self-test so the next review is reproducible. Runbook test (6 Pester tests) observed on green `stage-inspect` CI run #40 (`adbded8`) | _closed_ | 2026-09-07 | next `stage-inspect` green CI run with this commit included | The runbook test (6 Pester tests) fires if any of: the methodology section is missing or partial, the four defect classes are renamed, the four worked-example tests are deleted, the W-11 single source of truth loses a consumer, or the W-12 branch policy is weakened. Observed on green `stage-inspect` CI run #40 (`https://github.com/Gekko47/gantt-creator/actions/runs/34576783029`) |
| L12 | The `--blame-hang-timeout` collector used by `verify-office.ps1` aborts the testhost ~0.2 s after start via `createdump`/hang dump; the Office integration suite passes only with the blame collector omitted on this machine | _Maintainer_ | 2026-09-11 | next Office verify-gate redesign | Replace `--blame-hang-timeout` with a hang-detection approach that does not terminate the testhost (e.g. external process watchdog with soft timeout) |
