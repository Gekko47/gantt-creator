# Work item — R2.2a

> **Guide status**: Approved Tier A, 2026-09-23. Implemented; live Office defined-name fixture is excluded after repeated setup failure, with contract coverage retained. Follow [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md) and [`../GanttCreator_R2_Implementation_Plan.md`](../GanttCreator_R2_Implementation_Plan.md).

## Outcome

Initialise adopts only a pristine worksheet. A cell-empty worksheet containing non-cell user state is preserved and the create-new-sheet path is used.

## Decisions

1. Adoption requires `CountA(UsedRange) == 0` and an `A1`-only used range.
2. Shapes, legacy comments/notes, threaded comments, worksheet/workbook names, tables, pivot tables, query tables, or hyperlinks make the sheet non-pristine.
3. Non-pristine does not mean refusal: preserve the sheet and create `Gantt Data` after it.
4. Existing config/table conflicts and protection remain typed zero-mutation refusals.

## Scope and tests

- `src/GanttCreator.Office/ExcelWorkbookInitialiser.cs`
- `tests/GanttCreator.Office.ContractTests/WorkbookInitialiserTests.cs`
- `tests/GanttCreator.Office.IntegrationTests/InitialiseSheetIntegrationTests.cs`

Contract tests cover every artefact category and mutation sets. Live Office tests exercise shape, comment, and threaded-comment preservation; the live defined-name fixture is excluded because Excel rejects the test's `Names.Add` setup before production code is called. Workbook- and worksheet-name readiness remain covered by contract tests.

Checklist: C, F, G, I, J, K. Office gate: Required.

## Commands

```powershell
dotnet test tests/GanttCreator.Office.ContractTests
dotnet test tests/GanttCreator.Office.IntegrationTests
pwsh ./scripts/verify-quick.ps1
pwsh ./scripts/verify-office.ps1
git diff --check
```

## Stop conditions

- A probe requires mutation or broad workbook takeover.
- A cell-empty artefact-bearing sheet would be adopted or modified.
- More than one helper worksheet would be created.

## Implementation evidence — Office gate pass with defined-name live case excluded

- Office contract suite: PASS, 138/138, including eleven non-pristine categories, including worksheet- and workbook-name readiness.
- Release build: PASS, 0 warnings, 0 errors.
- Live Office evidence: shape, legacy comment, threaded comment, and all pre-existing OfficeIntegration tests PASS. The live defined-name case was removed after four setup failures in `Names.Add`, all before `ExcelWorkbookInitialiser.Initialise`; production code was not exercised by those failures. Latest failure evidence remains in `scripts/_artifacts/office-evidence/office-20260924-002538086.trx`.
- The approved scope exclusion is documented in this guide; contract coverage remains for both worksheet- and workbook-name readiness.
- Office gate: **Required and passed for the approved scope**. R2.2a is ready to commit.
