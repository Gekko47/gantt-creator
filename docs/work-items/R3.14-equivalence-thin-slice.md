# Work item — R3.14

> **Guide status**: Authored (Tier A) 2026-09-21 · verified against `stage-inspect`
> commit `8d53d39` · not implemented. Prerequisite reads:
> [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md) implementer contract.
> Depends on: R3.1–R3.12 (the full scene model + fixture). Phase-3 exit row.

## Outcome

A contract test walks one bar, one external label, and one milestone
diamond through the built scene and asserts every field the
entity-guide equivalence table requires of all four renderers — a
missing field is a defect, not a renderer workaround — and the Phase-3
exit evidence (deterministic scene snapshot demonstration) is
assembled, including the manifest's Phase-4 Tier-B guide upgrade.

## Context and owed deferrals

Roadmap row: *Prove the scene model with a cross-renderer
primitive-equivalence thin slice — A contract test walks one bar, one
external label, and one milestone diamond through the built scene and
asserts every field the entity-guide equivalence table requires of all
four renderers; a missing field is a defect, not a renderer workaround.
A gap that serialization alone cannot prove stops for a spike-plus-ADR
under the scope-change protocol, with the spike kept outside production
code — None.*

## Governing contracts

- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Entity-to-renderer equivalence" (the per-entity field table across
  the Excel live, editable-export, PowerPoint, and PNG renderers —
  the three thin-slice rows: span bar, external description label,
  milestone diamond) and "Shared entity contract".
- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Scene-first
  rendering" (renderers translate representation only).
- [`../03-ROADMAP.md`](../03-ROADMAP.md) — Phase-3 exit demonstration
  and the scope-change protocol (the gap path).
- [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md) — phase-exit
  obligations: Tier-B upgrade of the Phase-4 guides before any of them
  is implemented.

## Step 0 — verify prerequisites (current source)

- R3.1–R3.12 landed; `ReferenceSceneFixture` and `SceneSnapshot`
  exist.
- "Entity-to-renderer equivalence" re-read; the three rows' field
  lists are transcribed into the test **exactly** — the field list is
  the contract under test.

## Scope

- `tests/GanttCreator.Core.Tests/Scene/EquivalenceThinSliceTests.cs`
- `src/GanttCreator.Core/Scene/EquivalenceFields.cs` (named-field
  catalogue transcribed from the guide table, if a shared constant
  home is cleaner than test-local data — Office-free either way)
- Phase-3 exit evidence: this file's Notes + STATUS
- Phase-4 guides `R4.1`–`R4.10` (Tier-B → Tier-A upgrade — step 6)
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line

## Exclusions

- No renderer implementation (Phase 4/6/7); no golden images (R8.4);
  no serialization change (R3.12's snapshot reused); no new dependency.

## Design decisions

- **D1 — The thin slice is a scene-field assertion, not a render.**
  For each of the three entities the test builds through the real
  builders (fixture-scale input), then asserts the scene carries every
  equivalence-table field with a non-default, typed value: geometry
  (points), resolved style tokens (fill/stroke/width/hatch where the
  row names them), text + resolved text bounds + side (label),
  z-layer + deterministic order key, stable role-derived ID, owner ID,
  and the ownership-tag data the Excel layer will need
  (`GanttCreator.` prefix contract). A required-but-missing field =
  failing test = a defect in the model — never a renderer-side
  workaround later.
- **D2 — The gap path.** If a required field cannot be proven from the
  scene + serialization alone, stop: spike outside production code,
  draft the ADR, follow the scope-change protocol. No speculative
  model extension in this row.
- **D3 — Exit evidence.** Run the exit demonstration (fixture →
  `SceneSnapshot.Serialize` → determinism proof, recorded here +
  STATUS); `pwsh ./scripts/verify.ps1` green; upgrade the ten Phase-4
  guides to Tier A (prerequisites re-verified against landed Phase-3
  code; exact files/tests/commands; refresh date in each status line)
  **before any Phase-4 implementation starts**.

## Implementation steps (in order)

1. Transcribe the three equivalence-table rows (Step 0) into the field
   catalogue.
2. `EquivalenceThinSliceTests` per D1 — three assertions, each
   covering the complete field set; **positive test**: a deliberately
   field-stripped scene variant fails the assertion (proves the test
   detects missing fields).
3. Run the exit demonstration: build `ReferenceSceneFixture` →
   `SceneSnapshot.Serialize` → record the snapshot path + determinism
   proof in Notes; `pwsh ./scripts/verify.ps1` green.
4. If any D2 gap appears → stop (spike + ADR path).
5. Upgrade the ten Phase-4 guides to Tier A (D3): re-verify every
   prerequisite against landed code; record the refresh date in each
   status line; update the manifest tiers.
6. Docs: REPO-MAP, STATUS (phase-exit record), status lines, manifest.
   One commit for the test: `test: R3.14 …` — Checklist: C, E, I; the
   guide upgrade may be a separate `docs:` commit (one concern each).

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Core.Tests`** (new
  `EquivalenceThinSliceTests`): three field-complete assertions + the
  missing-field positive.
- **Automated — `pwsh ./scripts/verify.ps1`** (phase exit): all steps
  green.
- No Office gate (row: None) — Phase 3 never required Office.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Core.Tests
dotnet test tests/GanttCreator.Architecture.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify.ps1            # phase exit — expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A required field is missing from the scene → failing test → stop and
  fix the model (or the D2 spike+ADR path if unprovable).
- The equivalence table's three rows conflict with the landed model →
  stop; report (scope-change protocol).

## Risk and rollback

Main risk: the field list drifting from the guide — mitigated by exact
transcription at Step 0 and a section citation in the catalogue's XML
docs. Rollback: `git revert`; the test is additive.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **C, E, I**:
thin-slice assertions green with the missing-field positive; exit
demonstration recorded; `verify.ps1` green; Phase-4 guides upgraded to
Tier A with refresh dates; REPO-MAP/STATUS current; no unrelated
refactor.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| The thin slice covers exactly one bar, one external label, one milestone diamond | fact | roadmap row R3.14 |
| Every entity has equivalent representations across the four renderers unless explicitly excluded | fact | entity guide "Shared entity contract" + "Entity-to-renderer equivalence" |
| A gap serialization cannot prove takes the spike+ADR path, spike outside production code | fact | roadmap row R3.14 |
| Phase-3 exit adds no new compatibility-matrix rows (Phase 4 adds zoom/scale) | fact | roadmap "Cross-phase compatibility matrix" phase-exit subsets |

