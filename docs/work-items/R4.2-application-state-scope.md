# Work item — R4.2

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit. Depends on: R4.1 (ports).

## Outcome

One tested application-state scope saves and restores
`ScreenUpdating`, `EnableEvents`, `DisplayAlerts`, calculation mode,
status bar, and selection — only when changed, always in `try/finally`
— with fake-state tests covering every property and injected failure
point, and a live forced-failure Office gate proving restoration.

## Context and owed deferrals

Roadmap row: *Add application-state scope with restoration after
success/failure — Fake-state tests cover every property and injected
failure point — Required: force a live command failure and confirm
Excel state is restored.*

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "COM ownership and
  application state" ("Use one tested state-scope type to save and
  restore application settings") and "C# and architecture conventions"
  (save/restore `ScreenUpdating`, `EnableEvents`, `DisplayAlerts`,
  calculation mode, status bar, and selection only when changed, using
  `try/finally`).
- `AGENTS.md` — same six-property list; "Excel/PowerPoint calls execute
  on the required STA/main thread".
- `docs/08-TEST-CHECKLIST.md` section **J** (state restored even after
  failure) and **K** (failure injection around each application-state
  change).

## Step 0 — verify prerequisites

- R4.1 landed (the write ports the scope wraps).
- No existing state-scope type (search `src/GanttCreator.Office/` —
  if one landed earlier, this row is its residual: tests + gaps only).
- The six properties' interop shapes probed from the installed PIA
  (read/write access) before the adapter is written.

## Scope

- `src/GanttCreator.Office/` — the state-scope type over the Excel
  application (one type; naming at Tier-A upgrade) + `internal
  virtual` seams over each property read/write for Moq.
- `tests/GanttCreator.Office.ContractTests/` — fake-state tests.
- `tests/GanttCreator.Office.IntegrationTests/` — live forced-failure
  test (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No renderer logic; no Refresh command (R4.9 wires the scope in); no
  PowerPoint state (Phase 6 owns its scope needs via the same pattern);
  no new port beyond R4.1's.

## Design decisions (behavioural)

- **D1 — Capture-on-change.** The scope records each property's
  original value only when the command actually changes it; restore
  writes only what was captured. Unchanged properties are never
  touched (conventions list).
- **D2 — Dispose/`try/finally` equivalence.** The scope restores in
  `finally` semantics whether used via `using` or explicit
  try/finally; restore failure cannot skip the remaining properties.
- **D3 — Selection restore is best-effort and ordered last.** Selection
  restore can fail on edge hosts; it degrades with a logged technical
  record (operation ID) and never masks earlier restores.
- **D4 — Failure injection points.** Every property write is an
  injectable seam; the fake-state tests inject a failure at *each*
  point and assert full restoration of everything captured so far
  (checklist K).

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Office.ContractTests`**:
  per-property capture/restore pins; no-write-when-unchanged pin
  (**positive test**); injected failure at each of the six properties
  → everything captured is restored (**positive tests**); restore
  failure degrades without skipping (positive test).
- **Office host (Required)** — `pwsh ./scripts/verify-office.ps1` +
  F5: force a live command failure mid-render (debug hook or
  protected-sheet path); confirm all six properties restored; record
  host Windows/Office build (Phase 4 note: every Office evidence row
  from Phase 4 onward carries the builds).

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A seventh property appears necessary → stop; the six-property list
  is the architecture contract; extending it is an architecture
  revision, not a local choice.
- Selection restore proves impossible on a supported host → stop;
  record evidence; the degradation policy (D3) needs the human's nod
  before shipping.

## Risk and rollback

Main risk: property-ordering bugs on partial failure — mitigated by
per-point injection tests. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **G, J, K**:
all properties + failure points covered with positives; live gate
recorded with host builds; Release 0 warnings; XML docs; REPO-MAP/
STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| The six-property save/restore list is the contract | fact | architecture conventions + AGENTS.md |
| One tested state-scope type (not several) | fact | architecture "COM ownership and application state" |
| From the first Phase 4 Office gate, evidence rows record host Windows/Office builds | fact | roadmap Phase 4 note |
| Selection restore can fail on edge hosts | inference | D3 degradation; verify on the reference host at the live gate |
