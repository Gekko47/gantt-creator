# Work item — R2.4

## Outcome

Bulk-read every body row of `tblGanttData` into immutable, culture-invariant neutral `GanttRowDto`s via a single `Range.Value2` read plus the workbook `Date1904` flag. Identical serials read identically under GB (`DD/MM/YYYY`) and US (`MM/DD/YYYY`) display formats. 1904 workbooks are explicitly refused (ADR-0006). No validation, no rendering, no cell mutation.

## Scope

- `docs/work-items/R2.4-neutral-row-read.md` (this file)
- `docs/adr/0006-reject-1904-date-system.md`
- `src/GanttCreator.Core/GanttRowDto.cs` — immutable `sealed record`
- `src/GanttCreator.Core/ExcelCellConverter.cs` — pure `Value2` converters
- `src/GanttCreator.Office/IGanttTableReader.cs` — narrow port
- `src/GanttCreator.Office/GanttTableReadOutcome.cs` — typed outcome
- `src/GanttCreator.Office/ExcelGanttTableReader.cs` — PIA implementation
- `tests/GanttCreator.Core.Tests/GanttRowDtoTests.cs`, `ExcelCellConverterTests.cs`
- `tests/GanttCreator.Office.ContractTests/GanttTableReaderTests.cs`
- `tests/GanttCreator.Office.IntegrationTests/GanttTableReaderIntegrationTests.cs`
- `docs/REPO-MAP.md`, `docs/STATUS.md`

### Decisions recorded here

- **D1 — Bulk `Value2`, never `.Text`.** One `DataBodyRange.Value2` read into `object[,]`. `.Text` is locale display text; `Value2` is locale-invariant (`double` dates, `string`/`double`/`bool` otherwise). Empty table (`DataBodyRange is null`) → `Ok([])`.
- **D2 — Header-name mapping, Ordinal.** `ListColumns[i].Name` → schema index via `GanttTableSchema.Default.TryGetColumn` (Ordinal). Tolerates column reorder; missing required header → `TableMissing`.
- **D3 — Converter lives in Core, takes primitives.** `TryConvertDate(object?, out DateOnly?)` etc. Core never references COM; Office passes `Value2` cells only. Keeps Core testable without Office and `CoreBoundaryTests` green.
- **D4 — Blank/error semantics (U4).** Blank (`null`/empty/whitespace text; `null` dates) → `null` DTO field; R2.5 decides requiredness. Excel error cells surface as `int` in `Value2` → `null` field, row preserved, no throw.
- **D5 — Trimming without rewriting.** Text cells `Trim()`ed, Ordinal, no case-folding (mirrors R2.1 D4 / R2.3 D2). Original cell untouched.
- **D6 — COM ownership (R2.2 precedent).** One proxy per local, no chained expressions, `for`-index loops (no `foreach` on COM collections), no `FinalReleaseComObject` on shared roots, STA thread only, `internal virtual` seams for every indexer (CS0855).
- **D7 — 1904 rejected (U1).** `Workbook.Date1904 == true` → `DateSystemUnsupported` before touching the table. See ADR-0006. Schema stays v1.
- **D8 — GB pinned, US as contrast (U2).** Live test Format A = GB `DD/MM/YYYY`, Format B = US `MM/DD/YYYY`; identical DTOs both ways proves no `.Text` parsing.

## Exclusions

- Domain validation/all-errors mapping (R2.5): unknown Type, duplicate/missing IDs, reversed/missing dates, colour/label capability checks, `Custom Activity` style-key requirement, clipping.
- Row error reporting/UI, any cell mutation (R2.6).
- Settings/style/metric tables, save/reopen (R2.7); add-row commands (R2.8); `tblGanttTypes` materialisation, dropdown (R2.9); repair/migration (R2.10).
- Scene/geometry/rendering (R3+), Ribbon, PowerPoint, PNG, new NuGet packages, schema-version bump.



## Acceptance tests

- **Automated** — `dotnet test tests/GanttCreator.Core.Tests` (all green incl. new tests): serial table (`1`, `60` pinned 1900 leap-bug, `61`, leap day 2024-02-29, year boundary, one-day span); edges/non-finite (`0, -1, 2958466, NaN, ±Infinity`) → `null`, no throw; blank → `null`; `DateTime` → `DateOnly`; `string` never date-parsed; `bool` in date cell → `null`; `StackIndex` (`1.0→1`, `1.5/NaN→null`, `"3"→3`); `Visible` (`bool` as-is, `1→true, 0→false`, blank → `null`); validator positives per new guard in same commit; culture invariance under `tr-TR` and `de-DE`.
- **Automated** — `dotnet test tests/GanttCreator.Office.ContractTests`: header-reorder map, blank preservation, serial→`DateOnly`, error-`int`→`null`, each refusal with no-mutation verification (no `Value2` set, no `Sheets.Add/Delete`).
- **Automated** — `dotnet test tests/GanttCreator.Architecture.Tests`: `CoreBoundaryTests` green.
- **Office host (Required)** — `pwsh ./scripts/verify-office.ps1` + new integration tests: initialise blank workbook, write representative rows, read under GB `DD/MM/YYYY` then US `MM/DD/YYYY`, assert identical DTOs + correct `DateOnly`s; 1904 workbook asserts refusal. Record Office build.

## Evidence commands

```powershell
dotnet test tests/GanttCreator.Core.Tests           # expect all pass
dotnet test tests/GanttCreator.Office.ContractTests # expect all pass
dotnet test tests/GanttCreator.Architecture.Tests   # expect all pass
pwsh ./scripts/verify-quick.ps1                     # expect exit 0
git diff --check                                    # expect no output
```

Office gate: `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 600` — Required, expect exit 0.

## Risk and rollback

COM seam drift (mirrored R2.2 seam pattern + live parity test); 1904 scope creep (closed by D7/ADR-0006). Rollback: `git revert` of the single commit; pure addition, first consumer is R2.5.

## Definition of done

From `AGENTS.md`: acceptance criteria have passing automated tests; format, analyzers, Release build, unit tests, coverage pass; no warning suppressed without reason; no test weakened/deleted; every public member has XML docs; every new validator has a same-commit positive test; new Office calls behind adapter port with contract + integration tests; no `FinalReleaseComObject` on shared roots; no chained COM; workbook stays one visible sheet + one VeryHidden helper; REPO-MAP and STATUS current; diff contains no unrelated refactor.

## Notes during implementation

- `Range.Value2` for dates is `double` (OLE Automation date). `60` = 1900-02-29 phantom (inherited leap bug via `DateTime.FromOADate`); pin with comment, do not "fix".
- Excel error cells (`#N/A` etc.) surface as `int` in `Value2`; PIA-derived table
  verified as runtime evidence in a live integration test (`tests/.../GanttTableReaderIntegrationTests`, commit `6d2e3f1`).

### Live-Office evidence (2026-09-20)

- The first R2.4 reader-test body-row fix (`Read_returns_locale_invariant_dtos_under_gb_and_us_display_formats` — switching from assuming body-row adjacency to locating each row by the offset its returned `ListRow` actually occupies) originally landed in the single-commit rider `bbb45a8` (test(r2.2): correct live-office sheet expectations to the adopted-blank contract), because both sets of changes were staged together; per `docs/05-GIT-QUALITY.md` and `AGENTS.md` the commit was not rewritten, and the rider is documented in the R2.2 work item and here so later reviewers are not misled when diffing either commit. That fix was later superseded by `4e4380f`.

- `pwsh ./scripts/verify-office.ps1 -DeadlineSeconds 600` PASS: Office `16.0.20326.20144` (O365HomePremRetail, x64, en-us), 16/16 OfficeIntegration PASS in 5 m 44 s. `verify-quick.ps1` PASS in 149.6 s (all 13 steps).
- Table geometry: after the integration test cleared a one-row body, two `ListRows.Add()` calls produced `ListRows.Count=3` with body `A2:N4`; the created rows landed at body offsets 1 and 3 with an untracked blank body row between them. The reader returned one DTO per body row (correct); the test now locates rows by the offset each returned `ListRow` occupies and asserts every non-written body row is wholly blank (commit `4e4380f`). This is also the first live confirmation that a blank body row survives the read as an all-`null` DTO rather than being collapsed — R2.5 owns the requiredness decision for it.
- Locale invariance: the same `44931` serial displayed as GB `05/01/2023` and US `01/05/2023`; both reads returned identical `GanttRowDto` instances by record equality, and the test asserts the two display strings differ, so a `.Text`-parsing regression cannot pass.
- Harness observation (R1.6 scope, not this work item): the Office suite can leave `EXCEL.EXE` instances alive after the fixture's own teardown (5 remained after the gate run; the fixture manifest listed 19 launched PIDs), and the next `verify-quick.ps1` then fails its `build Release -warnaserror` step because ExcelDnaPack cannot delete `GanttCreator.AddIn-AddIn64-packed.xll` ("Perhaps loaded in Excel?"). Killing the strays and re-running passed; `verify-office.ps1` sweeps harness-owned Office processes only on its timeout path.
- R1.6 five-cycle packed-XLL log-watch flake: failed once on its 60 s poll bound (run 2, `RegisterXLL` returned true and the XLL loaded), passed at 90 s (run 3) — widened in `4d75ae3` with that evidence; the assertion is intact and a second failure at 90 s is a defect.
