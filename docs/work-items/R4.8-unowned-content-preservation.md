# Work item — R4.8

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit. Depends on: R4.1–R4.7.

## Outcome

Unowned shapes and cells, and the active worksheet, are provably
untouched by refresh — sentinel shape/cell and active-sheet tests pin
it, and a live gate refreshes with manual content present and proves
it unchanged.

## Context and owed deferrals

Roadmap row: *Preserve unowned shapes/cells and active worksheet —
Sentinel shape/cell and active-sheet adapter tests — Required:
refresh with manual content present and prove it is unchanged.*

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Live Excel
  renderer" (owns only `GanttCreator`-tagged shapes; never deletes or
  reformats unowned content); "Error handling" (Excel settings and
  selection restored).
- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Ownership, editability, and refresh".
- [`../04-TEST-STRATEGY.md`](../04-TEST-STRATEGY.md) — "Excel shape
  contract tests" (unowned shape and cell preservation; no extra
  worksheet creation).
- `AGENTS.md` — "Preserve unrelated user changes".

## Step 0 — verify prerequisites

- R4.1–R4.7 landed; the ownership filter (`GanttCreator.` tag) is the
  only mutation key (R4.3 D2 / R4.7 D1).
- The sentinel test pattern from R2.6 (byte-compare `Value2` matrices)
  re-read — the same discipline applies to cells here.

## Scope

- `tests/GanttCreator.Office.ContractTests/` — sentinel shape/cell +
  active-sheet adapter tests over the R4.1 fakes.
- `src/GanttCreator.Office/` — any hardening the tests force (e.g.
  explicit active-sheet assertion in the refresh path).
- `tests/GanttCreator.Office.IntegrationTests/` — live
  manual-content gate (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No reconciliation changes (R4.7 owns the algorithm; this row owns
  the preservation proofs); no multi-workbook behaviour beyond the
  active-workbook rule (R1.5 owns ribbon state; the compatibility
  matrix owns the cross-workbook row at exits).

## Design decisions (behavioural)

- **D1 — Sentinels, not assumptions.** The contract fixtures plant
  unowned shapes (with and without user names), user cells (values +
  formats), and a non-active sheet; refresh runs; every sentinel's
  `Value2`, formula, format, name, position, and the active-sheet
  identity are asserted unchanged. Deletion candidates that match by
  *geometry overlap* but lack the ownership tag are never touched
  (**positive test**).
- **D2 — No extra worksheets, ever.** Every refresh path asserts the
  sheet count and identities are unchanged (test-strategy rule).
- **D3 — Active worksheet is never switched.** The refresh path never
  activates another sheet; a contract test asserts zero
  activate-operations (**positive test**).
- **D4 — Failure paths preserve too.** Each injected failure point
  (checklist K) leaves sentinels unchanged and state restored.

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Office.ContractTests`**:
  the sentinel matrix (shape/cell/format/name/position/active-sheet)
  green after refresh; unowned-overlap never deleted (**positive
  test**); no-sheet-added pin; failure-path preservation pins.
- **Office host (Required)** — live: add manual content (a user shape,
  edited cells with formats) around the chart; refresh; prove it
  unchanged; record host builds.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- Any refresh path must touch an unowned object to succeed → stop;
  the design is wrong, not the test.
- A host behaviour reformats cells as a side effect of shape
  operations (e.g. table auto-format) → stop with evidence; scope-
  change path.

## Risk and rollback

Main risk: indirect mutations (selection side effects, table
  auto-expansion) — the sentinel matrix exists to catch exactly
  these. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **E, J, K**:
sentinel matrix green with positives; live manual-content gate
recorded with host builds; Release 0 warnings; XML docs; REPO-MAP/
STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| The live renderer owns only tagged shapes; never deletes/reformats unowned content | fact | architecture "Live Excel renderer" |
| Unowned shape/cell preservation and no-extra-worksheet are contract-test obligations | fact | test strategy "Excel shape contract tests" |
| Cell preservation asserts use the R2.6 byte-compare discipline | fact | R2.6 pattern (`Value2` matrices) |
| Active-sheet identity is part of the preserved state | fact | roadmap row R4.8 |
