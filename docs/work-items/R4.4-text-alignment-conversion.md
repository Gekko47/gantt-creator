# Work item — R4.4

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit. Depends on: R4.1–R4.3; Phase 3
> text primitives + resolved label bounds.

## Outcome

Scene text renders with the token font, mapped alignment, and the
scene-resolved overflow (ellipsis already applied) — the renderer
never re-measures, re-truncates, or reselects a label side — pinned by
text-frame, font, alignment, and overflow contract tests and a live
label inspection gate at normal and clipped lengths.

## Context and owed deferrals

Roadmap row: *Render text, font, alignment, and overflow policy —
Text-frame, font, alignment, and overflow contract tests — Required:
inspect representative labels at normal and clipped lengths.*

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Coordinate and
  rounding policy" (renderers consume the resolved text bounds and may
  not choose a different label side); "Live Excel renderer".
- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) — §22
  (text measurement and overflow rules), typography tokens
  (`FontFamily` `Aptos`; the six size/bold tokens), §2 title alignment
  (centred, middle), §4 header alignment.
- R3.6 D6 (ellipsis-at-`MaximumExternalLabelWidthPt` overflow policy).

## Step 0 — verify prerequisites

- R4.1–R4.3 landed; Phase-3 `SceneText` carries text, resolved
  `TextBounds`, typography token, and alignment (verify exact
  members).
- R8.2a's font-pinning ADR is **not** required yet — this row consumes
  the `FontFamily` token; pinning/provisioning is the Phase 8 ADR
  (D-G4). If a live host lacks the token font, record it; do not
  substitute silently.

## Scope

- `src/GanttCreator.Office/` — the text rendering half (TextFrame
  population, font, alignment mapping).
- `tests/GanttCreator.Office.ContractTests/` — text contract tests.
- `tests/GanttCreator.Office.IntegrationTests/` — live label
  inspection (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No text measurement (the scene already measured — R3.6's
  `ITextMetrics`); no overflow decisions (R3.6 D6 already applied);
  no font pinning/provisioning (R8.2a / D-G4); no panel cells (R3.11
  primitives render like any text here, but the panel builder itself
  is Phase 3).

## Design decisions (behavioural)

- **D1 — Consume, never re-measure.** The renderer writes the
  scene's text and positions the shape at the scene's resolved
  `TextBounds`; a contract test asserts no call to any measure API and
  no side change vs the scene's chosen side (**positive test**: a
  scene label placed Left stays Left even when Right looks emptier).
- **D2 — Font from the token.** Family/size/bold map from the
  typography token to the Office font properties; nothing is invented
  between token and property (the full matrix test lands in R4.6).
- **D3 — Alignment mapping is closed.** Centred/middle for title and
  header entities; label alignment per the scene text's alignment
  member (from §22/§2/§4). The mapping table is fixed at Tier-A
  upgrade and pinned by tests.
- **D4 — Overflow text renders as given.** The ellipsis is already in
  the scene text; the renderer applies no truncation of its own
  (assertion: text passed through verbatim, **positive test** with an
  over-long label fixture).

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Office.ContractTests`**:
  font/alignment matrix pins; verbatim-text pin; no-measure pin;
  bounds-within-tolerance pin.
- **Office host (Required)** — live: inspect representative labels at
  normal and clipped lengths (font name/size/bold, alignment, no
  re-truncation, side unchanged); record host builds + whether the
  token font was present (feeds R8.2a).

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A live host substitutes a missing font silently → stop; record it —
  that is exactly the R8.2a/D-G4 input (never an unreported
  substitution).
- Alignment cannot be expressed for a scene alignment member → stop;
  extend the scene contract via the scope-change protocol, not a
  renderer workaround.

## Risk and rollback

Main risk: hidden re-measurement in the Office text path — mitigated
by the no-measure contract test and the live inspection. Rollback:
`git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **E, J**:
contract pins green; live gate recorded with host builds + font
presence; Release 0 warnings; XML docs; REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| Renderers consume resolved text bounds and may not choose a different label side | fact | architecture "Coordinate and rounding policy" + §22 |
| Overflow ellipsis is scene-side; renderer applies nothing extra | fact | R3.6 D6 + §22 "Overflow" |
| Token font is `Aptos`; missing fonts must error or use an approved fallback, never silent substitution | fact | entity guide typography tokens + §22 note |
| Live-host font availability feeds R8.2a | fact | D-G4 register entry |
