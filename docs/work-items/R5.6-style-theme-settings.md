# Work item — R5.6

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 4 exit. Depends on: R2.7 (catalogues +
> settings storage), R2.10 (repair/migration), R4.6 (style mapping).

## Outcome

Style/theme settings dialogs edit colours, heights, and diamond size
through explicit local defaults (no Office theme inheritance, fully
offline) — writing `tblGanttStyles`/`tblGanttSettings` through the
R2.7 writer, validated on write and read, with live Excel output
compared against the approved tokens.

## Context and owed deferrals

Roadmap row: *Add style/theme settings using explicit local defaults —
Style round-trip, validation, and offline-dependency tests — Required:
edit colours/heights/diamond size and compare Excel output with
approved tokens.*

This row implements the "approved Ribbon dialogs" write path that
R2.7 D4 reserved for `tblGanttStyles`/`tblGanttSettings`.

## Governing contracts

- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "VeryHidden catalogue and style rules" ("`tblGanttStyles` and
  `tblGanttMetrics` are changed through approved Ribbon dialogs; the
  add-in writes and validates the underlying rows"), the token tables
  (ranges), and the style-dialog contrast warning ("must show a
  contrast warning when text/fill combinations are difficult to read.
  It may not silently replace an entered colour").
- R2.7 (storage + validation), R2.10 (integrity on read).

## Step 0 — verify prerequisites

- R2.7/R2.10/R4.6 landed (writer/reader/repair/mapping).
- The token tables re-read: the dialog's editable set is exactly the
  token rows (colours, heights incl. `ActivityHeightPt`, diamond size
  `MilestoneSizePt`) with their valid ranges; no token is editable
  outside its range.

## Scope

- `src/GanttCreator.Core/` — settings/style value model + range
  validation (reuse R2.7 catalogues).
- `src/GanttCreator.Office/` — dialog-driven writes via the R2.7
  writer (bulk, guard-first).
- `src/GanttCreator.AddIn/` — the dialog(s) (WinForms `TaskDialog`
  family or forms — fixed at Tier-A upgrade) + ribbon entry points.
- Tests per layer + integration (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No user *named-style* creation (that is the style-preset surface —
  verify at Tier-A upgrade whether a preset-management row exists in
  the current roadmap; if not, named-custom-style creation is out of
  scope and recorded); no per-row overrides (R5.10); no rendering
  changes (Refresh applies); no theme/Office-colour inheritance (D1).

## Design decisions (behavioural)

- **D1 — Explicit local defaults, never Office themes.** Every value
  is an explicit token value stored on `_GanttCreatorConfig`; the
  dialog never binds to Office theme colours; the feature works fully
  offline (offline-dependency tests pin no network/theme access).
- **D2 — Range-validated writes.** Each edit validates against the
  token's range (R2.7) before writing; out-of-range input is refused
  with the token's range in the message (**positive test**); the
  contrast warning fires for difficult text/fill pairs and never
  silently replaces the entered colour (entity-guide rule).
- **D3 — Round-trip + repair integration.** Values round-trip through
  save/reopen (R2.7 acceptance reused); damaged values surface through
  R2.10's integrity findings, not ad-hoc reads.
- **D4 — Live comparison target.** The Office gate edits
  colours/heights/diamond size and compares the rendered Excel output
  with the approved token values (the entity-guide tables are the
  specification).

## Acceptance tests

- **Automated**: Core — range validation matrix with positives;
  contrast-warning policy pins. Office — write mutation sets
  (exactly the intended rows); refusals. AddIn — dialog contract
  (cancel writes nothing — **positive test**).
- **Office host (Required)** — live: edit the three value families;
  Refresh; compare output against the tokens; record host builds.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Core.Tests
dotnet test tests/GanttCreator.Office.ContractTests
dotnet test tests/GanttCreator.AddIn.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A value needs to exceed its documented token range → stop; the
  tables are the contract (product-owner amendment path).
- Silent colour substitution anywhere → stop (entity-guide rule).

## Risk and rollback

Main risk: dialog-write drift from the R2.7 writer contract —
mitated by reusing the writer and its tests. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **D, G, I, J**:
range/contrast/round-trip matrices green with positives; offline pins;
live comparison recorded with host builds; Release 0 warnings; XML
docs; REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| Styles/metrics change only through approved dialogs; the add-in writes and validates the rows | fact | entity guide "VeryHidden catalogue and style rules" |
| Contrast warning required; never silent replacement | fact | entity guide typography section note |
| Token ranges are the validation contract | fact | entity guide token tables |
| No named-custom-style creation row exists in the current roadmap | fact | roadmap Phase 5 table (verified at Tier-A upgrade against any later revision) |
