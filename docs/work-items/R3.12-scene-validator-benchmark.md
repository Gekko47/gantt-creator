# Work item — R3.12

> **Guide status**: Authored (Tier A) 2026-09-21 · verified against `stage-inspect`
> commit `8d53d39` · not implemented. Prerequisite reads:
> [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md) implementer contract.
> Depends on: R3.1–R3.11 (the full builder set), R2.7 (catalogues).

## Outcome

A `SceneValidator` proves every built scene invariant-clean, a
deterministic `ReferenceSceneFixture` produces the Phase-3 exit
snapshot (planned/actual overlaps, three events on one lane, critical
segments, milestones, two labelled delineators), and a 1,000-event
benchmark records validate+build time under the 250 ms Core budget.

## Context and owed deferrals

Roadmap row: *Add scene invariant validator and representative
1,000-event benchmark — Zero invalid geometry and recorded benchmark
under the Core budget — None.*

Phase 3 note: R3.12's artifact is the deterministic scene snapshot and
the benchmark only — no PNG renderer exists until Phase 8, so L4
cannot close here; its replacement gate is the first approved
representative render (R8.4). Re-date L4 when KNOWN-LIMITATIONS is
next revised.

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Key rules" (no
  NaN/Infinity/negative extents; deterministic ordering), performance
  budgets (1,000 events: under 250 ms on the reference machine; record
  hardware with results), scene-first pipeline.
- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Minimum visual reference fixture".
- `docs/08-TEST-CHECKLIST.md` section **B** (walk the built scene
  asserting no NaN/Infinity/negative extents).

## Step 0 — verify prerequisites (current source)

- R3.1–R3.11 landed; a `SceneBuilder` orchestration exists or is
  assembled here (D1).
- "Minimum visual reference fixture" re-read; D2 matches exactly.

## Scope

- `src/GanttCreator.Core/Scene/SceneBuilder.cs` (orchestration, if not
  already landed piecewise), `SceneValidator.cs`,
  `SceneValidationReport.cs`, `ReferenceSceneFixture.cs`
- `tests/GanttCreator.Core.Tests/Scene/SceneValidatorTests.cs`,
  `ReferenceSceneFixtureTests.cs`, `SceneBenchmarkTests.cs`
- `tests/golden/scene/reference-scene.json` (snapshot baseline, D4)
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line

## Exclusions

- No PNG/golden images (L4 stays open; R8.4); no renderers; no
  benchmarking package or any new dependency (D3); no CI timing
  assertion (D3).

## Design decisions

- **D1 — One orchestrator.** `SceneBuilder` composes settings +
  catalogues + validated events + measured grid → `TimeScale` →
  `LaneLayoutBuilder` → `FrameBandsBuilder` → `LaneEventLayout` →
  span/milestone/overlay/delineator/panel builders → ordered
  `GanttScene` + warnings. Single entry point for Phase 4+.
- **D2 — `ReferenceSceneFixture`** matches the entity guide's minimum
  visual reference fixture plus the exit-demonstration contents;
  fully deterministic (fixed IDs, dates; no randomness).
- **D3 — Benchmark without a new dependency.** A `Stopwatch` harness
  in `SceneBenchmarkTests` (always runs, never fails on hardware
  variance): asserts correctness at 1,000 deterministic synthetic
  events and records elapsed time to test output; the **250 ms
  judgement is recorded evidence** on the reference machine
  (hardware + elapsed + verdict in this file's Notes), per the
  architecture's "record the reference hardware" rule.
- **D4 — Golden scene snapshot.** The fixture's `SceneSnapshot`
  serialization is committed as
  `tests/golden/scene/reference-scene.json` — a *scene* baseline, not
  a golden image (no human image review), but the same change policy:
  a stated reason and a dedicated commit for any update. Determinism
  pin: rebuilding reproduces the file byte-for-byte.

## Implementation steps (in order)

1. `SceneBuilder` orchestration (D1) with typed refusals for invalid
   inputs (**positive tests**).
2. `SceneValidator` (invariants): every primitive — finiteness,
   non-negative extents, z-ascending order + deterministic tiebreaks,
   ID uniqueness + role format, group children resolve, label
   containment in chart bounds, warnings deduplicated; typed report,
   zero throwing.
3. `ReferenceSceneFixture` (D2) + golden snapshot (D4).
4. Tests: validator positives (**a deliberately broken scene per
   invariant — NaN, negative extent, duplicate ID, unresolved group
   child, out-of-bounds label, unordered layers — each detected**);
   fixture determinism (build twice → byte-identical; ≥3 shuffles →
   identical); golden-file match; benchmark harness (correctness +
   recorded elapsed at 1,000 seeded synthetic events).
5. Record the reference-machine benchmark result in this file's Notes
   (human or self-hosted runner: hardware, elapsed, verdict vs 250 ms).
6. Docs: REPO-MAP, STATUS, status line (+ optional L4 re-date note).
   One commit: `feat: R3.12 …` — Checklist: B, E, G, I.

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Core.Tests`** (new
  `Scene/` files): validator positives, fixture determinism, golden
  match, benchmark correctness.
- **Automated — `dotnet test tests/GanttCreator.Architecture.Tests`**:
  boundaries unchanged.
- No Office gate (row: None).

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Core.Tests
dotnet test tests/GanttCreator.Architecture.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify.ps1            # branch-final — expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- The benchmark exceeds 250 ms on the reference machine → profile,
  record, and stop for a decision (architecture: "Do not optimise
  before profiling").
- The golden snapshot will not reproduce byte-identically → stop; find
  the nondeterminism (checklist B failure); never loosen the comparison.

## Risk and rollback

Main risk: golden-file brittleness (later entity changes update it) —
accepted: updates are dedicated commits with stated reasons. Rollback:
`git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **B, E, G, I**:
validator positives green; fixture + golden determinism proven;
benchmark recorded; Release build 0 warnings; XML docs; REPO-MAP/
STATUS current; no unrelated refactor.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| Core budget: validate + build 1,000 events < 250 ms on the reference machine | fact | architecture "Performance budgets" |
| L4 cannot close at Phase 3; replacement gate is R8.4's first approved render | fact | roadmap Phase 3 note (R3.12) |
| Exit fixture contents (overlaps, 3-per-lane, critical, milestones, 2 delineators) | fact | roadmap exit demonstration + entity guide "Minimum visual reference fixture" |
| Stopwatch harness + recorded evidence satisfies "recorded benchmark" without a new dependency | proposal | D3; confirm at review if a benchmarking package is preferred (dependency approval required) |

