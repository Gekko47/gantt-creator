# Work item — R8.1

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 7 exit. Depends on: the landed
> `GanttCreator.Raster` `ExportSize` (R0.5 sample, hardened).

## Outcome

The width/unit parsing residual is verified against the landed
`ExportSize`: the R8.7 preview value feeds it correctly, its
acceptance criteria link to the existing `ExportSizeTests`, and no
duplicate parser is introduced anywhere.

## Context and owed deferrals (residual row)

Roadmap row: *Add width/unit parsing, aspect calculation, rounding,
and safety limits — Table-driven cm/in/px, ratio, rounding, and
extreme-size tests — None.*

Phase 8 note: **R8.1 is landed early** — `ExportSize` (added as the
R0.5 sample, later hardened with `MaxTotalPixels`) implements
width/unit parsing, aspect calculation, rounding, and safety limits,
with `tests/GanttCreator.Raster.Tests/ExportSizeTests.cs`. The row
verifies only the residual: the R8.7 preview value that will feed
`ExportSize`, and the acceptance-criteria links to the existing
tests.

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "PNG renderer"
  (the three unit formulas; height calculated, never entered;
  positive dimensions under a documented memory/pixel safety limit).
- [`../04-TEST-STRATEGY.md`](../04-TEST-STRATEGY.md) — "Exact
  metadata and dimensions" (requested width parsing; calculated
  height and rounding; safety rejection before allocation).
- `docs/08-TEST-CHECKLIST.md` section **A** (culture, non-finite,
  overflow — `ExportSize.ParseValue`/`ToPixels` contracts already
  pin these).

## Step 0 — verify prerequisites

- `src/GanttCreator.Raster/ExportSize.cs` + `ExportSizeTests.cs`
  re-read; enumerate the residual gaps (there should be none in the
  parser itself — only the preview feed + acceptance links).
- The architecture formulas re-read; any divergence between the
  parser and the formulas is a defect to fix here (not a new parser).

## Scope

- `src/GanttCreator.Raster/ExportSize.cs` — only if a genuine
  residual gap is found (no rewrites).
- The preview-value contract: the type/shape `ExportSize` expects for
  the R8.7 preview (documented in this row; consumed by R8.7).
- Acceptance-criteria link updates (this file + the roadmap row
  reference the existing tests).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No second parser (the architecture's one-boundary discipline); no
  UI (R8.7); no encoding (R8.5/R8.6); no Core changes.

## Design decisions (behavioural)

- **D1 — Verify, don't rebuild.** The row's automated gate is the
  existing `ExportSizeTests` suite passing plus the new preview-feed
  pin; any parsing change would need its own justification (and would
  re-open the A-section checklist).
- **D2 — The preview feed is exact.** R8.7 shows the calculated
  height/pixel dimensions for a given (value, unit) — that value is
  produced by `ExportSize`; the pin asserts the preview equals the
  parser's output for the same input (no duplicate math in the UI).

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Raster.Tests`**: the
  existing suite green (unchanged); the new preview-feed pin.
- No Office gate (row: None).

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Raster.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A residual gap requires parser changes → stop; that reopens A-section
  coverage and the R0.5 hardening decisions; get the human's nod.
- The preview needs math `ExportSize` cannot express → stop; extend
  the Raster contract deliberately, never duplicate it.

## Risk and rollback

Minimal (verification row). Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **A, G, I**:
existing suite green; preview-feed pin added; acceptance links
recorded; Release 0 warnings; XML docs; REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| `ExportSize` already implements parsing/aspect/rounding/safety with tests | fact | roadmap Phase 8 note (R8.1) + `ExportSizeTests.cs` |
| The three unit formulas are the architecture's | fact | architecture "PNG renderer" |
| R8.7's preview consumes `ExportSize`; no duplicate math | fact | this row's D2 |
| `MaxTotalPixels` is the current safety limit | fact | the R0.5 hardening commit |
