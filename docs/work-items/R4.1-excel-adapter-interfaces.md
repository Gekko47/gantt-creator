# Work item — R4.1

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit (see [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md)).
> Depends on: Phase 3 complete (scene model, R3.1–R3.14); R2.x ports.

## Outcome

The narrow Excel write-side adapter ports the renderer needs — shape
creation/update/delete by stable ID with ownership tags, and live
panel-grid measurement — exist beside the four landed ports, with no
duplication, proven by architecture tests and compiling contracts.

## Context and owed deferrals (residual row)

Roadmap row: *Add narrow Excel application/workbook/worksheet/shape
adapter interfaces — Architecture test keeps Core reference-clean;
adapter contracts compile — None.*

Phase 4 note: **R4.1 is landed early in part** — `IExcelApplicationAdapter`
(R1.5), `IWorkbookInitialiser` (R2.2), `IGanttTableReader` (R2.4),
`IGanttValidationReporter` (R2.6) exist. This row adds the
shape/workbook-write adapters the renderer needs, following that
pattern; it **must not duplicate the existing ports**.

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Ports and
  adapters" (narrow ports, no interop type crossing; CS0433
  discipline), "COM ownership and application state" (locals, no
  chained access, one ownership policy).
- [`../04-TEST-STRATEGY.md`](../04-TEST-STRATEGY.md) — "Excel shape
  contract tests": the renderer talks to narrow fake adapters; contract
  fakes stay small and behaviourally faithful — **no complete fake
  Excel object model**.
- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Ownership, editability, and refresh" (ownership metadata beginning
  `GanttCreator.`).

## Step 0 — verify prerequisites

- Phase 3 landed: the scene model of R3.2/R3.12 (`GanttScene`,
  `ScenePrimitive`, `ZLayer` — **verify exact names against landed
  code**; this guide was authored against guides, not code).
- The four existing ports re-read; the new ports' surfaces are
  subtractive from them (no member duplication).
- `src/GanttCreator.Office/InternalsVisibleTo.cs` and the seam/Moq
  pattern from `ExcelWorkbookInitialiser` re-read.

## Scope

- `src/GanttCreator.Office/` — the new ports + PIA implementations +
  `internal virtual` seams (naming fixed at Tier-A upgrade): one
  shape-write port (create/update/delete by stable scene ID; set type,
  name, ownership tag, `Single` geometry, fill/stroke/font/alignment/
  pattern; group membership; z-order application) and one panel-grid
  measurement port (produces the `PanelCellGrid` input of R3.11 from
  live cells).
- Small contract fakes + tests in
  `tests/GanttCreator.Office.ContractTests/`.
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No rendering logic (R4.3+); no state scope (R4.2); no changes to the
  four existing ports; no Core changes; no complete fake Excel object
  model (test-strategy rule).

## Design decisions (behavioural; exact signatures at Tier-A upgrade)

- **D1 — Two new ports only.** A shape port and a panel-grid port;
  everything else the renderer needs already exists. Each surface is
  primitive-typed (strings/doubles/enums owned by Office or Core) —
  no interop types cross.
- **D2 — Ownership tag is a first-class member.** Every shape write
  carries the `GanttCreator.`-prefixed ownership metadata; deletes and
  lookups filter by it (the R4.7/R4.8 reconciliation and preservation
  contracts depend on this member existing from day one).
- **D3 — Geometry enters as `double` points, leaves as `Single` through
  the one boundary.** The port applies `GeometryMath.
  SnapToDisplayPrecision` (R3.1) internally; callers never pre-round.
- **D4 — Fakes are narrow and sequence-asserting.** They record the
  observable operation sequence and values (the test-strategy list);
  nothing more.

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Architecture.Tests`**:
  Core stays Office-free; AddIn names no interop types; **no duplicate
  port members** across Office interfaces (a reflection-based
  architecture test, if none exists yet, pins D1's no-duplication
  rule).
- **Automated — `dotnet build` Release 0 warnings** and the contract
  fakes' basic behaviour tests green.
- No Office gate (row: None).

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
dotnet test tests/GanttCreator.Architecture.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- A needed member already exists on a landed port → extend that port's
  consumers instead of duplicating (note: R1.5's adapter is
  read/event-side; writes belong here).
- A port member would need an interop type in its signature → stop
  (CS0433 discipline).

## Risk and rollback

Main risk: port-surface churn once rendering starts — accepted for
Tier B; the Tier-A upgrade (Phase 3 exit) fixes signatures against
landed code. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **G, J**: ports
compile; no duplication; fakes narrow; Release 0 warnings; XML docs;
REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| Four ports already exist and this row must not duplicate them | fact | roadmap Phase 4 note (R4.1) |
| Contract fakes stay small; no fake Excel object model | fact | test strategy "Excel shape contract tests" |
| Ownership metadata begins `GanttCreator.` | fact | entity guide "Shared entity contract" + "Ownership, editability, and refresh" |
| The panel-grid port's output contract | fact | R3.11's `PanelCellGrid` (verify the landed name at Step 0) |
