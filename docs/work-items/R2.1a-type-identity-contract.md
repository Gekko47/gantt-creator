# Work item — R2.1a

> **Guide status**: Implemented 2026-09-23; pending commit/review. Core 364/364 PASS; Release build 0 warnings/0 errors; `verify-quick.ps1` PASS. Office gate: None. Follow [`IMPLEMENTATION-PLAN.md`](IMPLEMENTATION-PLAN.md) and [`../GanttCreator_R2_Implementation_Plan.md`](../GanttCreator_R2_Implementation_Plan.md).

## Outcome

Pin the durable `GanttEntityType` identity separately from the 16 persisted display labels and use “16 selectable Types” versus “visual/entity guide sections” consistently.

## Scope

- `src/GanttCreator.Core/GanttEntityType.cs`
- `src/GanttCreator.Core/EntityTypeCatalog.cs`
- `src/GanttCreator.Core/GanttSchemaVersion.cs`
- `tests/GanttCreator.Core.Tests/GanttEntityTypeTests.cs` (new)
- `tests/GanttCreator.Core.Tests/EntityTypeCatalogTests.cs`
- `docs/07-GANTT-ENTITY-GUIDE.md`
- this guide and the findings disposition

## Decisions

1. Enum numeric values and member names are the durable machine identity.
2. Exact display names remain Ordinal, case-sensitive workbook schema text.
3. A display-label migration does not renumber or rename the enum; no localisation is implemented.
4. The terms are **16 selectable Types** and **visual/entity guide sections**; no Type is added to reconcile section counts.

## Acceptance tests

A table-driven `[Theory]` pins all 16 enum names and numeric values. Existing catalogue tests continue to prove one definition per enum value, exact display labels/order, unique identities, and exact parsing. Schema version remains 1.

Checklist: C, G, I.

## Commands

```powershell
dotnet test tests/GanttCreator.Core.Tests
pwsh ./scripts/verify-quick.ps1
git diff --check
```

Office gate: None.

## Stop conditions

- A required Type count differs from 16.
- A label change is proposed without a schema migration.
- The implementation would add localisation or a parallel Type list.

## Implementation evidence

- `dotnet test tests/GanttCreator.Core.Tests` — PASS, 364/364.
- `dotnet build GanttCreator.slnx -c Release` — PASS, 0 warnings, 0 errors.
- `pwsh ./scripts/verify-quick.ps1` — PASS, all 11 steps.
- Office gate: None, by design.
- Checklist certified: C, G, I.
- STATUS is intentionally not updated until a commit hash exists.
