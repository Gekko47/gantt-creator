# Work item — R4.5

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit. Depends on: R4.1–R4.4; Phase 3
> polygon primitives + z-order.

## Outcome

Milestone diamonds (four-point scene polygons) render as real Excel
freeform shapes, and the complete scene order is applied
deterministically — no opportunistic `BringToFront` — pinned by shape
count/type/order and milestone-geometry contract tests and a live
diamond/overlap inspection gate including same-date events.

## Context and owed deferrals

Roadmap row: *Render polygons/milestones and deterministic z-order —
Shape count/type/order and milestone geometry tests — Required:
inspect diamonds and overlaps in Excel, including same-date events.*

## Governing contracts

- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Z-order contract" (the layer table + layer-40 rule; "An agent must
  not call `BringToFront` opportunistically. The renderer applies this
  complete ordering deterministically"), §20/§21 milestone geometry.
- [`../04-TEST-STRATEGY.md`](../04-TEST-STRATEGY.md) — "Excel shape
  contract tests" (z-order application; group membership and child
  order).
- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Live Excel
  renderer" (step 6: "Apply z-order deterministically").

## Step 0 — verify prerequisites

- R4.1–R4.4 landed; Phase-3 `ScenePolygon` + the ordered
  `GanttScene.Primitives` list landed (verify names).
- The freeform API path probed from the PIA at Tier-A upgrade
  (`Shapes.BuildFreeform`/`ConvertToShape` — verify exact members and
  point semantics before relying).

## Scope

- `src/GanttCreator.Office/` — polygon rendering + the z-order
  application pass over the full owned set.
- `tests/GanttCreator.Office.ContractTests/` — count/type/order and
  diamond-geometry tests.
- `tests/GanttCreator.Office.IntegrationTests/` — live diamond/
  overlap inspection (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No style matrix (R4.6 — diamonds render with basic fill/stroke from
  R4.3's path); no reconciliation (R4.7); no groups (export grouping
  is Phase 6; `SceneGroup` metadata is consumed there).

## Design decisions (behavioural)

- **D1 — Diamonds via freeform at exact points.** Four scene points →
  one freeform shape; geometry asserted point-for-point within the
  documented tolerance; name/tag per R4.3 D2.
- **D2 — Z-order = scene order, applied in one pass.** The renderer
  creates/updates in the scene's already-deterministic order and
  applies any reordering the host requires in a single deterministic
  pass derived from that order — never per-shape `BringToFront`
  calls, never host-default order.
- **D3 — Same-date and same-stack overlaps are visible and ordered.**
  The live gate inspects same-date milestones and same-stack bars:
  overlap rendering follows the scene's subtype/sort/ID order exactly
  (entity guide: reproducible without pretending overlaps do not
  exist).

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Office.ContractTests`**:
  shape count/type pins for a mixed scene; diamond four-point pins;
  full-order pin (the observed creation/order-operation sequence
  equals the scene sequence); no-`BringToFront`-outside-the-pass
  assertion (**positive test**: an opportunistic call would fail the
  sequence pin).
- **Office host (Required)** — live: inspect diamonds (point geometry
  within tolerance) and overlaps (same-date milestones; same-stack
  bars) in Excel; record host builds.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- The host cannot express the scene order deterministically (e.g.
  z-order API gaps) → stop; spike + ADR path before any workaround.
- Freeform point semantics differ from assumed (scaling/offset
  traps) → stop with the probe evidence.

## Risk and rollback

Main risk: freeform API quirks — deliberately unresolved at Tier B;
the Tier-A upgrade probes first. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **E, J**:
count/type/order and diamond pins green; no opportunistic z-order
calls; live gate recorded with host builds; Release 0 warnings; XML
docs; REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| The renderer applies the complete z-order deterministically; no opportunistic `BringToFront` | fact | entity guide "Z-order contract" |
| Refresh step 6 is "Apply z-order deterministically" | fact | architecture "Live Excel renderer" |
| Diamonds are four-point scene polygons rendered as freeforms | fact | R3.7 D1 + R3.2 D2 (verify landed names) |
| `Shapes.BuildFreeform`/`ConvertToShape` express exact points | unknown until probed | PIA probe at Tier-A upgrade |
