# Work item — R4.7

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 3 exit. Depends on: R4.1–R4.6; Phase 3
> scene model + validator.

## Outcome

Refresh is idempotent and reconciles the owned-shape set by stable
scene ID — two refreshes produce identical owned IDs, counts, and
bounds; blocking errors leave the last valid chart unchanged — pinned
by reconciliation contract tests and a live double-refresh gate with
no duplicates, flicker defect, or selection corruption.

## Context and owed deferrals

Roadmap row: *Add idempotent refresh and owned-shape reconciliation —
Two refreshes produce identical owned IDs/counts/bounds — Required:
refresh twice and confirm no duplicates, flicker defect, or selection
corruption.*

## Governing contracts

- [`../02-ARCHITECTURE.md`](../02-ARCHITECTURE.md) — "Live Excel
  renderer": the seven refresh steps (read+validate → build scene →
  blocking errors shown without changing existing shapes → state
  scope → update/create/delete owned shapes by stable scene ID →
  deterministic z-order → restore even after failure); "Prefer
  updating existing shapes to delete/recreate … if diff rendering
  becomes unreliable, replace the complete owned set atomically within
  the state scope; document the choice with benchmarks"; refresh runs
  only on explicit commands; a failed Refresh leaves the last valid
  chart unchanged.
- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) —
  "Ownership, editability, and refresh"; "Refresh-only rendering
  contract" (normal edits never render).
- `docs/08-TEST-CHECKLIST.md` section **E** (refresh idempotence;
  blocking errors leave the chart unchanged).

## Step 0 — verify prerequisites

- R4.1–R4.6 landed; the scene builder + validator (R3.12) land the
  scene; the R2.4 reader + R2.5/R2.9 validation feed it.
- The seven-step order re-read; D1–D3 implement it literally.

## Scope

- `src/GanttCreator.Office/` — the refresh orchestrator (reconcile by
  stable ID: update-in-place, create missing, delete orphaned-owned;
  one atomic fallback path).
- `tests/GanttCreator.Office.ContractTests/` — reconciliation tests.
- `tests/GanttCreator.Office.IntegrationTests/` — live double-refresh
  (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No Ribbon wiring (R4.9); no benchmark (R4.10); no unowned-content
  guarantees beyond the ownership filter (R4.8 owns the sentinel
  tests); no PowerPoint/PNG.

## Design decisions (behavioural)

- **D1 — Reconcile by stable scene ID.** Owned shapes are matched by
  the R4.3 D2 name/tag pair: matched → update geometry/style/text in
  place; scene-missing → delete (owned only); scene-new → create. The
  observed operation set for an unchanged scene is **empty** (the
  idempotence pin).
- **D2 — Update-first; atomic fallback.** Default is update-in-place
  (preserves selection and performance). The complete-replace
  fallback exists behind the state scope and is chosen only with
  recorded benchmark evidence (architecture rule) — not as a
  convenience.
- **D3 — Blocking errors short-circuit before any shape mutation.**
  The step-3 rule: validation errors are surfaced (reusing the R2.6
  note flow) and the owned set is untouched (**positive test**).
- **D4 — Selection and state.** The R4.2 scope wraps the whole
  reconciliation; selection is captured/restored per its D3; the live
  gate explicitly checks selection integrity after double refresh.

## Acceptance tests

- **Automated — `dotnet test tests/GanttCreator.Office.ContractTests`**:
  unchanged-scene → zero operations (**positive pin**); changed scene
  → exactly the expected update/create/delete set; orphaned-owned
  deleted; blocking-error short-circuit (**positive test**); state
  restored after an injected mid-reconciliation failure (checklist K).
- **Office host (Required)** — live: refresh twice on the reference
  data; confirm identical owned IDs/counts/bounds, no duplicates, no
  visible flicker defect, no selection corruption; record host builds.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Office.ContractTests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- Diff reconciliation proves unreliable (flicker, partial states) →
  do not patch opportunistically: take the atomic fallback with
  benchmark evidence per D2, or stop if even that fails.
- An owned shape cannot be matched by ID (host renaming shapes) →
  stop; the ownership model is the invariant.

## Risk and rollback

Main risk: update-in-place style drift (stale properties on reused
  shapes) — mitigated by full-property update pins in the matrix
  tests (R4.6). Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **E, J, K**:
idempotence and reconciliation pins green with positives; live
double-refresh recorded with host builds; Release 0 warnings; XML
docs; REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| The seven refresh steps and their order | fact | architecture "Live Excel renderer" |
| Prefer update; atomic full-replace fallback only with benchmark evidence | fact | architecture "Live Excel renderer" |
| Two refreshes → identical owned IDs/counts/bounds | fact | roadmap row R4.7 + checklist E |
| Blocking errors leave existing shapes unchanged | fact | architecture refresh step 3 + checklist E |
