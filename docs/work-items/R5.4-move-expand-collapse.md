# Work item — R5.4

> **Guide status**: Authored (Tier B — binding contract) 2026-09-21 ·
> upgrade to Tier A at Phase 4 exit. Depends on: R5.3 (parent/child
  model), R3.4 (ordering), R4.7 (refresh reconciliation).

## Outcome

Move-up/down reorders rows deterministically and expand/collapse rolls
child rows up under their parent — the visible worksheet rows and the
rendered shapes stay aligned after Refresh — pinned by deterministic
ordering and visibility tests plus a live alignment exercise.

## Context and owed deferrals

Roadmap row: *Add move-up/down and expand/collapse display commands —
Deterministic ordering and visibility tests — Required: exercise
controls and confirm visible rows/shapes remain aligned.*

## Governing contracts

- [`../07-GANTT-ENTITY-GUIDE.md`](../07-GANTT-ENTITY-GUIDE.md) §10
  ("expand/collapse interaction where supported"), §9 (deterministic
  lane/stack ordering), "Refresh-only rendering contract".
- R2.3 (stable IDs survive reordering), R3.4 D2 (the ordering key).

## Step 0 — verify prerequisites

- R5.3 landed (parent model + row-mutation adapters); R3.4's ordering
  key re-read — move-up/down **writes `SortOrder`** (or row position,
  decided at Tier-A upgrade) rather than shuffling physical rows
  blindly; the chosen mechanism must keep R3.4 D2 deterministic. Effective
  `StackIndex` is re-derived from the resulting row/parent-child display
  position; move/expand/collapse never writes it.
- The `Visible` column semantics (blank = visible) re-read.

## Scope

- `src/GanttCreator.Core/` — reorder policy (move within the
  deterministic order; typed bounds) + roll-up visibility policy
  (collapsed children are excluded from the *scene*, not deleted from
  the table).
- `src/GanttCreator.Office/` — row-mutation reuse (R5.3's adapters).
- `src/GanttCreator.AddIn/` — the four commands + ribbon callbacks.
- Tests per layer + integration (tagged `OfficeIntegration`).
- `docs/REPO-MAP.md`, `docs/STATUS.md`, status line.

## Exclusions

- No parenting changes (R5.3); no label/style controls (R5.5/R5.6);
  no rendering beyond the existing Refresh; no schema change
  (collapse state is command-derived display state, not a new column —
  **verify no hidden-state persistence is required; if the product
  wants persisted collapse state, that is a schema ADR — stop-point**).

## Design decisions (behavioural)

- **D1 — Move = order write, not row shuffle.** Move-up/down adjusts
  the deterministic-order key (`SortOrder` first; physical row moves
  only if the Tier-A upgrade proves them necessary); IDs never change.
  R3.4 re-derives effective stack slots from display position; no command
  writes the visible `StackIndex` compatibility cell.
- **D2 — Collapse is scene exclusion.** Collapsed children are
  excluded from the built scene (their rows remain in the table
  untouched); the parent renders as its roll-up per §10. Expanding
  re-includes them. Both act only through Refresh.
- **D3 — Alignment invariant.** After any command + Refresh, the
  visible data-panel rows and the rendered lane shapes align exactly
  (row/data-panel alignment is §9's test obligation); the live gate
  proves it on the real sheet.

## Acceptance tests

- **Automated**: Core — reorder determinism (repeated moves stable;
  bounds refusals with **positive tests**); roll-up visibility matrix
  (collapsed/expanded/parent-only scenes pinned). Office — mutation
  sets; refusals. AddIn — command contracts + ribbon pins.
- **Office host (Required)** — live: exercise all four controls;
  confirm visible rows and shapes remain aligned after Refresh;
  record host builds.

## Evidence commands

```powershell
dotnet build src/GanttCreator.slnx /p:Configuration=Release /warnaserror
dotnet test tests/GanttCreator.Core.Tests
dotnet test tests/GanttCreator.Office.ContractTests
dotnet test tests/GanttCreator.AddIn.Tests
pwsh ./scripts/verify-quick.ps1      # expect exit 0
pwsh ./scripts/verify-office.ps1     # Required, expect exit 0
git diff --check                     # expect no output
```

## Stop conditions (item-specific)

- Persisted collapse state is requested → stop; schema ADR required
  (D-exclusions).
- A move would rewrite IDs or break R3.4 D2 determinism → stop.

## Risk and rollback

Main risk: order-key vs physical-row drift (Excel table sort state) —
the Tier-A upgrade probes live behaviour; the alignment gate catches
it. Rollback: `git revert`.

## Definition of done

From `AGENTS.md` + `docs/08-TEST-CHECKLIST.md` sections **B, C, G, I,
J**: ordering/visibility matrices green with positives; live alignment
gate recorded with host builds; Release 0 warnings; XML docs;
REPO-MAP/STATUS current.

## Evidence-ledger seeds

| Statement | Status | Evidence |
| --- | --- | --- |
| Splitter supports expand/collapse interaction "where supported" | fact | entity guide §10 |
| Row/data-panel alignment is a §9 test obligation | fact | entity guide §9 |
| IDs never change on reordering | fact | R2.3 |
| Collapse state persistence is not in schema v1 | inference | no schema column exists; stop-point if the product wants persistence |
